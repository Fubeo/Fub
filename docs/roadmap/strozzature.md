# Dove il contratto si strozza, capitolo per capitolo

Una riga per **famiglia di FEATURES**: cosa servirebbe perché quelle voci siano provider, e cosa lo impedisce oggi. È l'indice inverso della roadmap — si entra dal capitolo di FEATURES invece che dalla seduta.

[← indice](../todo.md)

---

**Come si legge questo file.**

* Una riga barrata è chiusa e porta il verbale.
* Una riga viva dice cosa manca **oggi**.

**Come mente.** Il modo in cui questo file diventa falso è uno solo, e non è che
qualcuno ci scriva una bugia: una riga invecchia quando la cosa che descrive si
chiude **altrove**, in un file che chi chiude non sta guardando.

* Un giro dedicato ne ha trovate **diciassette** che il codice smentiva, su
  ottantasette. Le barrate sono passate da ventinove a quarantasei senza che si
  chiudesse niente di nuovo.
* Prima di quel giro era già successo due volte: la riga «nessun `^block-id`» è
  stata falsa per undici verbali, quella su `views()` per trentaquattro.
* **Chi entra da qui cerca se una cosa manca, ed è per questo che una riga
  vecchia non allunga il lavoro: lo dirotta.**

**Perché nessun presidio lo copre.** La
[§16.8](16-crate-sdk-banchi-di-prova.md#168-la-prosa-che-conta-i-sorgenti-non-ha-nessun-presidio)
ha chiuso il presidio dei **numeri**
([0072](../decisions/0072-un-numero-si-scrive-accanto-a-come-si-ricava.md)), e
questo file resta la metà che non copre.

* Una riga di qui non porta un conteggio, porta un **giudizio**.
* Renderla rossa chiede prima di decidere cosa significhi «chiusa» per una
  strozzatura.
* Il collegamento però è verificabile quanto un link rotto: un `§X.Y` che si è
  chiuso, un simbolo che non esiste più. Ed è da lì che ricomincia chi la prende.

**Ed è successo di nuovo.** Una rilettura riga per riga contro il codice — non un
giro, un collaudo — ne ha trovate **ventitré** false in un colpo solo.

* Due nominavano un simbolo che nel repo non esiste più (`saveCurrent`,
  `ViewContext.selection`).
* Una si dichiarava «l'unica P0» quando di P0 aperte non ce n'era più nessuna.
* Le barrate sono passate da **sessanta** a **settantotto**, di nuovo senza che
  si chiudesse niente di nuovo.

La regola che ne esce, imparata per la terza volta: **chi chiude una voce cerchi
qui dentro il simbolo che ha appena cambiato**, perché la riga che lo nomina non
è nella seduta che sta guardando.

| Famiglia FEATURES | Cosa serve | Cosa manca oggi |
|---|---|---|
| ~~4.2 slash/hotkey, 16.2 automazioni, 27.1 CLI, 20.1 comandi plugin~~ | ~~`CommandProvider` vivo~~ | **chiuso ([decisione 0009](../decisions/0009-registro-dei-comandi.md))**: `Workspace` ha la tabella (`commands()`, `workspace.rs`) e `CoreCommands` ci sta dentro con quindici comandi (`features/src/commands.rs`). Questa riga ha detto «nessuno lo registra» per **quarantuno verbali** dopo che il registro c'era — ed è la prima riga del file, cioè quella che chi entra da qui legge per prima |
| ~~8.2 proprietà, 11 database, 16.1 template, 19.3 form, 28 settings~~ | ~~`UiNode` con **input**~~ | **chiuso ([decisione 0016](../decisions/0016-cosa-e-una-view.md))**: le varianti di `ui-kind` sono **trentatré**, e fra loro ci sono `text-input`, `text-area`, `number`, `checkbox`, `select`, `radio`, `slider`, `date-picker`, `form`, `table` e `tree` — cioè esattamente le cinque che questa riga elencava come mancanti |
| ~~20.1 impostazioni plugin, 28 config/profili, D7 spegnibilità~~ | ~~store di configurazione + schema dichiarativo~~ | **chiuso ([decisione 0036](../decisions/0036-le-impostazioni-e-i-tre-stati.md))**: lo schema lo dichiara il **manifest** (`PluginManifest.settings`, prima di `activate`, con i nomi passati a `rules::ids::check` come i servizi), il valore vive **nel vault** (`.fub/settings.json`) sopra il default dello schema — un posto solo, dalla [decisione 0076](../decisions/0076-le-impostazioni-vivono-nel-vault.md), con la sola diagnostica (`log.*`) rimasta nel file della macchina perché deve valere anche quando un vault non si apre — e `SettingSource` dice chi ha vinto. `FUB_VERSIONING` non c'è più: è `versioning.enabled`, e accanto le sta `plugins.disabled`, che è l'interruttore dell'host. Di variabili d'ambiente ne restano **due, per decisione**: `FUB_VAULT` non è una preferenza che dura ma un **argomento di avvio** (il gemello del `fub <path>` della CLI 27.1), e `FUB_CONFIG_DIR` è il **bootstrap** — dove stanno le impostazioni è l'unica cosa che non può essere un'impostazione |
| ~~13 allegati, 12 canvas, 11.4 CSV/JSON, 6.1 media~~ | ~~entry di vault che non sono documenti~~ | **chiuso ([decisione 0046](../decisions/0046-l-anagrafe-del-vault.md))**: `IndexQuery::Entries` porta un `VaultEntry { id, kind, size, mtime }` per **ogni** file, e la specie non si persiste perché dipende da chi è registrato adesso. `Vault::list_documents` non esiste più in `vault.rs`: un PNG esiste, e il controllo di salute distingue un allegato che c'è da uno che manca. Resta la **scrittura** degli allegati, che è la casella residua del [§14.1](14-entry-cartelle-lista.md#141-il-vault-cartella-di-progetto-include-documenti-e-asset) e sarà additiva |
| ~~18 sync, 23.1 cifratura at-rest, 26.3 PWA/OPFS, 3.1 vault read-only~~ | ~~astrazione sullo storage~~ | **chiuso ([decisione 0064](../decisions/0064-il-supporto-sta-sotto.md))**: `trait VaultStorage` in `kernel/storage.rs`, con `FsStorage` di default e un `MemStorage` che lo tiene onesto; sotto la linea del vault — documenti, cestino, sidecar, spazio dati dei plugin — `std::fs` non compare più, quindi un supporto che cifra è un `impl` e non un `if`. I **tre** file di `.fub/` che restavano fuori ci sono saliti con la [0065](../decisions/0065-una-scrittura-o-c-e-o-non-c-e.md), che ha dato al supporto l'atomicità che loro avevano già; resta il varco di `plugin_data_dir`, che consegna a un provider nativo una cartella vera — su un supporto che cifra è il punto in cui la cifratura si ferma, e la sua risposta è M5 |
| 10 task, 5.2 id di blocco, 7.1 link a blocco | modello con task e ancore stabili, **e un link che ci arrivi** | **quasi chiuso ([0003](../decisions/0003-modello-del-documento.md) + [0049](../decisions/0049-una-posizione-dentro-un-documento.md))**: il modello ce l'ha dalla 0003 — `ListItem.task` porta lo stato di spunta insieme al marcatore, ogni blocco porta un `anchor`, esiste `Anchor { id, span, marker }` con `canonical_anchor`/`valid_anchor` in `rules/`, e `LinkTarget::Wiki` porta il `block`. Il **link** adesso arriva: `Resolved(option<resolved-ref>)` ha dove mettere il punto, il kernel tiene le ancore in cache e nell'anagrafe, e il renderer emette `data-wikilink-block`. Resta una metà sola, e non è di firma: **nessuno conia** un'ancora — «copia il link a questo blocco» è una scrittura, quindi passa da `write_document`/`apply_edit` ([0008](../decisions/0008-modifica-chirurgica.md)) e non scade col freeze. Questa riga ha detto «nessun `^block-id`» per undici verbali dopo che l'ancora era entrata — un indice inverso vecchio non allunga il lavoro, lo **dirotta** |
| 9.1 faceted/field search, 9.2 query engine, 8.4 collezioni | canale query su **proprietà** | c'è (0005) e adesso si **compone** col testo (0019): resta da disegnare chi lo usa — il builder e le viste salvate |
| 8.2 proprietà typed (~16 specie), 8.3 note types, 11 database, 16.1 template | una specie **dichiarata** per chiave, di proprietà del **vault** | il valore è normalizzato **per valore** ([0003](../decisions/0003-modello-del-documento.md)) — `PropertyValue` sa che una data è una data e che un `[[wikilink]]` è una relazione — e `rules/properties.rs` decide già che specie diverse non si confrontano (`>` fra un numero e un testo è *falso*, non un errore). Manca la **dichiarazione per campo**: in tutto il repo non esiste un `PropertyType`, quindi «`scadenza` è una data» non è dicibile e ogni cliente la inferisce dal primo valore che incontra. Serve a validare in scrittura, a distinguere «campo assente» da «campo di specie sbagliata» — che oggi collassano entrambi in *falso, in silenzio* — e a disegnare il controllo giusto: 8.2 chiede colore, icona, rating, URL ed email, che **come valore** sono tutte stringhe. La forma è già stata trovata una volta ed è quella dello schema delle impostazioni ([0036](../decisions/0036-le-impostazioni-e-i-tre-stati.md)) — dichiarato, a livelli, con un cancello alla scrittura — con una differenza che decide dove vive: lì il proprietario di una chiave è il plugin che la dichiara, qui il frontmatter è dell'utente e le chiavi le leggono e le scrivono in molti, quindi il registro è del vault e non di chi arriva primo |
| ~~9.1 ricerca predefinita (refusi, prefisso), 8.4 e 16.1 che ne riusano il canale~~ | ~~una query che dica **quanto** si vuole essere indovinati~~ | **chiuso ([decisione 0050](../decisions/0050-cosa-si-chiede-a-una-ricerca.md))**: `TextQuery` porta `tolerance` (`Exact`/`Typos` — un'intenzione, mai una distanza di edit) e `partial_last_term`, e `TextField` nomina gli heading. Ciò che mancava davvero non era il fuzzy ma il modo di chiedere l'**esattezza**: era implicita, e ciò che è implicito non si può pretendere su un canale da cui poi si scrive. Il fuzzy vero resta lavoro di provider, e non scade |
| ~~9.1 ricerca nella nota aperta, salto all'occorrenza, occorrenze multiple per nota~~ | ~~un estratto ancorato al **documento**, non allo snippet~~ | **chiuso ([decisione 0049](../decisions/0049-una-posizione-dentro-un-documento.md))**: accanto a `highlights` (che restano intervalli dentro `snippet`, perché servono a **disegnare** una riga) c'è `occurrences: list<doc-position>`, byte del **sorgente** con la revisione su cui sono stati misurati. La regola di `absorb` non è stata rovesciata ma resa dipendente da chi chiede: un estratto per documento, N occorrenze. A riempirle è il kernel e non chi indicizza — un motore full-text indicizza la proiezione a testo piano, e fra quella e il sorgente non c'è nessuna mappa |
| ~~8.1 quick switcher, 4.2 palette, 9.1 casella di ricerca, 5.2 e 7.1 autocompletamento dei wikilink~~ | ~~una porta sola verso l'indice~~ | **chiuso ([decisione 0082](../decisions/0082-una-porta-per-chi-cerca.md) + [0083](../decisions/0083-le-due-superfici-che-restavano.md))**: la porta è `IndexQuery::Documents` e le query si compongono in `frontend/src/host/contract.ts`, non nei pannelli — tre configurazioni (campi liberi, un letterale `Docs` in più, campi sul nome) e non tre porte. Il quick switcher **adesso esiste** e non è nato su `list_documents`: `panels/quick-switcher.ts` su `Mod-o`, `TextField::Name` e `Excerpts::Omit`. L'autocompletamento di `[[` non chiede più l'elenco intero: chiede il **prefisso** (§21.2) e riceve una finestra ordinata, quindi perde il `validFor` che lo teneva in piedi e prende `filter: false` perché l'ordine è già la rilevanza. Il budget per **battuta** — che è ciò che rendeva questa riga diversa dalle altre — è misurato: fase 5 di [`una_ricerca.rs`](../../crates/fub-features/examples/una_ricerca.rs) |
| ~~17 import/export/migration (~120 voci)~~ | ~~trait `ImportProvider`/`ExportProvider`~~ | **chiuso ([decisione 0006](../decisions/0006-import-export-come-trait.md))**: i due trait stanno in `abi/transfer.rs`, `MarkdownImport`/`MarkdownExport` li implementano, il dispatch è `can_handle` e la sorgente è a byte. Un formato nuovo è un provider, non codice dell'app |
| ~~24.1 task manager, 22 AI, 14.2 clipper~~ | ~~lavoro in background reale~~ | **chiuso ([decisione 0032](../decisions/0032-il-runner-dei-job.md))**: `take_pending_jobs` ha il suo chiamante di produzione in `host/runner.rs` — un pool di thread per vault che drena la coda da sé, si lascia annullare a bandiera e si ferma prima che il vault chiuda. `spawn_job` in produzione esegue |
| ~~3.1 vault multipli, 3.3 finestre multiple, 26 piattaforme~~ | ~~sessioni multiple~~ | **chiuso ([decisione 0029](../decisions/0029-chiudere-un-vault-e-chiuderli-tutti.md) + [0036](../decisions/0036-le-impostazioni-e-i-tre-stati.md))**: `Host` teneva **una** `Option<VaultSession>` e aprire un vault chiudeva quello aperto; adesso i vault aperti sono una mappa (`host/session.rs`), ogni comando IPC accetta un `vault` opzionale e il «corrente» è tornato a essere una comodità della shell. La metà che la 0029 non poteva chiudere — *dove si scrive l'elenco dei vault fra un avvio e l'altro*, perché un elenco di vault non sta in nessun vault — l'ha chiusa la 0036 col livello macchina (`vaults.json`, recenti/preferiti/icone, `host/vaults.rs`) |
| ~~5.3 sicurezza markdown, 23 privacy~~ | ~~sanitizzazione e CSP in un punto~~ | **chiuso ([decisione 0017](../decisions/0017-chi-disegna-cio-che-il-core-non-conosce.md))**, che ha preso il §3.6: il sanitizer è un punto solo (`setSanitizedHtml`/`sanitizeFragment`, `ui/sanitize.ts`), e ciò che arriva da un provider non diventa mai markup; e la **CSP** c'è anche lei, e non è una promessa: sta in [`tauri.conf.json`](../../crates/fub-app/tauri.conf.json) — `default-src 'self'`, `frame-src 'none'`, `object-src 'none'`, `connect-src` limitato all'IPC e nessun `http`/`https` remoto. Questa riga diceva «nessuna policy sui contenuti remoti» mentre la policy li **vieta** |
| ~~25 accessibilità e localizzazione~~ | ~~tema a token + catalogo stringhe~~ | **chiuso**: il **catalogo** c'è ([0040](../decisions/0040-chi-localizza.md), [0042](../decisions/0042-il-catalogo-della-shell.md)): ogni campo che una persona legge è un `Text` che porta la propria provenienza, i provider emettono `Text::key` col proprio catalogo (`features/src/outline.rs`, `backlinks.rs`, `tags.rs`) e chi localizza è il kernel sulla via d'uscita; il catalogo della shell (`frontend/src/i18n/strings.ts`) **non compila** se una chiave manca. E il **tema a token** è arrivato con lo stesso verbale, senza aspettare un numero suo: `frontend/src/theme/tokens.css` più `theme/theme.ts`, con `theme/contrast.test.ts` che legge i token col `?raw` e pretende un rapporto di contrasto invece di fidarsi dell'occhio. «Tema» non vuol più dire riscrivere le regole |
| 2.1 recovery/journaling, 24.2 affidabilità | scrittura durevole, **e** un modo di tornare indietro | la **scrittura** è durevole dalla [0065](../decisions/0065-una-scrittura-o-c-e-o-non-c-e.md): `VaultStorage::write` fa temp+`sync_all`+rename+fsync della cartella, e chi rilegge dopo un crash trova questi byte o quelli di prima — tranne dove l'inode ha altri titolari (symlink, hardlink), dove si scrive sul posto perché la rename farebbe un danno peggiore. E l'**aggiornamento** dei tre file della macchina lo è dalla [0066](../decisions/0066-un-aggiornamento-non-e-una-scrittura.md): `update_atomic` rilegge sotto lock e fonde, quindi due installazioni non si cancellano più chiavi, scroll e preferiti a vicenda. E il **journal** c'è, dalla [0067](../decisions/0067-il-registro-di-cio-che-e-successo.md): `.fub/journal.jsonl`, una riga per mutazione del kernel, e **mai** il contenuto di prima — dalla [0103](../decisions/0103-un-registro-dice-cosa-e-successo.md) nemmeno l'inverso di un edit, che è diventato la sua **impronta**. Quindi il rollback dell'import e la transazione di lotto restano **scrivibili** e sono di chi li userà (17.3, 22.4), ma nella misura che il registro dichiara: rimetteranno i nomi al loro posto e ripescheranno dal cestino, e non disferanno gli edit — come già non disfacevano i salvataggi. E il [§15.2](15-il-disco.md#152-durabilità-e-recovery) è **chiuso** con la [0088](../decisions/0088-cio-che-non-e-ancora-successo.md), che ha preso le due caselle rimaste: il **buffer di crash** — `.fub/drafts/`, un file per bozza, il gemello del journal dall'altro verso: quello conserva l'impronta e mai il testo di ciò che è successo, questo solo il testo di ciò che non è ancora successo — e i **comandi di manutenzione**, che sono del registro con la regola che generalizza la [0086](../decisions/0086-una-cronologia-e-la-sua-porta.md): *la dichiarazione sta nel registro, l'esecuzione sta dove sta il potere*. Di questa strozzatura resta fuori la sola **corruption detection**, che è del 24.2 e non di qui: la [0106](../decisions/0106-un-formato-si-presenta.md) ha chiuso il §15.3 misurando che una versione di schema dice **quale formato** sono quei byte, non **se** quei byte sono integri — un file troncato a metà porta la sua versione giusta in testa e resta troncato |
| ~~3.3 split/finestre, 4.2-4.3 azioni sulla selezione, 13.3, 22.2~~ | ~~contesto per-pane e **selezione** nel contratto~~ | **chiuso ([decisione 0007](../decisions/0007-contesto-di-sessione.md))**: `HostApi::active_context() -> Option<ViewContext>` con pannello, documento, selezione e modalità |
| ~~7.2 bulk fix, 11.3 editing bulk, 16.3 undo, 17.3 rollback~~ | ~~scrittura **a lotti**~~ | **chiuso ([decisione 0011](../decisions/0011-il-lotto.md))**, ed era il **gemello non barrato** della riga che dice la stessa cosa più in basso — la seconda coppia di doppioni di questo file, dopo quella sull'origine degli eventi. `Workspace::batch` esiste (`workspace.rs`) e coalizza `index-updated`: una rinomina con 200 backlink passa da 201 ridisegni a 1 |
| ~~4.2 undo per sessione, 3.3 undo toast, 11.3 undo database, 16.3 undo automazioni~~ | ~~un'operazione che dichiara come si torna indietro~~ | **chiuso ([decisione 0045](../decisions/0045-l-undo-ha-due-pile.md))**: `CommandOutcome.undo: option<undo>` con `undo-step` che è **una modifica** (l'inverso della 0008) **o un comando** — perché le operazioni un nome ce l'hanno già, e annullare una rinomina è una rinomina all'incontrario. La pila è del kernel e dura quanto il vault aperto; `host-commands.undo-last` la consuma, `vault.undo` la invoca. Le due pile — testo nell'editor, operazioni nel kernel — non si fondono, e a decidere quale risponde è il fuoco |
| ~~3.2 cartelle, 8.2 metadata di cartella, 6.2 CSS per cartella~~ | ~~la cartella come cittadino del kernel~~ | **chiuso ([decisione 0047](../decisions/0047-la-cartella-esiste-nel-kernel.md))**: una cartella esiste perché il disco ce l'ha — **vuota compresa** — si chiede con `IndexQuery::Folders` un livello per volta, e `IndexQuery::Entries` prende una cartella. L'albero non è più solo in `organizer.ts` |
| ~~20.1 enable/disable, 20.2 hot reload, 24.2 safe mode~~ | ~~disattivare un provider~~ | **chiuso ([decisione 0028](../decisions/0028-come-un-componente-smette.md) + [0036](../decisions/0036-le-impostazioni-e-i-tre-stati.md))**: la disattivazione **toglie davvero** ciò che un bundle aveva registrato, l'interruttore è `plugins.disabled` (`host/settings.rs`) e non una variabile d'ambiente, e la shell lo aziona da un pannello (`set_plugin_enabled`). Resta il **hot reload**, che è un'altra domanda: rimettere dentro codice nuovo senza riavviare non è la stessa cosa che togliere quello vecchio |
| ~~20.3 sandbox e permessi, 23.1 permessi file/rete~~ | ~~un punto di applicazione dei permessi~~ | **chiuso ([decisione 0021](../decisions/0021-il-confine.md))**: chi registra si dichiara prima (`register_plugin`), e da quel manifest nasce un `Guard<H, P: Policy>` che accende una famiglia di capacità solo se il permesso c'è (`Granted::new`, `host/guard.rs`); un id che nessuno ha dichiarato nega tutto dicendo perché. Dei cinque permessi che **non avevano una capacità** ne restano **quattro** — `fub:external-fs`, `fub:clipboard`, `fub:camera`, `fub:microphone` —: il primo, `fub:network`, la capacità ce l'ha dalla [0097](../decisions/0097-un-recinto-che-vale-anche-quando-nessuno-guarda.md), che ha chiuso la §23.3. E il permesso che una capacità ce l'ha non usa il proprio **parametro**: `read-vault` ristretto a `Progetti/` legge tutto, ed è la casella della [§7.1](07-il-confine.md#la-casella-rimasta) |
| ~~18 sync, 14.2 clipper, 22 AI e RAG, 15.1 citazioni, 13.4 trascrizione~~ | ~~una capacità che parli con qualcosa **fuori dal disco**~~ | **chiuso ([decisione 0097](../decisions/0097-un-recinto-che-vale-anche-quando-nessuno-guarda.md))**: `http_fetch` era escluso dalla [0013](../decisions/0013-elenco-delle-capacita.md) con **due bloccanti nominati**, e sono caduti tutti e due: il §9.1 con la [0027](../decisions/0027-il-lavoro-lungo-vede-il-vault.md) (un job vede il vault, quindi c'è dove mettere ciò che si scarica) e il §7.3 con la [0021](../decisions/0021-il-confine.md), che scrive perfino la riga d'innesto — *«il giorno che `http_fetch` entrerà, `Capability::permission()` è la riga che le dà un permesso»*. Adesso `HostNetwork::fetch` è la **quindicesima** famiglia del contratto (`abi/traits.rs`, impl in `host/net.rs`) e `fub:network` è il primo parametro di permesso che questo repo **legga**: l'allowlist di host si onora, i redirect non si seguono, `*.` è obbligatorio per i sottodomini e le credenziali non prestano un nome dichiarato. Resta una casella: fermare una richiesta già partita |
| ~~2.3 modifiche esterne, 3.1 vault su share, 18.1 sync~~ | ~~che un derivato ritrovi la propria nota dopo una rinomina fatta **ad app chiusa**~~ | **chiuso ([decisione 0099](../decisions/0099-una-rinomina-che-non-ha-visto-nessuno.md))**: il path resta la chiave per sempre ([0043](../decisions/0043-il-path-e-la-chiave.md)) e la migrazione segue le rinomine che il watcher **vede**; quelle che non vede nessuno lasciano la nota «sparita» e una nuova al suo posto, quindi lo spazio per-documento si raccoglie al giro dopo ([0044](../decisions/0044-lo-stato-per-documento.md)), la bozza resta orfana ([0088](../decisions/0088-cio-che-non-e-ancora-successo.md)) e la cronologia delle versioni si stacca. Il materiale per riaccoppiare c'è già — `Revision::of_bytes` ([0087](../decisions/0087-il-testo-che-sta-dentro-gli-allegati.md)) e l'anagrafe durevole ([0046](../decisions/0046-l-anagrafe-del-vault.md)) — e la regola è arrivata: l'apertura **riconosce dal contenuto** ciò che il rilevatore non ha visto — un sparito e un comparso con la stessa impronta sono la stessa nota, e ciò che le stava attaccato la segue. **Uno a uno o niente**, e nel dubbio non si accoppia *e* non si raccoglie: delle due mosse, quella irreversibile si sospende |
| 4.2 modi di scrivere, 6.1 editing, 20.1 UI di plugin | che un terzo porti **un'altra** superficie di scrittura, non solo decori la nostra | **deciso, e la superficie si presta** ([0104](../decisions/0104-la-superficie-di-scrittura-si-presta.md)): *«l'editor è della shell»* vuol dire **questo** editor, non *l'editing*, e un terzo che porti la propria esperienza di scrittura è un cliente previsto. Le sei decisioni che questa riga elencava come somma — 0018, 0078, 0045, 0088, 0090 — **non erano la barriera**: rilette contro la domanda giusta quattro non toccano il caso e la 0018 lo concede per iscritto (*«chi disegna un editor tiene il proprio testo»*). Ciò che c'è già: `ViewSurface::Main` ospitata ([0079](../decisions/0079-il-grafo-esce-dall-overlay.md)), il `pane` del contesto ([0007](../decisions/0007-contesto-di-sessione.md)), le selezioni ([0093](../decisions/0093-le-selezioni-sono-n-e-il-buffer-e-uno.md)), `apply_edit` per scrivere. Restano **due porte, additive e non decise da nessuno**: nel contratto non esiste nessun **evento di tastiera** — un provider riceve `UiAction`, cioè un gesto già interpretato — e `UiNode` è dichiarativo, con `Html`/`WebView` riservati a `Trust::Core`. Il metro ha adesso una **quarta voce** (*se la superficie esiste*) e il caso sta scritto lì come **buco dichiarato** ([plugin-boundary.md](../architecture/plugin-boundary.md#cosa-non-può-essere-solo-un-guest-e-il-metro-per-deciderlo)): non vietato, non attrezzato. Il primo cliente ha un nome ed è la **modalità vim** |
| ~~6.1 anteprima interattiva, 5.3 sanitizzazione~~ | ~~il **modello** al confine, non solo HTML~~ | **chiuso ([decisione 0018](../decisions/0018-chi-vede-il-modello-parsato.md))**: il modello attraversa il contratto (`read_model`) ma **non** l'IPC verso il webview — il modello è del file, la webview lavora sul buffer; `render_preview` resta la fast-path, con le parti dichiarative della [0017](../decisions/0017-chi-disegna-cio-che-il-core-non-conosce.md) |
| ~~24.2 error reporting, 25.2 localizzazione, 16.3 retry~~ | ~~errori tipizzati al confine~~ | **chiuso ([decisione 0041](../decisions/0041-un-errore-e-testo-che-qualcuno-legge.md))**: i nove payload di `plugin-error` sono `text` e non `string` — cioè portano la propria provenienza e si possono tradurre — e le tre varianti nuove (`not-found`, `already-exists`, `io`) distinguono ciò che prima passava tutto come `internal`. La firma `Result<_, String>` con i suoi dodici `map_err(\|e\| e.to_string())` è andata via (`app/lib.rs`): la shell non indovina più. La **destinazione** è arrivata dopo, con la [decisione 0052](../decisions/0052-cio-che-va-storto-e-un-evento.md), e ha usato esattamente questo tipo: `Event::Trouble` porta un `plugin-error`, cioè un `text` che chi mostra traduce |
| ~~27.3 test utilities, 21.1 moduli Suite~~ | ~~un SDK usabile da fuori~~ | **chiuso** con la [0054](../decisions/0054-il-banco-del-lato-provider.md): `MemoryHost` è `fub_sdk::testing`, pubblico. Era `#[cfg(test)]` **e privato** dentro `fub-features` — cioè irraggiungibile perfino dagli integration test del suo stesso crate — e questa riga lo diceva più debole di com'era, oltre che nel file sbagliato (`src/testing.rs`, non `src/lib.rs`). Accanto c'è ora `testing::conformita`, le proprietà del contratto rese eseguibili da chi lo implementa |
| ~~2.2 config, 27.4 upgrade migration~~ | ~~versione di schema sui formati persistiti~~ | **chiuso ([decisione 0106](../decisions/0106-un-formato-si-presenta.md))**: non è più il solo indice di ricerca — bozze, anagrafe, journal, stato di vista e organizzazione portano ognuno la propria costante nominata, e `crates/fub-app/tests/schemi_su_disco.rs` pretende che un formato nuovo ne dichiari una invece di ricordarsene. I formati erano **dieci** e non nove. Ciò che una versione di schema **non** dice è se quei byte siano integri: un file troncato a metà porta la sua versione giusta in testa e resta troncato, e la corruption detection è del 24.2 |
| 20.1 ribbon/menu, 11-12 database e canvas, 7.3 grafo | superfici di UI che la **shell** ospiti | il contratto ne nomina **dieci** (`ViewSurface`, `abi/traits.rs`, dalla [0016](../decisions/0016-cosa-e-una-view.md)) e la shell ne ospita **otto**: la barra di stato è arrivata con la [0035](../decisions/0035-il-lavoro-lungo-si-racconta.md), la scheda di impostazioni con la [0036](../decisions/0036-le-impostazioni-e-i-tre-stati.md), l'area principale con la [0079](../decisions/0079-il-grafo-esce-dall-overlay.md) — e `main` è ospitata in un modo che nessun'altra condivide: non ha un contenitore in `index.html`, perché di riquadri ce ne sono N e un riquadro si riempie quando qualcuno ci mette una tab. Le **due** che restano non sono un buco del contratto ma della shell, e lo dicono a chi dichiara una view lì (`NON_OSPITATE`, `ui/views.ts`): `menu` e `context_menu` vogliono superfici che questa shell non ha |
| ~~11.2 viste multiple, 8.3 viste salvate, 9.2 query embed, 3.3 split~~ | ~~view **istanziabili** con parametri~~ | **chiuso ([decisione 0016](../decisions/0016-cosa-e-una-view.md))**: `ViewInstance { view, instance, params }` esiste, `render_view` e `on_action` lo ricevono, e i parametri arrivano già convalidati contro `ViewSpec.params` — che sono gli stessi `ParamSpec` dei comandi. Questa riga ha detto «`views()` è un elenco statico» per **trentaquattro verbali** dopo che gli esemplari erano entrati: l'elenco delle spec è statico e lo resta, ma non era quello a mancare. Ciò che resta aperto è un'altra cosa e ha un numero suo, la **[§22.3](22-cosa-sa-dire-un-abbonamento.md#223-la-maschera-di-ridisegno-è-della-view-non-dellesemplare)**: i parametri sono dell'esemplare e `ViewSpec.refresh` è della dichiarazione, quindi un widget parametrico non può stringere la propria maschera |
| ~~4.3, 7.2, 8.2, 10.1, 11.3, 16.1, 19.2, 22.2~~ | ~~modificare **un pezzo** di documento~~ | **chiuso ([decisione 0008](../decisions/0008-modifica-chirurgica.md))**: `HostApi::apply_edit(id, EditRequest { base, edits })`, con la revisione nella firma e `Conflict` invece della sovrascrittura silenziosa |
| ~~4.2 undo illimitato, 11.3, 16.3, 17.3 rollback, 3.3 undo toast~~ | ~~un proprietario dell'undo~~ | **chiuso ([decisione 0045](../decisions/0045-l-undo-ha-due-pile.md))**, ed è il **terzo doppione** di questo file: la riga barrata che dice la stessa cosa sta due schermate più su. I proprietari sono **due e non si fondono** — il testo del buffer in CodeMirror, le operazioni nel kernel — e a decidere quale risponde è il fuoco: `vault.undo` è su `Mod-Alt-z` perché `Mod-z` è dell'editor |
| ~~16.2 trigger, 18 sync, 19.2 collaborazione~~ | ~~origine e causalità sugli eventi~~ | **chiuso ([decisione 0012](../decisions/0012-origine-degli-eventi.md))**, ed era il **gemello non barrato** della riga che dice la stessa cosa più in basso: due righe per una domanda sola, e la seconda sapeva già di essere chiusa. `Notice { event, origin }` porta `Origin { actor, batch }`. Il *chi* c'è, e dalla [0069](../decisions/0069-cosa-sa-dire-un-abbonamento.md) c'è anche il **cosa**: `DocumentChanged` porta `changes`, che dice quali aspetti e quali nomi |
| ~~21.2 moduli Suite che si parlano, 24.1 vault grandi~~ | ~~abbonamento a grana fine~~ | **chiuso ([decisione 0033](../decisions/0033-la-grana-di-un-abbonamento.md))**: `EventMask` non è più `list<EventKind>` — porta `kinds`, `topics` (prefissi che si spezzano sui separatori del contratto, non sui caratteri) e `subjects` (documento o cartella), i tre campi in AND e ognuno vuoto che vuol dire *non filtro*. A `Event::Custom` ci si abbona per prefisso di topic, non più tutto o niente. I campi sono **quattro** dalla [0069](../decisions/0069-cosa-sa-dire-un-abbonamento.md), che ha aggiunto `changes` — *cosa* è cambiato nel documento, per aspetto — e ha chiuso la [seduta 22](22-cosa-sa-dire-un-abbonamento.md) insieme alla [0063](../decisions/0063-la-maschera-e-dell-esemplare.md). Il *quando* nella maschera non c'è, e non per dimenticanza: una maschera filtra e non causa, quindi la sveglia si dichiara nel manifest |
| ~~16.2 trigger su intervallo, 16.3 schedule e delay, 18.1 sync periodico, 24.2 background sync~~ | ~~un abbonamento che dica **quando**~~ | **chiuso ([decisione 0069](../decisions/0069-cosa-sa-dire-un-abbonamento.md))**: un componente dichiara le proprie sveglie nel manifest (`plugin-manifest.timers`), il kernel le espone e rifiuta di far suonare ciò che nessuno ha dichiarato, e il pool dei job — che i thread ce li ha dalla [0032](../decisions/0032-il-runner-dei-job.md) — dorme fino alla prima invece che senza scadenza. La capacità resta esclusa e la [0013](../decisions/0013-elenco-delle-capacita.md) aveva ragione, ma per l'**altra** sua regola: una sveglia informa, quindi è un evento (`Event::TimerFired`). Resta l'**orario di parete** — «alle 9», che vuole un fuso e una regola sull'ora legale —, ed è la **[§22.4](22-cosa-sa-dire-un-abbonamento.md#224-un-orario-di-parete-non-è-un-intervallo)** |
| ~~16.2 trigger su orario e su data, 10.5 promemoria a scadenza~~ | ~~una sveglia a un **orario di parete**~~ | **chiuso ([decisione 0091](../decisions/0091-un-orario-di-parete-non-e-un-intervallo.md))**: accanto a `every` e `after`, che si misurano in tempo trascorso, `timer-schedule` ha `at-wall-clock`, che si misura in **ora civile** (`abi/traits.rs`). Il fuso viene dalla macchina perché `locale.timezone` c'era già; l'ora legale **non** ha voluto un campo; e la regola che il contratto non sa calcolare è nata *accanto* a `nth_after` e non al suo posto. Resta una casella: recuperare una sveglia di parete attraverso un **riavvio**, perché lo scheduler non persiste dove è arrivato |
| ~~5.2 (~50 estensioni), 6.2 per nota/cartella~~ | ~~opzioni di parse aperte~~ | **chiuso ([decisione 0017](../decisions/0017-chi-disegna-cio-che-il-core-non-conosce.md))**: `ParseContext { doc_id, options: OptionMap }` (`abi/format.rs`) — non più N booleani ma una mappa con namespace, e ciò che scade col freeze non è la sua larghezza ma la **forma** |
| ~~12 canvas, 11.4 CSV/JSON, 13.2 PDF, 2.3 encoding~~ | ~~documenti non-testo~~ | **chiuso ([decisione 0017](../decisions/0017-chi-disegna-cio-che-il-core-non-conosce.md) + [0087](../decisions/0087-il-testo-che-sta-dentro-gli-allegati.md))**: questa riga ha detto `parse(source: &str)` per **settanta verbali** dopo che `parse` prendeva un `document-source`, e lo diceva **in contraddizione con la riga qui sotto**, che nella stessa tabella dichiarava che `DocumentSource::Bytes` fa entrare i non-testo. Il parser sapeva ricevere byte dalla 0017; a non saperlo era il **tragitto**, e in due punti che nessuno aveva scritto qui: chi indicizza non consultava `FormatDescriptor::source` e leggeva testo comunque — quindi un documento a byte veniva scartato all'apertura del vault, con un «non è UTF-8» su un file che nessuno aveva promesso fosse testo — e il confine dei plugin sapeva dire solo `read_document -> String`. Adesso il descrittore si consulta in un posto solo (`DocumentStore::source_from_disk`), l'impronta si prende sui byte con `Revision::of_bytes` (che **è** `of`, o rivendicare un documento a byte costerebbe una riscansione), e `read-document-bytes` sta accanto a `read-document` sotto lo stesso permesso. Il testo **dentro** un PDF resta lavoro di provider (FEATURES §9.1): questa riga era il canale, non l'estrattore |
| 12.6 lavagna, 12.7 inchiostro sulle note, 13.3 annotazioni | un'isola d'inchiostro ancorata a un **punto** del documento | il **formato** non è il problema: un `.canvas` diventa `Document` il giorno che qualcuno rivendica l'estensione — `rules/media.rs` lo usa come esempio canonico — e `DocumentSource::Bytes` fa entrare anche i non-testo ([0017](../decisions/0017-chi-disegna-cio-che-il-core-non-conosce.md)). Il **dove** adesso c'è: `DocPosition { span, anchor, revision }` attraversa il canale dati ([0049](../decisions/0049-una-posizione-dentro-un-documento.md)) — prima uno span con la revisione su cui era stato calcolato ce l'aveva il solo `EditRequest` ([0008](../decisions/0008-modifica-chirurgica.md)). Resta da decidere come un'isola d'inchiostro si **attacca** a quel punto e dove vive il suo dato. La superficie giusta esiste già ed è `PaneMode::Reading` ([0007](../decisions/0007-contesto-di-sessione.md)), che è l'unico modo in cui `has_caret()` è falso |
| ~~27.1 CLI, 27.2 API locale, 26.2-26.3 mobile/PWA, 27.4 e2e~~ | ~~un montaggio riusabile~~ | **chiuso ([decisione 0023](../decisions/0023-chi-monta-il-kernel.md))**: il composition root era dentro `#[tauri::command] open_vault` e adesso è il crate `fub-host` (`host/mount.rs`), che non dipende da tauri — i cinque clienti previsti lo prendono senza prendersi un webview, e un test lo pretende (`dependency_invariant.rs`) |
| ~~3.2 nascosti visibili su richiesta~~ · 3.1 ignore, 9.1, 18.1, 23.2 esclusioni | politica di esclusione come dato | **chiuso a metà ([decisione 0110](../decisions/0110-la-struttura-non-e-una-preferenza.md))**: `IGNORED_DIRS` non è più una costante, ed è diventato **due** cose invece di una lista sola — `files.excluded-folders` e `files.show-hidden`, dichiarate per-vault ([decisione 0036](../decisions/0036-le-impostazioni-e-i-tre-stati.md)), e accanto la **struttura** (`.fub/`, `.trash/`, il temporaneo di una scrittura) che nessuna impostazione rivela, perché mostrarla è indicizzare l'indice e riesumare il cestino. Le altre quattro esclusioni ereditano `IgnorePolicy` come valutatore parametrico e la regola con cui si compongono: **per sottrazione**, e nessuna ridefinisce la struttura. Il `.gitignore` (3.1) resta: un **file** come sorgente di politica ha una sintassi propria e un proprietario che non è Fub |
| 9.2 query builder/explain, 9.1 operatori e faccette | la query come albero nel contratto | c'è (0019): resta la **superficie** — un builder nella shell, e un explain plan che attraversi il confine quando il profiler avrà dei clienti |
| ~~28 settings, 8.2 proprietà, 11.3 editing, 19.3 form~~ | ~~riconciliazione e **chiave** dei nodi~~ | **chiuso ([decisione 0016](../decisions/0016-cosa-e-una-view.md))**: `mountView` disegna la prima volta e dalla seconda **riconcilia** (`riconcilia`/`patchTree`, `ui/node.ts`), accoppiando i nodi per chiave invece di rifare l'albero — l'`innerHTML = ""` che questa riga citava sopravvive solo nel commento che racconta com'era. Un input non perde più il fuoco a ogni ridisegno |
| 3.3 workspace salvabili, 8.3 viste salvate, 20.1 settings | tre stati distinti (settings/vista/layout) | **due dei tre ce l'hanno**: le impostazioni hanno schema, due livelli e un canale per leggerle ([decisione 0036](../decisions/0036-le-impostazioni-e-i-tre-stati.md)); lo stato di vista ha due famiglie di capacità e un file della macchina che il kernel possiede, con la chiave composta dall'host — proprietario ed esemplare, non un `PaneId` ([decisione 0037](../decisions/0037-lo-stato-di-vista.md)). Il terzo è arrivato con la [0078](../decisions/0078-i-riquadri-sono-un-fatto-della-shell.md), che ha trovato «il layout» essere **due** oggetti con due case già esistenti: *com'era aperta la finestra* non ha un nome ed è stato di vista (file della macchina, fatto), *un workspace salvato* un nome ce l'ha ed è del vault — un'impostazione ha un valore alla volta, un layout ne ha uno per nome. Resta il solo **formato** del secondo, che aspetta assetti veri |
| ~~17 import/export, 18 sync/backup, 22 AI e RAG, 19.4 publishing, 13.4 trascrizione~~ | ~~lavoro lungo che **vede** il vault~~ | **chiuso ([decisione 0027](../decisions/0027-il-lavoro-lungo-vede-il-vault.md))**: `Plugin::run_job(&self, job, payload, host: &mut dyn HostApi)` — l'host arriva **per chiamata** e non come snapshot, e chi salva aspetta 0,6 µs invece di tutta la camminata |
| ~~5.2 (~50 estensioni), 20.1 markdown extensions, 27.3 custom blocks~~ | ~~innestare sintassi su un formato esistente~~ | **chiuso ([decisione 0017](../decisions/0017-chi-disegna-cio-che-il-core-non-conosce.md))**: l'innesto non passa dal registro dei formati ma da un registro suo — le `SyntaxRule` di `kernel/syntax.rs`, con un id namespaced e un `SyntaxConflict::DuplicateId` che rifiuta il doppione invece di lasciarlo vincere. `FormatRegistry::register` non sovrascrive più in silenzio: sostituire è un gesto esplicito (`replace`), e questa riga descriveva la sostituzione come se fosse l'unica strada |
| ~~6.1 mermaid/math/chart, 5.2 callout personalizzati~~ | ~~un renderer per `Block::Custom`~~ | **chiuso ([decisione 0017](../decisions/0017-chi-disegna-cio-che-il-core-non-conosce.md))**: i `CustomRenderer` hanno un registro nel kernel (`kernel/renderer.rs`) e l'anteprima si compone da lì. Il `custom_kind == CALLOUT` di `format-markdown/src/render.rs` che questa riga citava è il kind **del core**, cioè un cliente di quel registro e non l'unico varco |
| 21.1 API condivise, 21.2 moduli che si parlano, 20.1 dipendenze/conflitti | chiamate **tipizzate** fra plugin | **quasi chiuso ([decisione 0021](../decisions/0021-il-confine.md))**: oltre a `Event::Custom` (senza risposta) e `IndexQuery::Custom` (che dalla [0019](../decisions/0019-il-canale-dati.md) ha un destinatario dichiarato, ma resta una domanda all'indice) c'è `call_service`, sotto `Capability::Services` e `fub:call-service`, con un registro namespaced nel workspace: una chiamata che **scrive**, che non presta i permessi in nessuno dei due versi e in cui una ricorsione risponde nominando il giro. Ciò che resta della riga è l'altro terzo: nessuno **dichiara** una dipendenza da un altro bundle, quindi un conflitto o un modulo mancante si scoprono al primo `Unserved` |
| ~~9.2 query engine, 22.1 vettoriale, 8.2 proprietà, 11 database, 15.1 citazioni~~ | ~~routing delle query verso gli indici~~ | **chiuso ([decisione 0019](../decisions/0019-il-canale-dati.md))**: chi serve cosa è **dichiarato alla registrazione** (`QueryRoute`, `index/routing.rs`), non scoperto interpellando in ordine finché uno non dice `BadArgs`; un conflitto si vede al montaggio, e ciò che nessuno serve torna `PluginError::Unserved` invece dell'errore dell'ultimo interpellato |
| ~~2.2 UUID, 8.3 Zettelkasten ID, 10.4 calendario, 25.2 collazione~~ | ~~caso, fuso orario, locale come capacità~~ | **chiuso ([decisione 0039](../decisions/0039-il-locale-e-il-caso.md))**: `HostEnv` non ha più solo l'orologio — `user_locale()` rende lingua, fuso IANA, offset in minuti, primo giorno della settimana e orologio a 12/24 ore, e `random_bytes(n)` rende l'entropia. Il locale lo **pubblica la shell** (la webview porta un ICU intero, il kernel avrebbe avuto bisogno di un database dei fusi per rispondere peggio) e il kernel lo compone con le chiavi `locale.*`; le **forme** di un'identità — UUID v4/v7, id corti — stanno in `fub_sdk::ids`, perché la capacità è l'entropia e il formato è codice di libreria |
| ~~6.3 stampa/PDF, 19.4 pubblicazione, 6.2 CSS per nota, 5.3 sanitizzazione~~ | ~~opzioni di rendering~~ | **chiuso ([decisione 0017](../decisions/0017-chi-disegna-cio-che-il-core-non-conosce.md))**: `RenderOptions { target: RenderTarget, options: OptionMap }` (`abi/format.rs`) — il bersaglio è dichiarato (schermo, stampa) e le opzioni sono la stessa mappa con namespace del `ParseContext` |
| ~~8.1 note recenti, 24.1 apertura rapida, 18.1 sync differenziale, 3.2 duplicati~~ | ~~mtime, dimensione, impronta per entry~~ | **chiuso ([decisione 0046](../decisions/0046-l-anagrafe-del-vault.md))**: l'anagrafe è durevole in `.fub/data/entries.json` e `reindex` **chiede agli indici cosa hanno già** (`IndexProvider::up_to_date`) prima di leggere e parsare. `mtime + size` basta a **saltare** e non a **credere** (la regola *racily clean* di git); l'impronta esiste come campo `Option` e a riempirla manca il job, che è la casella residua del [§14.1](14-entry-cartelle-lista.md#141-il-vault-cartella-di-progetto-include-documenti-e-asset) |
| 24.1 vault grandi, 3.3 split e finestre, 22 AI, 18 sync | letture che non si mettono in coda dietro una scrittura | **quasi chiuso ([0022](../decisions/0022-il-kernel-a-pezzi.md) + [0024](../decisions/0024-chi-legge-non-aspetta-chi-legge.md))**: cinque proprietari invece di ventiquattro campi, e un `RwLock` al posto del `Mutex` — le view che si ridisegnano vanno da 7 a 25 volte più veloci e chi salva non viene più affamato (6,4 s di attesa misurati, prima). Da quando la [0026](../decisions/0026-due-query-insieme.md) ha tolto il lock interno alla ricerca — un provider poteva rimettersi in fila da sé dentro il prestito condiviso, e il `RwLock` del kernel non lo attraversa — anche il **carico misto** scala (1,0× → 6,8× a otto thread). Dalla [0027](../decisions/0027-il-lavoro-lungo-vede-il-vault.md) anche il **lavoro lungo** può uscire dal lock: `run_job` ha l'`HostApi` e se lo prende una chiamata alla volta (chi salva aspetta 0,6 µs invece di tutta la camminata). E adesso c'è anche chi lo esegue: la [0032](../decisions/0032-il-runner-dei-job.md) chiude il **§9.3** con un pool di thread per vault che drena la coda da sé, si lascia annullare e si ferma prima che il vault chiuda |
| 20.1 UI di plugin, 21.1 moduli installabili separatamente | far entrare un renderer di terzi nella shell | **quasi chiuso ([decisione 0079](../decisions/0079-il-grafo-esce-dall-overlay.md))**: `renderUiNode` resta uno `switch` esaustivo, ma `UiKind::Custom { ns }` ha un registro suo (`ui/custom.ts`, `customRenderer`) e un `fallback` dichiarativo per chi quel `ns` non lo conosce — il grafo è uscito dall'overlay passando di lì. Il limite che resta è **dichiarato** in quel file e non è più lo `switch`: registrare un renderer è codice della shell, non di un plugin, e quel passo è M5 |
| ~~23.3 SBOM/licenze/CVE, 20.3 reproducible builds, dependency audit~~ | ~~tooling di supply chain~~ | **chiuso ([decisione 0001](../decisions/0001-supply-chain-e-sbom.md))**: la CI ha il job `supply chain` con `cargo-deny-action` su [`deny.toml`](../../deny.toml) e l'SBOM SPDX 2.3 come artefatto (`.github/workflows/ci.yml`), e gira anche nella corsa settimanale perché un advisory nuovo non aspetta il prossimo push |
| ~~10 task, 8.2 proprietà, 15.1 citazioni, 17.2 export, 22.1 chunking~~ | ~~il **modello parsato** in mano a un provider~~ | **chiuso ([decisione 0018](../decisions/0018-chi-vede-il-modello-parsato.md))**: `HostApi::read_model(id) -> Result<DocumentModel>` — pull, un documento alla volta, riparsato dal disco |
| ~~12 canvas, 11.4 CSV/JSON, 13.2 PDF, 8.3 tipi nota~~ | ~~sapere **che formato è** un documento~~ | **chiuso ([decisione 0018](../decisions/0018-chi-vede-il-modello-parsato.md))**: `HostApi::format_of(id) -> Option<DocumentFormat>` — descrittore e capacità effettive, senza toccare il disco; `none` = nessuno lo rivendica |
| 11.2-11.3 database, 10.3 task, 9.2 query, 28 settings | una view che tiene lo **stato** | mezza chiusa, e la metà giusta: `on_action` è `&mut self` dalla [decisione 0016](../decisions/0016-cosa-e-una-view.md) — un'azione può cambiare qualcosa — mentre `render_view` resta `&self`, perché disegnare è una lettura e gira sotto prestito condiviso. Lo stato che deve **sopravvivere** alla sessione non sta nella view ma nel suo posto dichiarato ([0037](../decisions/0037-lo-stato-di-vista.md)); quello che restava scoperto — lo stato **in volo** di un widget parametrico — l'ha preso la [0063](../decisions/0063-la-maschera-e-dell-esemplare.md) con `ViewProvider::interests(&ViewInstance)`, che chiude la §22.3 e lascia una casella sola: la query incorporata in una nota, che non è un esemplare di `ViewSpec` |
| ~~22 AI, 18 sync, 11.5 dashboard, 24.1 progresso~~ | ~~una view che si fa **ridisegnare**~~ | **chiuso ([decisione 0016](../decisions/0016-cosa-e-una-view.md) + [0033](../decisions/0033-la-grana-di-un-abbonamento.md))**: il **push** c'è — un provider emette (`HostApi::emit`) e chi si abbona filtra per prefisso di topic, non più tutto o niente —, e lo stato «in caricamento» è un nodo del contratto, `UiKind::Pending`, che sta lì e non in una risposta di `render_view` perché il caso normale è **parziale**: la testata c'è, la tabella arriva |
| ~~3.3 sidebar personalizzabili, 20.1 venti pannelli di plugin~~ | ~~metadati di presentazione di una view~~ | **chiuso ([0016](../decisions/0016-cosa-e-una-view.md))**: `ViewSpec` ha id, titolo, **`surface`** (non più `placement`), `params`, `follows` e `refresh` — e accanto anche `icon`, `order`, `open_by_default`, `preferred_size` e `closable` (`abi/traits.rs`), cioè esattamente i tre che questa riga elencava come mancanti più due. Il default è **chiusa**, perché una view che si apre da sola costa lo spazio di tutti quelli che non la volevano |
| ~~19.3 form, 8.2 editor proprietà, 11.3 editing~~ | ~~le azioni portano **valori**~~ | **chiuso ([decisione 0016](../decisions/0016-cosa-e-una-view.md))**: l'azione è un `ActionRef` col suo payload, e la shell manda l'azione **coi campi in vigore** del form (`ui/node.ts`). I dati non stanno più dentro l'`ActionId` — che era la codifica che ogni view aveva reinventato per conto suo |
| ~~20.1 conflitti plugin, 21.2 moduli che convivono~~ | ~~una regola sugli **spazi di nomi**~~ | **chiuso ([decisione 0021](../decisions/0021-il-confine.md))**: il secondo **non è più muto** — chi registra un id già preso riceve `RegistryError::Claimed`, che nomina chi lo teneva, e ogni id passa da `rules::ids::check`, cioè dal namespace di chi lo dichiara. `view_owner` prende ancora il primo che combacia (`providers.rs`) e va bene così: il secondo non entra mai |
| ~~24.2 safe mode/recovery, 3.1 switch vault, 26.2-26.3~~ | ~~uno **spegnimento**~~ | **chiuso ([decisione 0028](../decisions/0028-come-un-componente-smette.md) + [0029](../decisions/0029-chiudere-un-vault-e-chiuderli-tutti.md) + [0031](../decisions/0031-chi-possiede-i-bundle.md))**: `IndexProvider::close` è **obbligatoria** (un `Drop` non ha l'`HostApi`), `deactivate` ha il suo chiamante in `host/registry.rs` e lo riceve mentre è ancora intero, e chi chiude sa di chiudere — `Host::close` fa il giro su ogni vault aperto sull'evento `Exit` (`app/lib.rs`), non più solo dal watcher |
| 7.1 link markdown/relativi, 7.2 link rotti, 13.1 riferimenti allegati | un grafo che risolve **tutti** i link, **e** un clic che ci arrivi | **quasi chiuso ([decisione 0004](../decisions/0004-il-grafo-e-i-link-non-wiki.md))**, e questa riga ha detto «`LinkGraph` scarta ogni `LinkTarget` che non sia `Wiki`» per **ottantasei verbali** dopo che il `RefKind::Path` era entrato: `register_links` risolve il path contro la cartella del sorgente (`graph.rs`), la riscrittura al rename ri-basa anche i link **del documento che si è mosso**, e le immagini non divergono più — `push_link` emette il link **e** lo registra, che erano «due gesti che non devono poter divergere». Resta il **clic**: `render.rs` emette `<a class="internal-path" data-path=… href="#">` e nel `frontend/src` la stringa `internal-path` non compare — l'anteprima aggancia il solo `a.wikilink` (`panels/preview.ts`), quindi un `[testo](nota.md)` è un arco vero nel grafo, si riscrive al rename, e cliccarlo non fa niente |
| ~~7.1 redirect da note rinominate e stable note ID, 2.2 UUID per nota, 8.3 id Zettelkasten~~ | ~~chiedere **cosa nomina** un riferimento, e una seconda chiave se serve~~ | **chiuso ([decisione 0043](../decisions/0043-il-path-e-la-chiave.md))**: il path è la chiave **per sempre** — un id stabile dentro il file è una **proprietà** (già esprimibile), fuori dal file non sopravvive alla rinomina fatta ad app chiusa. Ciò che mancava era la domanda: `IndexQuery::Resolve { target, from }` → `Resolved(option<doc-id>)`, kernel-owned, che toglie di mezzo `resolve_link` — l'ultimo comando IPC con cui la shell sapeva del vault qualcosa che un plugin non poteva chiedere. Il redirect è la feature che si innesta sul `None` |
| ~~24.1 vault enormi~~ · 2.1 corruption detection, 24.2 repair | apertura **a fasi** (la tolleranza per-documento è fatta) | **chiuso a metà ([decisione 0068](../decisions/0068-un-vault-si-apre-per-quel-che-si-legge.md) + [0070](../decisions/0070-un-vault-si-apre-in-due-tempi.md))**: una nota che non si legge o che non parsa non chiude più il vault — finisce fra gli scarti di un'`Apertura` e esce come `Event::Trouble`. E la **forma** è arrivata con la [0070](../decisions/0070-un-vault-si-apre-in-due-tempi.md), che ha chiuso il [§15.7](15-il-disco.md): l'apertura si taglia in due sulla **scansione** — la stessa riga con cui la 0068 aveva separato il fatale dal tollerato — e la seconda metà è un job, che si racconta e si lascia interrompere (`Apertura::interrotta`, `kernel/workspace.rs`). Di questa riga resta fuori la sola **corruption detection**, che è del 24.2 |
| ~~13.3 annotazioni, 10 task, 11 database, 4.3 commenti~~ | ~~stato **per-documento** migrato dal kernel~~ | **chiuso ([decisione 0044](../decisions/0044-lo-stato-per-documento.md))**: `fub_abi::rules::doc_data` dichiara **dove** sta ciò che è attaccato a una nota (`doc/<documento>/<nome>` dentro lo spazio dati di un plugin), e il kernel lo migra al rename — anche per i plugin **spenti**, camminando il disco — e lo raccoglie con un giro all'apertura quando la nota non è più né nel vault né nel cestino. Non una capacità nuova: un posto dentro `data_*`, che c'era già. Ciò che deve sopravvivere al documento (i tombstone del versioning) sta fuori da lì, per regola scritta |
| ~~20.1 pannello plugin, 28 settings, 24.2 diagnostica~~ | ~~inventario delle feature attive~~ | **chiuso ([decisione 0021](../decisions/0021-il-confine.md))**: era `VaultInfo.versioning`, **un booleano per feature**, e adesso è `VaultInfo.plugins` (`host/records.rs`) — chi c'è, con manifest, fiducia, permessi e ciò che ha registrato |
| ~~9.1-9.2 ogni query nuova, 7.3 grafo, 8.4 collezioni~~ | ~~la query sull'**IPC**~~ | **chiuso ([decisione 0019](../decisions/0019-il-canale-dati.md))**: erano quattro comandi Tauri — `search`, `list_tags`, `graph_data`, `backlinks` — e adesso è **uno** generico (`query_index`, `app/lib.rs`), gemello di `render_view`/`view_action`: la shell ha le stesse capacità di un plugin, e una variante nuova del canale dati non vuole più un comando in più |
| ~~27.3 version compatibility, 20.1 versioning plugin~~ | ~~presidio dell'additività del contratto~~ | **chiuso ([decisione 0002](../decisions/0002-additivita-del-contratto.md))**: `wit_conformance` confronta abi↔WIT **oggi**, ed è un'altra invariante; accanto c'è `crates/fub-abi/tests/wit_additivity.rs`, che confronta il contratto attuale con le **copie congelate** di `wit/frozen/` — una variante rimossa o un campo rinominato non cambiano la minor, quindi `abi_compatible` da solo accetterebbe il plugin e lascerebbe rompere il confine a valle. La [0102](../decisions/0102-i-byte-non-stanno-nel-record.md) gli ha insegnato che su un'interfaccia **esportata** una funzione nuova non è un'aggiunta ma un'obbligazione |
| ~~22.4 centro di comando LLM, 27.1 CLI, 27.2 API locale, 16.2 automazioni~~ | ~~comandi descritti a una **macchina**~~ | **chiuso ([decisione 0009](../decisions/0009-registro-dei-comandi.md) + [decisione 0010](../decisions/0010-comando-descritto-a-una-macchina.md))**: `CommandSpec { id, title, description, keybinding, params, scope }`, e l'host convalida gli argomenti contro la spec prima di chiamare il comando |
| ~~22.4 anteprima del piano, 7.2 bulk fix, 17.3 rollback, 16.3 undo~~ | ~~invocare **senza applicare** (dry-run)~~ | **chiuso ([decisione 0010](../decisions/0010-comando-descritto-a-una-macchina.md))**: `invoke(…, InvokeMode::DryRun)` → `CommandPlan` (i `DocId` impattati e un `EditRequest` per documento), con l'host che presta un `HostApi` in sola lettura — il non-scrivere è garantito, non promesso |
| 22.4 approvazione per operazione, 20.3 sandbox, 23.1 | il **consenso** dell'utente distinto dal permesso | il giro dry-run→piano→apply c'è ([decisione 0010](../decisions/0010-comando-descritto-a-una-macchina.md)) e la shell lo usa; ciò che manca è chi lo **impone** a un chiamante che non vuole simulare — è una policy sopra la firma, e adesso ha dove stare: il `Policy` della [decisione 0021](../decisions/0021-il-confine.md) |
| ~~7.2 bulk fix, 17.3 import, 11.3 editing bulk, 24.1 progresso~~ | ~~N scritture che sono **una cosa sola**~~ | **chiuso ([decisione 0011](../decisions/0011-il-lotto.md))**: `Workspace::batch(\|ws\| …)` coalizza `index-updated` e chiude con `Event::BatchEnded { batch, changed }` — una rinomina con 200 backlink passa da 201 ridisegni completi a 1, e gli eventi per-documento passano tutti. Non è una transazione, e il tutto-o-niente è dell'altro strato: adesso c'è il registro delle mutazioni ([0067](../decisions/0067-il-registro-di-cio-che-e-successo.md)), che di ogni operazione tiene i confini (la chiave di lotto) e l'inverso — ciò che manca è **chi lo ripercorre**, non l'informazione |
| ~~16.2 trigger su-modifica, 18 sync, 19.2 collaborazione~~ | ~~un evento che dice **chi lo ha causato**~~ | **chiuso ([decisione 0012](../decisions/0012-origine-degli-eventi.md))**: `handle` riceve un `Notice { event, origin }` con `Origin { actor, batch }`; `Actor::is_plugin(id)` è come un'automazione che scrive evita di richiamarsi da sola — prima l'unica difesa era il `DISPATCH_BUDGET` che tronca |
| ~~7.3 grafo, 8.2 proprietà, 7.2 salute del vault, 10 task, 15.1 citazioni~~ | ~~un canale dati che un provider possa **servire**~~ | **chiuso ([decisione 0019](../decisions/0019-il-canale-dati.md))**: le risposte del kernel sono un `IndexProvider` **registrato per primo** (`CoreIndex`, `index/core.rs`), non un `return` prima del ciclo — le sette varianti su nove che nessuno poteva servire adesso si sostituiscono chiedendole per nome |
| ~~9.2 query di terzi, 22.1 indici semantici, 11 colonne, 21.2~~ | ~~le regole del contratto raggiungibili da chi lo implementa~~ | **chiuso ([decisione 0020](../decisions/0020-le-regole-in-un-posto-solo.md))**: `fub_abi::rules` — `path`, `properties`, `tag`, `health` — e il kernel le usa da lì come chiunque altro. Ci è salita anche la coda di ogni risposta a `Documents` (`properties::finish`), che il §6.1 non nominava |
| ~~ogni capacità futura, 20.3 permessi, M5 (proxy WASM)~~ | ~~**una** implementazione di `HostApi` più le politiche~~ | **chiuso ([decisione 0021](../decisions/0021-il-confine.md))**: `HostApi` è un'impl in blocco sulle famiglie (`abi/traits.rs`), e delle tre scritte a mano nel kernel una sola *fa* qualcosa (`KernelHost`, `host/kernel.rs`); `ReadHost` (`host/read.rs`) non è un host mutilato ma un tipo che soddisfa `ReadApi` e **non** `HostApi`, e il rifiuto è scritto una volta sola in `Guard<H, P: Policy>` (`host/guard.rs`). Resta a mano `MemoryHost` (`sdk/src/testing/mod.rs`), che è il banco di prova del lato provider ([0054](../decisions/0054-il-banco-del-lato-provider.md)) |
| 18.1 esclusioni dal sync, 18.2 backup, 24.2 rebuild, 2.2 portabilità | sapere se un dato persistito è derivato o autorevole | `data_write` non lo chiede, e sotto `.fub/data/` stanno entrambe le specie. La **forma** è decisa ([0048](../decisions/0048-una-radice-sola.md)): due radici per plugin — `data_*` resta l'autorevole, una famiglia `cache_*` porta il derivato —, additiva e quindi dopo M3; finché non c'è, la specie di un blob resta indistinguibile |
| ~~4.2 live preview, 8.3 naming, 2.3 path policy, 25.2 collazione~~ | ~~le regole condivise fra kernel e shell in un posto solo~~ | **chiuso ([decisione 0020](../decisions/0020-le-regole-in-un-posto-solo.md))**: una fixture generata dai tipi Rust (`rules_mirror.rs` → `rules-samples.json` → `rules-mirror.test.ts`) lega ciò che resta scritto due volte, nei due versi. La duplicazione resta e smette di essere silenziosa; la collazione **non** è nell'elenco, perché i due ordini devono divergere |
| ~~21.2 venti bundle, 27.3 test utilities, 27.4~~ | ~~un banco di prova del kernel riusabile~~ | **chiuso** con la [0055](../decisions/0055-il-banco-del-lato-host.md): `crates/fub-testkit` è un **builder** sui cinque assi su cui i 35 helper differivano davvero, non una `fn vault()` — che è la forma che gli helper avevano già scartato da soli. Le nove `PlainProvider` erano **tre** comportamenti (sei su `txt`, due su `md`, una che rende `<pre>`), quindi `TestoDiProva` è parametrico: un tipo solo senza cambiare soggetto a sei test. Un test nuovo sul kernel costa **2** righe di impalcatura contro una mediana di 135 |
| ~~6.2 temi e snippet, 25.1 accessibilità, 20.1 UI di plugin~~ | ~~una shell con una struttura~~ | **chiuso**: la **struttura** c'è ([decisione 0015](../decisions/0015-la-forma-della-shell.md)): dei 14 file piatti in `frontend/src/` ne restano due (`main.ts`, sceso da 1622 righe a 248, e `style.css`), il resto è l'albero dichiarato in [shell.md](../architecture/shell.md). E il **tema a token** è arrivato con la [0042](../decisions/0042-il-catalogo-della-shell.md): i token stanno in `frontend/src/theme/tokens.css`, `style.css` li legge invece di ripetere i colori, e `theme/contrast.test.ts` pretende un rapporto di contrasto sui token invece di fidarsi dell'occhio |
| ~~9.1 ricerca, e ogni indice futuro (22.1, 10, 11, 15.1, 8.2)~~ | ~~un esito sull'alimentazione dell'indice~~ | **chiuso ([decisione 0051](../decisions/0051-l-alimentazione-risponde.md))**: i tre metodi sono a lotto e restituiscono `Vec<IndexLoss>`, che **nomina** il documento perduto. Il provider di ricerca aveva tre uscite silenziose e adesso le dice tutte e tre; questa riga ha detto il contrario per otto verbali |
| ~~10.5 notification center e alert, 24.2 error reporting, 16.3 automation errors, 18.1 errori di sync~~ | ~~una destinazione per «cosa è andato storto»~~ | **chiuso ([decisione 0052](../decisions/0052-cio-che-va-storto-e-un-evento.md))**: `Event::Trouble` porta severità, soggetto ed errore, e il centro notifiche è in ascolto. I **14** `console.warn/error` della shell sono stati convertiti dalla [decisione 0080](../decisions/0080-un-guasto-si-dice-a-chi-sta-lavorando.md) insieme all'esito del salvataggio (§20.4 chiusa: nel `frontend/src` fuori dai `.test.ts` non ne resta nessuno). E i **ventisette** `eprintln!` del backend — che questa riga contava prima 32 e poi 27 — non restano: li ha smistati la [0062](../decisions/0062-il-log-e-il-pavimento-l-evento-e-la-porta.md) col criterio *il log è il pavimento, l'evento è la porta*, e nei `crates/*/src` di produzione lo `stderr` è sceso a **zero** |
| ~~18.2 versioning, 16.2-16.3 automazioni, 20.2 log plugin~~ | ~~che qualcuno legga l'esito di un handler~~ | **chiuso ([decisione 0052](../decisions/0052-cio-che-va-storto-e-un-evento.md))**: `deliver_to_handlers` raccoglie l'esito di ogni handler e lo emette come guasto a nome suo; l'operazione che ha emesso l'evento continua a non fallire. Uno snapshot del versioning che non si scrive adesso si vede |
| ~~2.1 autosave e crash recovery, 24.2 error reporting, 3.1 vault read-only~~ | ~~uno stato di salvataggio e una superficie per un messaggio~~ | **chiuso ([decisione 0080](../decisions/0080-un-guasto-si-dice-a-chi-sta-lavorando.md))**, che ha preso il [§20.4](20-quando-qualcosa-va-storto.md): il salvataggio ha un **esito** che vive in un file suo (`state/salvataggio.ts`, `statoDi`/`esitoDelFallimento` — e `conflitto` è separato da `fallito`, perché i due si riparano in due modi che non si somigliano), la barra di stato lo mostra e il resto va al centro notifiche (`ui/notify.ts`), che è la stessa destinazione dei `console.warn/error` convertiti nello stesso giro. `saveCurrent` **non esiste più** in `frontend/src`, e questa riga lo nominava al presente. Il buffer di crash è arrivato per conto suo, con la [0088](../decisions/0088-cio-che-non-e-ancora-successo.md) |
| 2.3 modifiche esterne, 3.1 tool di sync esterni, 18.1 per-file status, 24.2 file lock | sapere se il rilevamento delle scritture altrui funziona | il watcher è l'unico rilevatore, i suoi esiti per-path sono scartati (`let _ = ws.sync_renamed_path(…)` e `let _ = ws.sync_path(…)`, `host/watcher.rs`) e nessuno chiede mai se sia vivo |
| ~~27.4 testing, 27.3 plugin linting, 20.3 permission revocation~~ | ~~presidi esaustivi per costruzione, non a memoria~~ | **chiuso ([decisione 0056](../decisions/0056-un-elenco-che-e-la-sorgente.md))**: le view non si elencano più — `fub_features::ogni_feature_ufficiale()` è l'inventario **da cui `mount.rs` registra**, quindi una view fuori di lì non è una view dimenticata dai test, è una view che non esiste nell'app, e `fub-host/tests/le_view_ufficiali.rs` lo verifica nelle due direzioni. Le capacità nemmeno: l'insieme atteso si **calcola** da `Capability::ALL` filtrando ciò che `ReadOnly` nega, e appena acceso è uscito rosso su **quattro** famiglie negate che nessuno provava (`VaultWrite`, `DataWrite`, `Events`, `Services`) — le sette del `TriesEverything` erano sette *metodi*, non sette capacità. Resta dichiarato un limite: la copertura è alla grana della **famiglia**, non del metodo |
| ~~4.2 multi-cursore e selezioni multiple, 4.3 azioni sulla selezione~~ | ~~che il contesto sappia nominare **più di un** punto d'inserimento~~ | **chiuso ([decisione 0093](../decisions/0093-le-selezioni-sono-n-e-il-buffer-e-uno.md))**: il campo non è più un `Option<Selection>` ma `selections: Option<SelectionSet>` (`abi/session.rs`), con la **primaria come campo** e le secondarie in lista, e l'ancoraggio **sopra** l'insieme perché il buffer è uno. Il multi-cursore era già acceso nell'editor (`basicSetup`): fuori mancava la sola facoltà di **dirlo**. Questa riga si dichiarava anche «l'unica P0», e dopo quel commit non ne resta aperta **nessuna** — le voci vive di [todo.md](../todo.md) sono una P2 e due P1. Resta una casella, `note.task.toggle` su N cursori, che è una decisione di firma sua |
| ~~20.3 permission revocation, 25.2 privacy, 22 AI (cosa vede un modello)~~ | ~~un permesso su **cosa l'utente sta scrivendo**, non solo su cosa è salvato~~ | **chiuso ([decisione 0095](../decisions/0095-cosa-guardo-e-cosa-sto-scrivendo.md))**: `Capability::Env` si è spaccata in **tre** — l'orologio resta senza permesso perché è della macchina, *quale nota è aperta* diventa `fub:read-session`, *il testo selezionato* `fub:read-selection` (`Capability::SessionSelection => Some(permission::READ_SELECTION)`, `host/guard.rs`; la stringa sta in `abi/options.rs`) — quindi ciò che l'utente sta scrivendo si dichiara in un manifest, si mostra e si nega. La raccomandazione che questa riga portava — appoggiare la selezione a `read-vault` «perché non inventa niente» — era **sbagliata**: avrebbe fatto sparire la scelta che la voce esisteva per dare. La rete è entrata dopo ([0097](../decisions/0097-un-recinto-che-vale-anche-quando-nessuno-guarda.md)), cioè nell'ordine che questa riga chiedeva |
| ~~17.1 migrazione da Obsidian/Notion/Evernote, 6.3 export PDF, 14.3 email~~ | ~~trasferire senza tenere in memoria ciò che si trasferisce~~ | **chiuso ([decisione 0102](../decisions/0102-i-byte-non-stanno-nel-record.md))**: il confine resta di byte e non di path ([decisione 0006](../decisions/0006-import-export-come-trait.md)) — nessuna capacità filesystem entra, in nessuno dei due versi — ma i byte non stanno più *dentro* il record: `ImportSource.content` può essere una **chiave** che l'host risolve, e un artefatto si versa in un `ArtifactSink` mentre lo si produce. La lettura è **posizionale** perché la directory di un archivio sta in fondo, quindi `.docx`/`.epub`/`.odt` si sfogliano invece di scaricarsi. Restare in memoria è ancora il caso comune: adesso è una scelta dichiarata e non l'unica strada |
| ~~8.2 proprietà tipizzate, 10.4 raggruppamento per giorno, 11 database~~ | ~~un modo per **dichiarare** come sono scritte le date di questo vault~~ | **chiuso ([decisione 0108](../decisions/0108-una-data-la-dichiara-chi-possiede-il-vault.md))**: solo l'ISO-8601 a larghezza fissa è una data ([decisione 0003](../decisions/0003-modello-del-documento.md)) e la regola **non cambia**, perché ciò che serviva non era un parser più tollerante ma **qualcuno che dichiarasse il formato**. Un vault dichiara il proprio ordine dei campi con `properties.date-format` (`dmy`/`mdy`/`ymd`, default *solo ISO*), l'ISO si legge sempre e per primo, e la dichiarazione **aggiunge** una lettura senza cambiarne nessuna. La chiave **non** è della famiglia `locale.*`: là il sistema ha una risposta, qui la risposta del sistema sarebbe sbagliata per costruzione — `05/07/2026` su due macchine è due giorni diversi, e il formato è un fatto dei **file**. Ciò che il formato non copre non resta muto: `HealthCheck::UnrecognizedDates` elenca le proprietà che sembrano date e non lo sono, col rilevatore che è **lo stesso parser con tutti gli ordini insieme**. Resta fuori una casella: i **nomi dei mesi**, che sarebbero una tabella per lingua e non un ordine di campi |
| ~~18.1 sync, 3.1 vault su filesystem diversi~~ | ~~che due file davvero distinti restino due archi~~ | **chiuso ([decisione 0107](../decisions/0107-il-caso-di-una-lettera.md))**: `resolution_key` ([`abi/rules/path.rs:49`](../../crates/fub-abi/src/rules/path.rs)) minuscola tutta la chiave ([decisione 0004](../decisions/0004-il-grafo-e-i-link-non-wiki.md)) perché il vault sincronizzato fra macOS e Linux sia lo stesso vault, e **non cambia di una riga**: quella ragione regge per intero. Accanto le è nata `exact_key` ([`abi/rules/path.rs:66`](../../crates/fub-abi/src/rules/path.rs)) — trim e NFC, senza il minuscolo — e la differenza sta in una riga: la prima dice **chi è candidato**, la seconda **chi ha ragione fra i candidati**, dove prima sceglieva `first()`, cioè l'ordine ASCII. Gli indici restano per chiave, quindi l'invalidazione incrementale non si muove. Dove nemmeno la chiave esatta può decidere — due file nella **radice** del vault, perché `resolve_key` consulta l'indice dei path solo se la chiave contiene `/`, e lì non esiste nessun wikilink che disambigui — la risposta non è una regola ma un avviso: `HealthCheck::CollidingPaths`, terzo caso in coda a un enum che ne aveva due, che cammina l'anagrafe (`foto.PNG` e `foto.png` collidono come due note) ed emette una issue per **ogni** membro del gruppo. Riparazione automatica non ce n'è, ed è una scelta: quale dei due file abbia il nome sbagliato è la domanda a cui nessuno ha una risposta |
