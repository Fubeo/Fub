# Dove il contratto si strozza, capitolo per capitolo

Una riga per **famiglia di FEATURES**: cosa servirebbe perché quelle voci siano provider, e cosa lo impedisce oggi. È l'indice inverso della roadmap — si entra dal capitolo di FEATURES invece che dalla seduta.

[← indice](../todo.md)

---

| Famiglia FEATURES | Cosa serve | Cosa manca oggi |
|---|---|---|
| 4.2 slash/hotkey, 16.2 automazioni, 27.1 CLI, 20.1 comandi plugin | `CommandProvider` vivo | il trait esiste (`abi/traits.rs`) ma **nessuno lo registra**: `Workspace` non ha una tabella comandi |
| 8.2 proprietà, 11 database, 16.1 template, 19.3 form, 28 settings | `UiNode` con **input** | 8 varianti sole, tutte in sola lettura: nessun campo, select, checkbox, tabella, albero |
| ~~20.1 impostazioni plugin, 28 config/profili, D7 spegnibilità~~ | ~~store di configurazione + schema dichiarativo~~ | **chiuso ([decisione 0036](../decisions/0036-le-impostazioni-e-i-tre-stati.md))**: lo schema lo dichiara il **manifest** (`PluginManifest.settings`, prima di `activate`, con i nomi passati a `rules::ids::check` come i servizi), il valore vive in **due** livelli con una precedenza sola — vault (`.fubmd/settings.json`) sopra macchina (`fubmd_host::config_dir`) sopra il default dello schema — e `SettingSource` dice quale ha vinto. `FUBMD_VERSIONING` non c'è più: è `versioning.enabled`, e accanto le sta `plugins.disabled`, che è l'interruttore dell'host. Di variabili d'ambiente ne restano **due, per decisione**: `FUBMD_VAULT` non è una preferenza che dura ma un **argomento di avvio** (il gemello del `fubmd <path>` della CLI 27.1), e `FUBMD_CONFIG_DIR` è il **bootstrap** — dove stanno le impostazioni è l'unica cosa che non può essere un'impostazione |
| 13 allegati, 12 canvas, 11.4 CSV/JSON, 6.1 media | entry di vault che non sono documenti | `Vault::list_documents` filtra per estensione dei `FormatProvider` (`vault.rs`): un PNG **non esiste** |
| 18 sync, 23.1 cifratura at-rest, 26.3 PWA/OPFS, 3.1 vault read-only | astrazione sullo storage | `std::fs` diretto in `vault.rs` e in `workspace.rs` (storage plugin) |
| 10 task, 5.2 id di blocco, 7.1 link a blocco | modello con task e ancore stabili | **il modello ce l'ha** dalla [0003](../decisions/0003-modello-del-documento.md): `ListItem.task` porta lo stato di spunta insieme al marcatore, ogni blocco porta un `anchor`, esiste `Anchor { id, span, marker }` con `canonical_anchor`/`valid_anchor` in `rules/`, e `LinkTarget::Wiki` porta il `block`. Quello che non arriva è il **link**: chi risolve scarta il campo perché `IndexResult::Resolved(option<doc-id>)` non ha dove metterlo (§21.10). Questa riga ha detto «nessun `^block-id`» per undici verbali dopo che l'ancora era entrata — un indice inverso vecchio non allunga il lavoro, lo **dirotta** |
| 9.1 faceted/field search, 9.2 query engine, 8.4 collezioni | canale query su **proprietà** | c'è (0005) e adesso si **compone** col testo (0019): resta da disegnare chi lo usa — il builder e le viste salvate |
| 9.1 ricerca predefinita (refusi, prefisso), 8.4 e 16.1 che ne riusano il canale | una query che dica **quanto** si vuole essere indovinati | `TextMode` ha due varianti, `Terms` e `Phrase` (`abi/query.rs`): la tolleranza non è dicibile, e quindi **nemmeno l'esattezza** — il giorno che il provider indovina, indovina anche per `vault.replace` e per le collezioni. È la [0025](../decisions/0025-la-ricerca-predefinita.md), §21.1–§21.2, e scade col freeze |
| 9.1 ricerca nella nota aperta, salto all'occorrenza, occorrenze multiple per nota | un estratto ancorato al **documento**, non allo snippet | `DocumentMatch.highlights` sono span in byte *dentro* `snippet` (`abi/traits.rs`) e `absorb` ne tiene uno solo per nota: `ViewUpdate::Reveal { doc_id, span }` esiste, la shell lo sa eseguire, e la ricerca non ha coordinate da passargli (§21.3) |
| 8.1 quick switcher, 4.2 palette, 9.1 casella di ricerca | una porta sola verso l'indice | il quick switcher non esiste ancora: nascendo da sé nascerebbe su `list_documents` con un confronto di sottostringhe, cioè una seconda ricerca peggiore della prima sulla strada più battuta dell'app (§21.5) |
| 17 import/export/migration (~120 voci) | trait `ImportProvider`/`ExportProvider` | non esistono: ogni formato sarebbe codice nell'app |
| 24.1 task manager, 22 AI, 14.2 clipper | lavoro in background reale | `take_pending_jobs` (`dispatcher.rs`) **non ha chiamanti fuori dai test**: `spawn_job` in produzione non esegue nulla |
| ~~3.1 vault multipli, 3.3 finestre multiple, 26 piattaforme~~ | ~~sessioni multiple~~ | **chiuso ([decisione 0029](../decisions/0029-chiudere-un-vault-e-chiuderli-tutti.md) + [0036](../decisions/0036-le-impostazioni-e-i-tre-stati.md))**: `Host` teneva **una** `Option<VaultSession>` e aprire un vault chiudeva quello aperto; adesso i vault aperti sono una mappa (`host/session.rs`), ogni comando IPC accetta un `vault` opzionale e il «corrente» è tornato a essere una comodità della shell. La metà che la 0029 non poteva chiudere — *dove si scrive l'elenco dei vault fra un avvio e l'altro*, perché un elenco di vault non sta in nessun vault — l'ha chiusa la 0036 col livello macchina (`vaults.json`, recenti/preferiti/icone, `host/vaults.rs`) |
| 5.3 sicurezza markdown, 23 privacy | sanitizzazione e CSP in un punto | il sanitizer c'è ed è un punto solo (`setSanitizedHtml`/`sanitizeFragment`, `ui/sanitize.ts`), e ciò che arriva da un provider non diventa mai markup; resta la **CSP**: nessuna policy sui contenuti remoti |
| 25 accessibilità e localizzazione | tema a token + catalogo stringhe | stringhe italiane cablate **anche dentro i provider** (le view producono testo di UI) |
| 2.1 recovery/journaling, 24.2 affidabilità | scrittura durevole | `Vault::write` è `std::fs::write` (`vault.rs`): niente temp+rename, niente fsync |
| ~~3.3 split/finestre, 4.2-4.3 azioni sulla selezione, 13.3, 22.2~~ | ~~contesto per-pane e **selezione** nel contratto~~ | **chiuso ([decisione 0007](../decisions/0007-contesto-di-sessione.md))**: `HostApi::active_context() -> Option<ViewContext>` con pannello, documento, selezione e modalità |
| 7.2 bulk fix, 11.3 editing bulk, 16.3 undo, 17.3 rollback | scrittura **a lotti** | il kernel muta un documento alla volta: N scritture = N eventi (`workspace.rs`) |
| ~~4.2 undo per sessione, 3.3 undo toast, 11.3 undo database, 16.3 undo automazioni~~ | ~~un'operazione che dichiara come si torna indietro~~ | **chiuso ([decisione 0045](../decisions/0045-l-undo-ha-due-pile.md))**: `CommandOutcome.undo: option<undo>` con `undo-step` che è **una modifica** (l'inverso della 0008) **o un comando** — perché le operazioni un nome ce l'hanno già, e annullare una rinomina è una rinomina all'incontrario. La pila è del kernel e dura quanto il vault aperto; `host-commands.undo-last` la consuma, `vault.undo` la invoca. Le due pile — testo nell'editor, operazioni nel kernel — non si fondono, e a decidere quale risponde è il fuoco |
| 3.2 cartelle, 8.2 metadata di cartella, 6.2 CSS per cartella | la cartella come cittadino del kernel | `metas` è una mappa piatta (`index/core.rs`): l'albero esiste solo in `organizer.ts` |
| 20.1 enable/disable, 20.2 hot reload, 24.2 safe mode | disattivare un provider | `register_*` fa solo `push`: `unregister` non esiste |
| ~~20.3 sandbox e permessi, 23.1 permessi file/rete~~ | ~~un punto di applicazione dei permessi~~ | **chiuso ([decisione 0021](../decisions/0021-il-confine.md))**: chi registra si dichiara prima (`register_plugin`), e da quel manifest nasce un `Guard<H, P: Policy>` che accende una famiglia di capacità solo se il permesso c'è (`Granted::new`, `host/guard.rs`); un id che nessuno ha dichiarato nega tutto dicendo perché. Restano senza un punto da presidiare i permessi che **non hanno una capacità**: `fubmd:network`, `fubmd:external-fs`, `fubmd:clipboard`, `fubmd:camera`, `fubmd:microphone` — la forma c'è, il confine che aprirebbero non esiste ancora |
| ~~6.1 anteprima interattiva, 5.3 sanitizzazione~~ | ~~il **modello** al confine, non solo HTML~~ | **chiuso ([decisione 0018](../decisions/0018-chi-vede-il-modello-parsato.md))**: il modello attraversa il contratto (`read_model`) ma **non** l'IPC verso il webview — il modello è del file, la webview lavora sul buffer; `render_preview` resta la fast-path, con le parti dichiarative della [0017](../decisions/0017-chi-disegna-cio-che-il-core-non-conosce.md) |
| 24.2 error reporting, 25.2 localizzazione, 16.3 retry | errori tipizzati al confine | i comandi IPC restituiscono `Result<_, String>`: la shell indovina (`host/ipc.ts`) |
| 27.3 test utilities, 21.1 moduli Suite | un SDK usabile da fuori | `MemoryHost` è `#[cfg(test)]` dentro `fubmd-features` (`features/src/lib.rs`) |
| 2.2 config, 27.4 upgrade migration | versione di schema sui formati persistiti | ce l'ha il solo indice di ricerca (`search.rs`), che è **derivato** |
| 20.1 ribbon/menu, 11-12 database e canvas, 7.3 grafo | superfici di UI che la **shell** ospiti | il contratto ne nomina **dieci** (`ViewSurface`, `abi/traits.rs`, dalla [0016](../decisions/0016-cosa-e-una-view.md)) e la shell ne ospita **sette**: la barra di stato è arrivata con la [0035](../decisions/0035-il-lavoro-lungo-si-racconta.md), la scheda di impostazioni con la [0036](../decisions/0036-le-impostazioni-e-i-tre-stati.md). Le tre che restano non sono un buco del contratto ma della shell, e ognuna dice il perché a chi dichiara una view lì (`NON_OSPITATE`, `ui/views.ts`): `main` vuole il modello di layout (§1.2), `menu` e `context_menu` vogliono superfici che questa shell non ha |
| 11.2 viste multiple, 8.3 viste salvate, 9.2 query embed, 3.3 split | view **istanziabili** con parametri | `views()` è un elenco statico e `view_owner` risolve per id (`providers.rs`) |
| ~~4.3, 7.2, 8.2, 10.1, 11.3, 16.1, 19.2, 22.2~~ | ~~modificare **un pezzo** di documento~~ | **chiuso ([decisione 0008](../decisions/0008-modifica-chirurgica.md))**: `HostApi::apply_edit(id, EditRequest { base, edits })`, con la revisione nella firma e `Conflict` invece della sovrascrittura silenziosa |
| 4.2 undo illimitato, 11.3, 16.3, 17.3 rollback, 3.3 undo toast | un proprietario dell'undo | vive solo in CodeMirror, su **un** `EditorView` riusato per tutte le note |
| 16.2 trigger, 18 sync, 19.2 collaborazione | origine e causalità sugli eventi | `DocumentChanged { id }` non dice chi ha scritto: la shell indovina (`panels/document.ts`) |
| 21.2 moduli Suite che si parlano, 24.1 vault grandi | abbonamento a grana fine | maschera su 8 `EventKind`; a `Event::Custom` ci si abbona **tutto o niente** |
| 5.2 (~50 estensioni), 6.2 per nota/cartella | opzioni di parse aperte | `ParseContext` sono **due booleani** (`format.rs`) |
| 12 canvas, 11.4 CSV/JSON, 13.2 PDF, 2.3 encoding | documenti non-testo | `parse(source: &str)` e `Vault::read -> String`: un formato binario non entra |
| 12.6 lavagna, 12.7 inchiostro sulle note, 13.3 annotazioni | un'isola d'inchiostro ancorata a un **punto** del documento | il **formato** non è il problema: un `.canvas` diventa `Document` il giorno che qualcuno rivendica l'estensione — `rules/media.rs` lo usa come esempio canonico — e `DocumentSource::Bytes` fa entrare anche i non-testo ([0017](../decisions/0017-chi-disegna-cio-che-il-core-non-conosce.md)). Manca **dove** si àncora: uno span più la revisione su cui è stato calcolato ce l'ha il solo `EditRequest` ([0008](../decisions/0008-modifica-chirurgica.md)), e una posizione che attraversi il canale dati non c'è (§21.10, §21.3). La superficie giusta esiste già ed è `PaneMode::Reading` ([0007](../decisions/0007-contesto-di-sessione.md)), che è l'unico modo in cui `has_caret()` è falso |
| ~~27.1 CLI, 27.2 API locale, 26.2-26.3 mobile/PWA, 27.4 e2e~~ | ~~un montaggio riusabile~~ | **chiuso ([decisione 0023](../decisions/0023-chi-monta-il-kernel.md))**: il composition root era dentro `#[tauri::command] open_vault` e adesso è il crate `fubmd-host` (`host/mount.rs`), che non dipende da tauri — i cinque clienti previsti lo prendono senza prendersi un webview, e un test lo pretende (`dependency_invariant.rs`) |
| 3.1 ignore, 3.2 nascosti, 9.1, 18.1, 23.2 esclusioni | politica di esclusione come dato | `IGNORED_DIRS` è una costante di compilazione (`vault.rs`) |
| 9.2 query builder/explain, 9.1 operatori e faccette | la query come albero nel contratto | c'è (0019): resta la **superficie** — un builder nella shell, e un explain plan che attraversi il confine quando il profiler avrà dei clienti |
| 28 settings, 8.2 proprietà, 11.3 editing, 19.3 form | riconciliazione e **chiave** dei nodi | `mountView` fa `innerHTML = ""` a ogni ridisegno (`ui/node.ts`): un input perde il focus |
| 3.3 workspace salvabili, 8.3 viste salvate, 20.1 settings | tre stati distinti (settings/vista/layout) | **due dei tre ce l'hanno**: le impostazioni hanno schema, due livelli e un canale per leggerle ([decisione 0036](../decisions/0036-le-impostazioni-e-i-tre-stati.md)); lo stato di vista ha due famiglie di capacità e un file della macchina che il kernel possiede, con la chiave composta dall'host — proprietario ed esemplare, non un `PaneId` ([decisione 0037](../decisions/0037-lo-stato-di-vista.md)). Resta il **layout**, che ha più configurazioni per lo stesso utente — quindi un insieme nominato e non un valore — e che aspetta il modello di layout (§1.2): oggi l'area principale è un pannello solo, e non c'è nessuna disposizione da salvare |
| 17 import/export, 18 sync/backup, 22 AI e RAG, 19.4 publishing, 13.4 trascrizione | lavoro lungo che **vede** il vault | `Plugin::run_job` è senza `HostApi` (`abi/traits.rs`): l'input deve stare tutto nel payload |
| 5.2 (~50 estensioni), 20.1 markdown extensions, 27.3 custom blocks | innestare sintassi su un formato esistente | `FormatRegistry::by_ext` è estensione→**un** provider (`registry.rs`): si sostituisce, non si estende |
| 6.1 mermaid/math/chart, 5.2 callout personalizzati | un renderer per `Block::Custom` | è un `if custom_kind == "callout"` dentro il provider markdown (`render.rs`) |
| 21.1 API condivise, 21.2 moduli che si parlano, 20.1 dipendenze/conflitti | chiamate **tipizzate** fra plugin | solo `Event::Custom` (senza risposta) e `IndexQuery::Custom` (che dalla 0019 ha un destinatario dichiarato, ma resta una domanda all'indice) |
| ~~9.2 query engine, 22.1 vettoriale, 8.2 proprietà, 11 database, 15.1 citazioni~~ | ~~routing delle query verso gli indici~~ | **chiuso ([decisione 0019](../decisions/0019-il-canale-dati.md))**: chi serve cosa è **dichiarato alla registrazione** (`QueryRoute`, `index/routing.rs`), non scoperto interpellando in ordine finché uno non dice `BadArgs`; un conflitto si vede al montaggio, e ciò che nessuno serve torna `PluginError::Unserved` invece dell'errore dell'ultimo interpellato |
| ~~2.2 UUID, 8.3 Zettelkasten ID, 10.4 calendario, 25.2 collazione~~ | ~~caso, fuso orario, locale come capacità~~ | **chiuso ([decisione 0039](../decisions/0039-il-locale-e-il-caso.md))**: `HostEnv` non ha più solo l'orologio — `user_locale()` rende lingua, fuso IANA, offset in minuti, primo giorno della settimana e orologio a 12/24 ore, e `random_bytes(n)` rende l'entropia. Il locale lo **pubblica la shell** (la webview porta un ICU intero, il kernel avrebbe avuto bisogno di un database dei fusi per rispondere peggio) e il kernel lo compone con le chiavi `locale.*`; le **forme** di un'identità — UUID v4/v7, id corti — stanno in `fubmd_sdk::ids`, perché la capacità è l'entropia e il formato è codice di libreria |
| 6.3 stampa/PDF, 19.4 pubblicazione, 6.2 CSS per nota, 5.3 sanitizzazione | opzioni di rendering | `RenderOptions` è **un** booleano (`abi/format.rs`), ed è argomento di `render_html` |
| 8.1 note recenti, 24.1 apertura rapida, 18.1 sync differenziale, 3.2 duplicati | mtime, dimensione, impronta per entry | `DocMeta` non li ha (`index/core.rs`) e `reindex` riparsa **tutto** (`workspace.rs`) |
| 24.1 vault grandi, 3.3 split e finestre, 22 AI, 18 sync | letture che non si mettono in coda dietro una scrittura | **quasi chiuso ([0022](../decisions/0022-il-kernel-a-pezzi.md) + [0024](../decisions/0024-chi-legge-non-aspetta-chi-legge.md))**: cinque proprietari invece di ventiquattro campi, e un `RwLock` al posto del `Mutex` — le view che si ridisegnano vanno da 7 a 25 volte più veloci e chi salva non viene più affamato (6,4 s di attesa misurati, prima). Da quando la [0026](../decisions/0026-due-query-insieme.md) ha tolto il lock interno alla ricerca — un provider poteva rimettersi in fila da sé dentro il prestito condiviso, e il `RwLock` del kernel non lo attraversa — anche il **carico misto** scala (1,0× → 6,8× a otto thread). Dalla [0027](../decisions/0027-il-lavoro-lungo-vede-il-vault.md) anche il **lavoro lungo** può uscire dal lock: `run_job` ha l'`HostApi` e se lo prende una chiamata alla volta (chi salva aspetta 0,6 µs invece di tutta la camminata). E adesso c'è anche chi lo esegue: la [0032](../decisions/0032-il-runner-dei-job.md) chiude il **§9.3** con un pool di thread per vault che drena la coda da sé, si lascia annullare e si ferma prima che il vault chiuda |
| 20.1 UI di plugin, 21.1 moduli installabili separatamente | far entrare un renderer di terzi nella shell | `renderUiNode` è uno `switch` esaustivo compilato nel bundle (`ui/node.ts`) |
| 23.3 SBOM/licenze/CVE, 20.3 reproducible builds, dependency audit | tooling di supply chain | la CI non ha `cargo-deny` né generazione SBOM |
| ~~10 task, 8.2 proprietà, 15.1 citazioni, 17.2 export, 22.1 chunking~~ | ~~il **modello parsato** in mano a un provider~~ | **chiuso ([decisione 0018](../decisions/0018-chi-vede-il-modello-parsato.md))**: `HostApi::read_model(id) -> Result<DocumentModel>` — pull, un documento alla volta, riparsato dal disco |
| ~~12 canvas, 11.4 CSV/JSON, 13.2 PDF, 8.3 tipi nota~~ | ~~sapere **che formato è** un documento~~ | **chiuso ([decisione 0018](../decisions/0018-chi-vede-il-modello-parsato.md))**: `HostApi::format_of(id) -> Option<DocumentFormat>` — descrittore e capacità effettive, senza toccare il disco; `none` = nessuno lo rivendica |
| 11.2-11.3 database, 10.3 task, 9.2 query, 28 settings | una view che tiene lo **stato** | `render_view` **e** `on_action` sono `&self` (`abi/traits.rs`) |
| 22 AI, 18 sync, 11.5 dashboard, 24.1 progresso | una view che si fa **ridisegnare** | `refresh` filtra eventi del kernel; nessun push dal provider, nessuno stato "in caricamento" |
| 3.3 sidebar personalizzabili, 20.1 venti pannelli di plugin | metadati di presentazione di una view | `ViewSpec` ha id, titolo, placement, refresh (`abi/traits.rs`) — niente icona, ordine, default |
| 19.3 form, 8.2 editor proprietà, 11.3 editing | le azioni portano **valori** | `UiAction.payload` non è mai popolato (`ui/node.ts`): i dati stanno dentro l'`ActionId` |
| 20.1 conflitti plugin, 21.2 moduli che convivono | una regola sugli **spazi di nomi** | `view_owner` prende il primo id che combacia (`providers.rs`): il secondo è muto |
| 24.2 safe mode/recovery, 3.1 switch vault, 26.2-26.3 | uno **spegnimento** | `flush_indexes` ha un solo chiamante: il watcher (`host/watcher.rs`); `deactivate` non ne ha nessuno |
| 7.1 link markdown/relativi, 7.2 link rotti, 13.1 riferimenti allegati | un grafo che risolve **tutti** i link | `LinkGraph` scarta ogni `LinkTarget` che non sia `Wiki` (`graph.rs`) |
| ~~7.1 redirect da note rinominate e stable note ID, 2.2 UUID per nota, 8.3 id Zettelkasten~~ | ~~chiedere **cosa nomina** un riferimento, e una seconda chiave se serve~~ | **chiuso ([decisione 0043](../decisions/0043-il-path-e-la-chiave.md))**: il path è la chiave **per sempre** — un id stabile dentro il file è una **proprietà** (già esprimibile), fuori dal file non sopravvive alla rinomina fatta ad app chiusa. Ciò che mancava era la domanda: `IndexQuery::Resolve { target, from }` → `Resolved(option<doc-id>)`, kernel-owned, che toglie di mezzo `resolve_link` — l'ultimo comando IPC con cui la shell sapeva del vault qualcosa che un plugin non poteva chiedere. Il redirect è la feature che si innesta sul `None` |
| 24.1 vault enormi, 2.1 corruption detection, 24.2 repair | apertura a fasi e tolleranza per-documento | `reindex` è tutto-o-niente (`workspace.rs`): una nota che non parsa chiude il vault |
| ~~13.3 annotazioni, 10 task, 11 database, 4.3 commenti~~ | ~~stato **per-documento** migrato dal kernel~~ | **chiuso ([decisione 0044](../decisions/0044-lo-stato-per-documento.md))**: `fubmd_abi::rules::doc_data` dichiara **dove** sta ciò che è attaccato a una nota (`doc/<documento>/<nome>` dentro lo spazio dati di un plugin), e il kernel lo migra al rename — anche per i plugin **spenti**, camminando il disco — e lo raccoglie con un giro all'apertura quando la nota non è più né nel vault né nel cestino. Non una capacità nuova: un posto dentro `data_*`, che c'era già. Ciò che deve sopravvivere al documento (i tombstone del versioning) sta fuori da lì, per regola scritta |
| ~~20.1 pannello plugin, 28 settings, 24.2 diagnostica~~ | ~~inventario delle feature attive~~ | **chiuso ([decisione 0021](../decisions/0021-il-confine.md))**: era `VaultInfo.versioning`, **un booleano per feature**, e adesso è `VaultInfo.plugins` (`host/records.rs`) — chi c'è, con manifest, fiducia, permessi e ciò che ha registrato |
| ~~9.1-9.2 ogni query nuova, 7.3 grafo, 8.4 collezioni~~ | ~~la query sull'**IPC**~~ | **chiuso ([decisione 0019](../decisions/0019-il-canale-dati.md))**: erano quattro comandi Tauri — `search`, `list_tags`, `graph_data`, `backlinks` — e adesso è **uno** generico (`query_index`, `app/lib.rs`), gemello di `render_view`/`view_action`: la shell ha le stesse capacità di un plugin, e una variante nuova del canale dati non vuole più un comando in più |
| 27.3 version compatibility, 20.1 versioning plugin | presidio dell'additività del contratto | `wit_conformance` confronta abi↔WIT **oggi**, mai con la versione precedente |
| ~~22.4 centro di comando LLM, 27.1 CLI, 27.2 API locale, 16.2 automazioni~~ | ~~comandi descritti a una **macchina**~~ | **chiuso ([decisione 0009](../decisions/0009-registro-dei-comandi.md) + [decisione 0010](../decisions/0010-comando-descritto-a-una-macchina.md))**: `CommandSpec { id, title, description, keybinding, params, scope }`, e l'host convalida gli argomenti contro la spec prima di chiamare il comando |
| ~~22.4 anteprima del piano, 7.2 bulk fix, 17.3 rollback, 16.3 undo~~ | ~~invocare **senza applicare** (dry-run)~~ | **chiuso ([decisione 0010](../decisions/0010-comando-descritto-a-una-macchina.md))**: `invoke(…, InvokeMode::DryRun)` → `CommandPlan` (i `DocId` impattati e un `EditRequest` per documento), con l'host che presta un `HostApi` in sola lettura — il non-scrivere è garantito, non promesso |
| 22.4 approvazione per operazione, 20.3 sandbox, 23.1 | il **consenso** dell'utente distinto dal permesso | il giro dry-run→piano→apply c'è ([decisione 0010](../decisions/0010-comando-descritto-a-una-macchina.md)) e la shell lo usa; ciò che manca è chi lo **impone** a un chiamante che non vuole simulare — è una policy sopra la firma, e adesso ha dove stare: il `Policy` della [decisione 0021](../decisions/0021-il-confine.md) |
| ~~7.2 bulk fix, 17.3 import, 11.3 editing bulk, 24.1 progresso~~ | ~~N scritture che sono **una cosa sola**~~ | **chiuso ([decisione 0011](../decisions/0011-il-lotto.md))**: `Workspace::batch(\|ws\| …)` coalizza `index-updated` e chiude con `Event::BatchEnded { batch, changed }` — una rinomina con 200 backlink passa da 201 ridisegni completi a 1, e gli eventi per-documento passano tutti. Non è una transazione: il tutto-o-niente resta al journal del §15.2 |
| ~~16.2 trigger su-modifica, 18 sync, 19.2 collaborazione~~ | ~~un evento che dice **chi lo ha causato**~~ | **chiuso ([decisione 0012](../decisions/0012-origine-degli-eventi.md))**: `handle` riceve un `Notice { event, origin }` con `Origin { actor, batch }`; `Actor::is_plugin(id)` è come un'automazione che scrive evita di richiamarsi da sola — prima l'unica difesa era il `DISPATCH_BUDGET` che tronca |
| ~~7.3 grafo, 8.2 proprietà, 7.2 salute del vault, 10 task, 15.1 citazioni~~ | ~~un canale dati che un provider possa **servire**~~ | **chiuso ([decisione 0019](../decisions/0019-il-canale-dati.md))**: le risposte del kernel sono un `IndexProvider` **registrato per primo** (`CoreIndex`, `index/core.rs`), non un `return` prima del ciclo — le sette varianti su nove che nessuno poteva servire adesso si sostituiscono chiedendole per nome |
| ~~9.2 query di terzi, 22.1 indici semantici, 11 colonne, 21.2~~ | ~~le regole del contratto raggiungibili da chi lo implementa~~ | **chiuso ([decisione 0020](../decisions/0020-le-regole-in-un-posto-solo.md))**: `fubmd_abi::rules` — `path`, `properties`, `tag`, `health` — e il kernel le usa da lì come chiunque altro. Ci è salita anche la coda di ogni risposta a `Documents` (`properties::finish`), che il §6.1 non nominava |
| ~~ogni capacità futura, 20.3 permessi, M5 (proxy WASM)~~ | ~~**una** implementazione di `HostApi` più le politiche~~ | **chiuso ([decisione 0021](../decisions/0021-il-confine.md))**: `HostApi` è un'impl in blocco sulle famiglie (`abi/traits.rs`), e delle tre scritte a mano nel kernel una sola *fa* qualcosa (`KernelHost`, `host/kernel.rs`); `ReadHost` (`host/read.rs`) non è un host mutilato ma un tipo che soddisfa `ReadApi` e **non** `HostApi`, e il rifiuto è scritto una volta sola in `Guard<H, P: Policy>` (`host/guard.rs`). Resta a mano `MemoryHost` (`features/src/testing.rs`), che è il banco di prova del §16 |
| 18.1 esclusioni dal sync, 18.2 backup, 24.2 rebuild, 2.2 portabilità | sapere se un dato persistito è derivato o autorevole | `data_write` non lo chiede, e sotto `.fubmd-data/` stanno entrambe le specie |
| ~~4.2 live preview, 8.3 naming, 2.3 path policy, 25.2 collazione~~ | ~~le regole condivise fra kernel e shell in un posto solo~~ | **chiuso ([decisione 0020](../decisions/0020-le-regole-in-un-posto-solo.md))**: una fixture generata dai tipi Rust (`rules_mirror.rs` → `rules-samples.json` → `rules-mirror.test.ts`) lega ciò che resta scritto due volte, nei due versi. La duplicazione resta e smette di essere silenziosa; la collazione **non** è nell'elenco, perché i due ordini devono divergere |
| 21.2 venti bundle, 27.3 test utilities, 27.4 | un banco di prova del kernel riusabile | 18 helper `vault()`/`workspace()` e 14 `FormatProvider` giocattolo copiati nei test |
| 6.2 temi e snippet, 25.1 accessibilità, 20.1 UI di plugin | una shell con una struttura | 14 file piatti in `frontend/src/`, `main.ts` a 1622 righe, **18** custom property in 950 righe di CSS |
| 9.1 ricerca, e ogni indice futuro (22.1, 10, 11, 15.1, 8.2) | un esito sull'alimentazione dell'indice | `on_document_indexed`, `on_document_removed` e `reconcile` restituiscono `()` (`abi/traits.rs`): il provider di ricerca **sa** di aver perso un documento e non ha come dirlo (`search.rs`) |
| 10.5 notification center e alert, 24.2 error reporting, 16.3 automation errors, 18.1 errori di sync | una destinazione per «cosa è andato storto» | 14 `eprintln!` su `stderr` e 12 `console.warn/error` nella console della webview — nessuno dei due ha un lettore in un'app impacchettata; tre commenti del kernel rimandano a «M4: notifica» |
| 18.2 versioning, 16.2-16.3 automazioni, 20.2 log plugin | che qualcuno legga l'esito di un handler | `let _ = handler.handle(notice, &mut host)` (`workspace.rs`): uno snapshot del versioning che non si scrive lascia il pannello cronologia identico a quando funzionava |
| 2.1 autosave e crash recovery, 24.2 error reporting, 3.1 vault read-only | uno stato di salvataggio e una superficie per un messaggio | `saveCurrent` non ha `catch` (`panels/document.ts`) e in `index.html` non esiste un elemento di stato: una scrittura fallita non cambia niente sullo schermo |
| 2.3 modifiche esterne, 3.1 tool di sync esterni, 18.1 per-file status, 24.2 file lock | sapere se il rilevamento delle scritture altrui funziona | il watcher è l'unico rilevatore, i suoi esiti per-path sono scartati (`let _ = ws.sync_renamed_path(…)` e `let _ = ws.sync_path(…)`, `host/watcher.rs`) e nessuno chiede mai se sia vivo |
| 27.4 testing, 27.3 plugin linting, 20.3 permission revocation | presidi esaustivi per costruzione, non a memoria | `view_refresh_masks.rs` elenca quattro view a mano e `TriesEverything` cinque capacità: la quinta view e la sesta capacità entrano restando verdi |
