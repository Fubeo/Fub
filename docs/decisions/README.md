# Decisioni

Qui stanno i **verbali** delle decisioni chiuse: il ragionamento con cui una
voce è stata risolta, cosa si è scartato e perché, e cosa resta scoperto dopo.
Non stanno in `todo.md` perché quel file è l'elenco di ciò che **resta da
fare**, e chi ci va dentro cerca il lavoro aperto, non l'archivio; ma buttarli
via non si può, perché sono «il perché, che è ciò che fra sei mesi non si
ricostruisce dal diff» (la frase è del §1.5, ora
[decisione 0003](0003-modello-del-documento.md)). Un file per decisione,
numerazione progressiva, in ordine cronologico di chiusura. Il numero e il nome
del file non cambiano mai. Il **contenuto** — cosa si è deciso, cosa si è
scartato, perché — è immutabile; la **forma** no: un verbale si può riscrivere
per renderlo più chiaro, alle condizioni della
[0143](0143-i-verbali-si-possono-riscrivere.md).

| # | Decisione | § di provenienza | Data |
|---|---|---|---|
| [0001](0001-supply-chain-e-sbom.md) | Supply chain e compliance — la sola parte che non si recupera dopo | §4.9 | 2026-07-26 |
| [0002](0002-additivita-del-contratto.md) | L'additività del contratto è una promessa senza presidio | §4.10 | 2026-07-26 |
| [0003](0003-modello-del-documento.md) | Modello del documento — le lacune che si vedono solo a valle | §1.5 | 2026-07-26 |
| [0004](0004-il-grafo-e-i-link-non-wiki.md) | Il grafo conosce solo i wikilink — e la promessa vale a metà, in silenzio | §2.21 | 2026-07-26 |
| [0005](0005-canale-dati-verso-le-view.md) | `IndexQuery` — il canale dati verso le view | §1.6 | 2026-07-26 |
| [0006](0006-import-export-come-trait.md) | Import/export come trait, non come codice dell'app | §1.7 | 2026-07-26 |
| [0007](0007-contesto-di-sessione.md) | Contesto di una view — `active_document()` non regge tab, split né selezione | §1.9 | 2026-07-26 |
| [0008](0008-modifica-chirurgica.md) | Modificare un pezzo di documento — la primitiva che non c'è | §1.16 | 2026-07-26 |
| [0009](0009-registro-dei-comandi.md) | Comandi — il trait più importante che nessuno usa | §1.1 | 2026-07-26 |
| [0010](0010-comando-descritto-a-una-macchina.md) | Un comando si descrive a un umano, non a una macchina | §1.36 | 2026-07-26 |
| [0011](0011-il-lotto.md) | Il lotto — il kernel muta **un documento alla volta** | §1.12 | 2026-07-26 |
| [0012](0012-origine-degli-eventi.md) | Gli eventi non dicono chi li ha causati | §1.18 | 2026-07-26 |
| [0013](0013-elenco-delle-capacita.md) | `HostApi` — chiudere l'elenco delle capacità prima del freeze | §1.4 | 2026-07-26 |
| [0014](0014-i-verbali-fuori-da-todo.md) | La memoria del progetto sta in un file solo | §4.13 | 2026-07-26 |
| [0015](0015-la-forma-della-shell.md) | La forma della shell, e l'unica porta verso l'host | §1.1 + §1.3 | 2026-07-26 |
| [0016](0016-cosa-e-una-view.md) | Cosa è una view | §2.1–§2.8 | 2026-07-26 |
| [0017](0017-chi-disegna-cio-che-il-core-non-conosce.md) | Chi disegna ciò che il core non conosce | §3.1–§3.6 | 2026-07-26 |
| [0018](0018-chi-vede-il-modello-parsato.md) | Chi vede il modello parsato | §4.1–§4.3 | 2026-07-27 |
| [0019](0019-il-canale-dati.md) | Il canale dati: chi risponde, e chi instrada | §5.1–§5.5 | 2026-07-27 |
| [0020](0020-le-regole-in-un-posto-solo.md) | Le regole in un posto solo | §6.1–§6.2 | 2026-07-27 |
| [0021](0021-il-confine.md) | Il confine: quante volte si scrive la disciplina | §7.1–§7.6 | 2026-07-27 |
| [0022](0022-il-kernel-a-pezzi.md) | Il kernel a pezzi: cinque proprietari invece di ventiquattro campi | §8.1 | 2026-07-27 |
| [0023](0023-chi-monta-il-kernel.md) | Chi monta il kernel: un crate `fub-host`, e l'app ridotta a colla | §8.2 | 2026-07-27 |
| [0024](0024-chi-legge-non-aspetta-chi-legge.md) | Il lock: chi legge non aspetta chi legge, e chi salva non aspetta per sempre | §8.3 | 2026-07-27 |
| [0025](0025-la-ricerca-predefinita.md) | La ricerca predefinita: di classe *omnisearch*, e built-in | [FEATURES](../FEATURES.md) §9.1 → **apre** la [seduta 21](../roadmap/21-la-ricerca-predefinita.md) | 2026-07-27 |
| [0026](0026-due-query-insieme.md) | Due query insieme: nessuna dichiarazione, una misura | §8.4 | 2026-07-27 |
| [0027](0027-il-lavoro-lungo-vede-il-vault.md) | Il lavoro lungo vede il vault: un host per chiamata, non uno snapshot | §9.1 | 2026-07-27 |
| [0028](0028-come-un-componente-smette.md) | Come un componente smette: una chiusura obbligatoria, e una disattivazione che toglie davvero | §9.2 + §9.4 | 2026-07-27 |
| [0029](0029-chiudere-un-vault-e-chiuderli-tutti.md) | Chiudere un vault, e chiuderli tutti: l'ultimo giro, il punto di consistenza, e la mappa che il backend non aveva | §9.5 + §9.6 | 2026-07-27 |
| [0030](0030-il-rilevamento-si-puo-chiedere.md) | Il rilevamento si può chiedere: una bandiera sola, e gli esiti che smettono di essere buttati | §9.7 | 2026-07-27 |
| [0031](0031-chi-possiede-i-bundle.md) | Chi possiede i bundle: una strada sola per montare, e chi smette avvisato mentre è ancora intero | §9.3 (**prima metà**) | 2026-07-27 |
| [0032](0032-il-runner-dei-job.md) | Il runner dei job: chi esegue, chi lo può fermare, e chi non si porta via il vault | §9.3 (**seconda metà**) — chiude la voce e la [seduta 9](../roadmap/09-il-lavoro-lungo-e-lo-spegnimento.md) | 2026-07-27 |
| [0033](0033-la-grana-di-un-abbonamento.md) | La grana di un abbonamento: il topic, il soggetto, e i prefissi che non sono `starts_with` | §10.1 | 2026-07-28 |
| [0034](0034-il-freno-e-il-raggruppamento.md) | Il freno e il raggruppamento: il tetto sta con chi ritira, la finestra è la velocità di chi consuma | §10.2 | 2026-07-28 |
| [0035](0035-il-lavoro-lungo-si-racconta.md) | Il lavoro lungo si racconta: chi guarda, cosa vede, e chi mette il nome sul progresso | §10.3 — chiude la [seduta 10](../roadmap/10-gli-eventi.md) | 2026-07-28 |
| [0036](0036-le-impostazioni-e-i-tre-stati.md) | Le impostazioni: chi dichiara una chiave, dove sta il suo valore, e chi la può scrivere | §11.1 — chiude il primo residuo della [0010](0010-comando-descritto-a-una-macchina.md) | 2026-07-28 |
| [0037](0037-lo-stato-di-vista.md) | Lo stato di vista: di chi è lo scroll, dove vive, e perché non viaggia col vault | §11.2 (**prima metà**) — resta il layout | 2026-07-28 |
| [0038](0038-il-kernel-possiede-il-sidecar.md) | Il kernel possiede il sidecar: chi scrive l'organizzazione, e chi la porta dietro a un rename | §11.3 | 2026-07-28 |
| [0039](0039-il-locale-e-il-caso.md) | Il locale e il caso: ciò che l'host sa e nessuno gli aveva chiesto | §12.3 | 2026-07-28 |
| [0040](0040-chi-localizza.md) | Chi localizza: il testo che porta la propria provenienza | §12.1 | 2026-07-28 |
| [0041](0041-un-errore-e-testo-che-qualcuno-legge.md) | Un errore è testo che qualcuno legge — e una domanda su cui qualcuno rama | §12.2 | 2026-07-28 |
| [0042](0042-il-catalogo-della-shell.md) | Il catalogo della shell, e la luce in cui si legge | §12.4 — chiude la [seduta 12](../roadmap/12-stringhe-errori-locale.md) | 2026-07-28 |
| [0043](0043-il-path-e-la-chiave.md) | Il path è la chiave, e un id stabile è una proprietà | §13.1 | 2026-07-28 |
| [0044](0044-lo-stato-per-documento.md) | Lo stato per-documento: un posto dichiarato, e chi ci passa dietro | §13.2 | 2026-07-28 |
| [0045](0045-l-undo-ha-due-pile.md) | L'undo ha due pile, e non si fondono | §13.3 — chiude la [seduta 13](../roadmap/13-identita-del-documento.md) | 2026-07-28 |
| [0046](0046-l-anagrafe-del-vault.md) | L'anagrafe del vault: cosa c'è, cosa ne so, e cosa non devo rileggere | §14.1 + §14.2 (**metà** della [seduta 14](../roadmap/14-entry-cartelle-lista.md)) | 2026-07-28 |
| [0047](0047-la-cartella-esiste-nel-kernel.md) | La cartella esiste nel kernel, e la lista si chiede per cartella | §14.3 + §14.4 ([seduta 14](../roadmap/14-entry-cartelle-lista.md)) | 2026-07-29 |
| [0048](0048-una-radice-sola.md) | Una radice sola, e la classe di un dato | §15.4 — la P0 della [seduta 15](../roadmap/15-il-disco.md) | 2026-07-29 |
| [0049](0049-una-posizione-dentro-un-documento.md) | Una posizione dentro un documento | §21.3 + §21.10 ([seduta 21](../roadmap/21-la-ricerca-predefinita.md)) | 2026-07-29 |
| [0050](0050-cosa-si-chiede-a-una-ricerca.md) | Cosa si chiede a una ricerca | §21.1 + §21.2 — con la 0049 chiude le quattro P0 della [seduta 21](../roadmap/21-la-ricerca-predefinita.md) | 2026-07-29 |
| [0051](0051-l-alimentazione-risponde.md) | L'alimentazione risponde, e risponde a lotti | §20.1 — l'**ultima P0** aperta del piano | 2026-07-29 |
| [0052](0052-cio-che-va-storto-e-un-evento.md) | Ciò che va storto è un evento, e il kernel smette di buttarlo | §20.2 (**meno una casella**) + §20.3 ([seduta 20](../roadmap/20-quando-qualcosa-va-storto.md)) | 2026-07-29 |
| [0053](0053-il-contratto-ha-una-sorgente.md) | Il contratto ha una sorgente, e due confini che non hanno la stessa forma | §16.4 + §16.5 ([seduta 16](../roadmap/16-crate-sdk-banchi-di-prova.md)) | 2026-07-29 |
| [0054](0054-il-banco-del-lato-provider.md) | Il banco del lato provider: dove si prova un provider contro il contratto | §16.1 ([seduta 16](../roadmap/16-crate-sdk-banchi-di-prova.md)) | 2026-07-29 |
| [0055](0055-il-banco-del-lato-host.md) | Il banco del lato host: un builder, perché i trentacinque non erano lo stesso vault | §16.2 ([seduta 16](../roadmap/16-crate-sdk-banchi-di-prova.md)) | 2026-07-29 |
| [0056](0056-un-elenco-che-e-la-sorgente.md) | Un elenco che è la sorgente, e un insieme che il compilatore chiude | §16.7 — **meno la sua seconda metà**, che diventa la §16.8 ([seduta 16](../roadmap/16-crate-sdk-banchi-di-prova.md)) | 2026-07-29 |
| [0057](0057-la-dieta-dell-ipc.md) | La dieta dell'IPC: un elenco che diventa rosso quando qualcosa si aggiunge | §16.6 (**meno una casella**) ([seduta 16](../roadmap/16-crate-sdk-banchi-di-prova.md)) | 2026-07-29 |
| [0058](0058-un-nome-che-nasce.md) | Un nome che nasce non è un nome che c'è, e la sorgente è il file | §15.5 ([seduta 15](../roadmap/15-il-disco.md)) | 2026-07-29 |
| [0059](0059-la-generazione-non-e-un-round-trip.md) | La generazione non è un round-trip, e la frase che lo diceva adesso è una rete | il doc di `FormatProvider::serialize` — la sesta specie della [§16.8](../roadmap/16-crate-sdk-banchi-di-prova.md), **non** una voce di [todo.md](../todo.md) | 2026-07-30 |
| [0060](0060-il-modello-dice-il-vero-sui-byte.md) | Il modello dice il vero sui byte del file, e un corpus che nessuno confronta non cresce | §17.1 (**prima metà**: il corpus e il fuzzing) — resta il banco delle prestazioni ([seduta 17](../roadmap/17-presidi-che-restano.md)) | 2026-07-30 |
| [0061](0061-un-giro-che-non-passa-dal-modello.md) | Un giro che non prende i byte dal modello, e uno che ci passa | §17.1 (**una casella su cinque**: il round-trip sul corpus) — resta il banco delle prestazioni ([seduta 17](../roadmap/17-presidi-che-restano.md)) | 2026-07-30 |
| [0062](0062-il-log-e-il-pavimento-l-evento-e-la-porta.md) | Il log è il pavimento, l'evento è la porta — `tracing` al posto di `eprintln!`, con log su file, livelli e log per-plugin | §17.3 — chiude la voce **e** la casella residua della §20.2: i ventisette punti di `stderr` hanno due destinazioni, non una ([seduta 17](../roadmap/17-presidi-che-restano.md)) | 2026-07-30 |
| [0063](0063-la-maschera-e-dell-esemplare.md) | La maschera è dell'esemplare, e la risposta stava già nell'elenco | §22.3 — chiude la voce **meno una casella**: la query incorporata in una nota, che non è un esemplare di `ViewSpec` ([seduta 22](../roadmap/22-cosa-sa-dire-un-abbonamento.md)) | 2026-08-01 |
| [0064](0064-il-supporto-sta-sotto.md) | Il supporto sta sotto, e la specie di una voce non segue il link | §15.1 — chiude la voce **meno una casella**: le tre righe di `.fub/` che scrivono con `write_atomic` e aspettano il §15.2 ([seduta 15](../roadmap/15-il-disco.md)) | 2026-08-01 |
| [0065](0065-una-scrittura-o-c-e-o-non-c-e.md) | Una scrittura o c'è o non c'è, e i due casi in cui il file non è nostro | §15.2 (**metà voce**: la scrittura) **più** la casella residua della [0064](0064-il-supporto-sta-sotto.md) — resta il recovery ([seduta 15](../roadmap/15-il-disco.md)) | 2026-08-01 |
| [0066](0066-un-aggiornamento-non-e-una-scrittura.md) | Un aggiornamento non è una scrittura, e il lock costa una promessa | §15.2 (la *lost update*, che la [0065](0065-una-scrittura-o-c-e-o-non-c-e.md) aveva rimandata) — resta il recovery ([seduta 15](../roadmap/15-il-disco.md)) | 2026-08-01 |
| [0067](0067-il-registro-di-cio-che-e-successo.md) | Il registro di ciò che è successo, e l'inverso al posto del contenuto | §15.2 (**una casella su tre** del recovery: il journal delle mutazioni) — restano il buffer di crash e i comandi di manutenzione ([seduta 15](../roadmap/15-il-disco.md)) | 2026-08-01 |
| [0068](0068-un-vault-si-apre-per-quel-che-si-legge.md) | Un vault si apre per quel che si legge, e dice cosa non ha letto | §15.7 (**la prima metà**: fallire in parte) — resta la forma dell'apertura ([seduta 15](../roadmap/15-il-disco.md)) | 2026-08-01 |
| [0069](0069-cosa-sa-dire-un-abbonamento.md) | Cosa sa dire un abbonamento: una dichiarazione che nessuno valuta mente a chi la scrive | §22.1 + §22.2 — chiude tutte e due le voci e ne **apre una**, la §22.4 ([seduta 22](../roadmap/22-cosa-sa-dire-un-abbonamento.md)) | 2026-08-01 |
| [0070](0070-un-vault-si-apre-in-due-tempi.md) | Un vault si apre in due tempi, e il secondo è un job | §15.7 (**la seconda metà**: la forma dell'apertura) — chiude la voce ([seduta 15](../roadmap/15-il-disco.md)) | 2026-08-03 |
| [0071](0071-una-feature-si-spegne-dove-si-dichiara.md) | Una feature si spegne dove si dichiara | §16.3 (**il primo tempo**: la cargo feature per bundle) — chiude **mezza** voce, il secondo tempo resta ([seduta 16](../roadmap/16-crate-sdk-banchi-di-prova.md)) | 2026-08-03 |
| [0072](0072-un-numero-si-scrive-accanto-a-come-si-ricava.md) | Un numero si scrive accanto a come si ricava | §16.8 — chiude la voce, e con lei **l'ultima viva della seduta 16** oltre al secondo tempo della 16.3 ([seduta 16](../roadmap/16-crate-sdk-banchi-di-prova.md)) | 2026-08-03 |
| [0073](0073-una-condizione-che-nessuno-valuta.md) | Una condizione che nessuno valuta è una scadenza senza data | §16.3 — **non chiude niente**: presidia la *condizione* che tiene fuori il secondo tempo, perché finora a valutarla non c'era nessuno ([seduta 16](../roadmap/16-crate-sdk-banchi-di-prova.md)) | 2026-08-03 |
| [0074](0074-selezionare-non-e-raccontare.md) | Selezionare non è raccontare | §21.9 — chiude la voce, e con lei l'unica della [seduta 21](../roadmap/21-la-ricerca-predefinita.md) che chiedesse una **misura** invece di un comportamento | 2026-08-03 |
| [0075](0075-una-view-non-chiede-con-una-finestra.md) | Una view non chiede con una finestra, e chi scrive le versioni è chi le disegna | §1.2 (**una casella su due**: cestino e cronologia come `ViewProvider`) — resta il modello di layout ([seduta 18](../roadmap/18-editor-e-tastiera.md)); e **tre righe su cinque** del debito del §16.6 | 2026-08-03 |
| [0076](0076-le-impostazioni-vivono-nel-vault.md) | Le impostazioni vivono nel vault, e la macchina tiene solo ciò che serve quando il vault non si apre | **Revisione** della [0036](0036-le-impostazioni-e-i-tre-stati.md), §11.1 ([seduta 11](../roadmap/11-impostazioni-e-i-tre-stati.md)): non chiude una voce nuova, cambia dove sta un valore | 2026-08-03 |
| [0077](0077-una-scorciatoia-e-una-chiave.md) | Una scorciatoia è una chiave di impostazione, e un comando di shell è un comando | §18.2 ([seduta 18](../roadmap/18-editor-e-tastiera.md)) — chiude registro unico, palette fuzzy, conflitti e scorciatoie riconfigurabili; resta il solo accordo **in sequenza** | 2026-08-03 |
| [0078](0078-i-riquadri-sono-un-fatto-della-shell.md) | I riquadri sono un fatto della shell, e il buffer è del documento | §1.2 ([seduta 18](../roadmap/18-editor-e-tastiera.md)) — **chiude la voce**, ultima casella; chiude anche la metà rimasta del §11.2 e sblocca la §3.3. Zero firma: il `pane` del `ViewContext` c'era dalla [0007](0007-contesto-di-sessione.md) | 2026-08-03 |
| [0079](0079-il-grafo-esce-dall-overlay.md) | Il grafo esce dall'overlay, e una tab non è più per forza un documento | §3.3 ([seduta 18](../roadmap/18-editor-e-tastiera.md)) — **chiude la voce**, lasciando una casella. Il grafo è un `ViewProvider` su `ViewSurface::Main` (la prima a dichiararla) e disegna via `UiKind::Custom`: i **dati** passano dal canale di tutti, i **pixel** restano della shell. Una tab è un tipo discriminato, non un path con un prefisso. Zero firma, la terza volta di fila | 2026-08-03 |
| [0080](0080-un-guasto-si-dice-a-chi-sta-lavorando.md) | Un guasto si dice a chi sta lavorando, e il salvataggio ha un esito | §20.4 ([seduta 20](../roadmap/20-quando-qualcosa-va-storto.md)) — **chiude la voce**, e con lei la seduta meno la §20.5. I quattordici `console.warn`/`console.error` della shell passano da `notify`; il salvataggio ha un **esito** oltre all'avviso, con quattro stati e la regola che il guasto vince sul buffer sporco. Un test ha trovato che `notify` senza DOM lanciava: il canale dei guasti si rompeva mentre gli si raccontava un guasto | 2026-08-03 |
| [0081](0081-un-accordo-ha-un-proprietario.md) | Un accordo ha un proprietario, e i due registri si guardano insieme | Nessuna voce: un **difetto** (`Mod-Shift-f` dichiarato dal kernel e dalla shell, e vinceva il comando sbagliato) e il presidio che non poteva vederlo — in un banco `allCommands()` è metà dei dati. L'accordo resta alla shell, `search.open` lo perde e resta nella palette; la domanda si fa sui due registri insieme con una fixture generata, e `SHELL_KEYS` la tiene completa **per tipo**. Lascia aperta la §18.2 | 2026-08-03 |
| [0082](0082-una-porta-per-chi-cerca.md) | Una porta per chi cerca, e la ricerca dentro la nota aperta | §21.4 ([seduta 21](../roadmap/21-la-ricerca-predefinita.md)) — **chiude la voce** e **mezza** §21.5. Tutto ciò che accetta del testo e propone note passa da `IndexQuery::Documents`, e le query si compongono nel contratto e non nei pannelli; la ricerca nella nota è una clausola di due letterali su `Mod-f`, zero firma; l'autocompletamento andrà alla query con prefisso e non a una lista spinta, che sarebbe un indice alimentato dagli eventi | 2026-08-03 |
| [0083](0083-le-due-superfici-che-restavano.md) | Le due superfici che restavano, e il giro per battuta misurato | §21.5 ([seduta 21](../roadmap/21-la-ricerca-predefinita.md)) — **chiude la voce**, di cui la 0082 aveva chiuso la metà che era decisione. Il quick switcher nasce sulla porta unica (`Mod-o`, campi sul nome, `Excerpts::Omit`); l'autocompletamento perde `validFor` e prende `filter: false`, perché l'ordine è la rilevanza del kernel; il giro per battuta è **misurato** (fase 5 di `una_ricerca.rs`: ~3 ms, e l'elenco intero costa meno di lato kernel — ciò che si compra è la correttezza). Le recenti a mani vuote vivono quanto la finestra, per non anticipare la §21.7 | 2026-08-03 |
| [0084](0084-un-peso-e-una-preferenza.md) | Un peso è una preferenza, e una copia in RAM ha bisogno di chi la rinfresca | §21.6 ([seduta 21](../roadmap/21-la-ricerca-predefinita.md)) — **chiude la voce**. I due boost cablati diventano **quattro** chiavi (`search.boost.*`, `program_writable`, `min 0.0` / `max 100.0`), e lo zero è ammesso perché in tantivy non esclude il documento: il campo combacia ancora e smette solo di punteggiare — «non cercare lì» resta `TextQuery.fields`. `query()` non riceve un host, quindi i pesi si leggono in `activate` e vivono in un `Arc<RwLock>` che un `EventHandler` sul prefisso rinfresca a vault aperto: un peso si **tara**, e una taratura che passa dalla riapertura non la fa nessuno. Schema e stringhe stanno nella feature e non nell'host — l'eccezione è il versioning, il cui interruttore la feature non possiede — e il banco importa i default invece di copiarli. Zero firma | 2026-08-03 |
| [0085](0085-leggere-non-e-cambiare.md) | Leggere non è cambiare, e un anello non è un costo | Nessuna voce: tre **difetti** sul percorso di una battuta nella casella di ricerca — ma uno solo era un **anello**. Il rilevatore contava le aperture come cambiamenti, e chi apre di più i documenti del vault è Fub stesso (la §21.3 apre il sorgente di ogni riga di risultati): una ricerca leggeva sessanta note, il kernel le rileggeva per scoprirle identiche, e quelle riletture erano altre sessanta aperture, fino all'overflow del ponte. Gli altri due — `locate` che scandiva un termine oltre il tetto con un `contains` per ogni inserimento, e le righe attaccate una per una a una lista già nella pagina — erano costi **lineari**, gravi ma senza retroazione. Nel dubbio (`Any`, `Other`) si conta come cambiamento: una rilettura di troppo costa un file aperto, una di meno un indice che drifta in silenzio. Fino all'overflow, l'anello non diceva niente a nessuno | 2026-08-04 |
| [0086](0086-una-cronologia-e-la-sua-porta.md) | Una cronologia sta dove il recinto la mette, e il recinto decide anche chi la cancella | §21.7 ([seduta 21](../roadmap/21-la-ricerca-predefinita.md)) — **chiude la voce**. Le ricerche e le note recenti stanno nello **stato di vista della shell** (chiave `history`, accanto a `layout`): non viaggia col vault, è recintata per proprietario, `forget_vault` la dimentica già. Il doc di `ViewStateRead` si allarga apposta invece di far finta che una cronologia sia «dove eri rimasto». Il prezzo è dichiarato: il recinto è per proprietario e l'id di chi scrive non è un parametro, quindi un comando del registro **non ci arriva** — «cancella» è `shell.history.clear`, comando di shell senza accordo, e da CLI non si invoca. L'interruttore è `history.enabled` di `fub.core` (non `program_writable`, riga di privacy della 0036), **acceso** di default perché il dato non esce, e **di vault** perché una scelta di privacy che vale su un portatile e non sull'altro non protegge: l'interruttore viaggia, ciò che governa no. Spegnerlo **cancella**. Dieci per elenco, una ricerca ripetuta risale, si ricorda la ricerca **conclusa** e non le battute; anche le note aperte diventano persistenti, sotto lo stesso interruttore. Dal risultato vuoto si crea la nota, col nome che passa da `rules/nome-cercato.ts` (`name` è un path, non un'etichetta) e senza indovinare se sia libero. Il gesto ha due superfici (la ricerca del vault e il quick switcher) e non una terza: `doc-search.ts` usa la stessa chiave `search.empty`, ma non trovare dentro la nota aperta non vuol dire che manchi una nota. Zero firma | 2026-08-04 |
| [0087](0087-il-testo-che-sta-dentro-gli-allegati.md) | Il testo che sta dentro gli allegati | §21.8 — **chiude la voce e con lei la [seduta 21](../roadmap/21-la-ricerca-predefinita.md)**. I due blocchi che la voce nominava non c'erano più: `list_documents` è via con la [0046](0046-l-anagrafe-del-vault.md), `parse` prende un `DocumentSource` dalla [0017](0017-chi-disegna-cio-che-il-core-non-conosce.md). Mancava il **tragitto**: `FormatDescriptor::source` era consultato in `parse_from_disk` — cioè da chi apre e da chi disegna — e **non** da chi indicizza, che leggeva testo comunque. Lo stesso documento aveva due destini a seconda di chi lo leggeva, e un provider a byte vedeva i propri documenti all'apertura e li vedeva scartare in silenzio all'indicizzazione, con un «non è UTF-8» su un file che nessuno aveva promesso fosse testo. Adesso il descrittore si consulta in **un posto solo** (`source_from_disk`), e l'impronta si prende sui byte con `Revision::of_bytes` — che **è** `of`, stesso FNV-1a, perché un documento non deve cambiare impronta il giorno che qualcuno lo rivendica a byte, o rivendicarlo costerebbe una riscansione. La firma nuova è **una**, `read-document-bytes` su `host-vault-read`: senza, un estrattore di terzi (PDF, OCR, trascrizione — le nove voci di FEATURES §9.1, che in omnisearch stanno in un'estensione a parte) non ha modo di chiedere ciò su cui deve lavorare. Sta accanto a `read-document` e non al posto suo — chi legge testo non deve poter dimenticare di decodificare — e sotto lo **stesso** permesso, perché leggere byte non è un grado di fiducia in più. È additiva, quindi `wit/frozen/0.1.0.wit` non si tocca; ma dopo il freeze sarebbe stata inutile, non impossibile. Il cliente che valida il canale è un provider di prova a byte, **non** un estrattore di PDF: nessun crate di parsing entra nel workspace. `MemoryHost` tiene i documenti a byte, o l'estrattore non si potrebbe scrivere sul banco del lato provider | 2026-08-04 |
| [0088](0088-cio-che-non-e-ancora-successo.md) | Ciò che non è ancora successo, e chi lo ripara | §15.2 — **chiude la voce**. Le due caselle erano scritte prima del supporto, della scrittura atomica e del journal, e rilette contro quel codice ne è uscita una specifica già scritta: il journal diceva di sé di **non** contenere il buffer sporco, quindi le bozze sono il suo **gemello dall'altro verso** — il registro conserva l'*inverso* e mai il testo di ciò che è **successo**, le bozze conservano soltanto il testo di ciò che **non è ancora successo**. Da lì il posto (`.fub/drafts/`, profondità uno = autorevole, [0048](0048-una-radice-sola.md): un testo mai salvato è per definizione l'unica copia) e la forma (**un file per bozza**, o ogni autosave sarebbe un *aggiornamento* di un file condiviso, cioè il difetto che la [0066](0066-un-aggiornamento-non-e-una-scrittura.md) aveva appena tolto). Lo stato di vista è scartato perché è la [0086](0086-una-cronologia-e-la-sua-porta.md) **all'incontrario**: là decideva che il dato non deve viaggiare, qui che deve. La tensione di strato è dichiarata invece che risolta di straforo — *la shell decide quando una bozza esiste, il kernel decide cosa vuol dire tenerla*. La scrittura ha due porte IPC e non un comando, e la capacità manca **per sempre** e non «in attesa di un cliente»: il testo non salvato è il dato più privato di un vault, e una `draft_write` lo darebbe a ogni plugin. La lettura invece è del canale di tutti (`IndexQuery::Drafts`), che manda i **fatti** (`base`, `current`, `exists`) e tace sul giudizio — con `base` che vuol dire «non lo so» e un caso `incerta` distinto da `divergente`, perché trattare ogni incertezza come un conflitto insegna a cliccare senza leggere. Una bozza **orfana** non si raccoglie: è l'unica copia rimasta. Il recupero è un **buffer sporco precaricato**, non un dialogo modale. Dei quattro comandi di manutenzione uno era già altrove e la casella si **riformula**: `vault_health` resta una query — una lettura che risponde con dati non può essere un comando (0013) — e guadagna il suo primo lettore, il rapporto diagnostico, perché non ne aveva **nessuno**. Gli altri tre sono comandi del registro con la regola che generalizza la 0086: **la dichiarazione sta nel registro, l'esecuzione sta dove sta il potere** — così sono in palette, rimappabili e raggiungibili dalla CLI senza che una capacità nuova compaia sul confine. Un test ha trovato il modo tipico in cui un ramo privilegiato sbaglia: intercettare *prima* della coda comune consegna una chiave di catalogo invece di una frase. E il banco di conformità ha insegnato una riga che non era scritta: l'ordine dei casi **è** il discriminante dell'ABI, quindi *additiva vuol dire in fondo* | 2026-08-04 |
| [0089](0089-da-cosa-e-partita-una-scrittura.md) | Da cosa è partita una scrittura | §18.1 — **chiude la voce**. La guardia c'era e valeva per metà del sistema: la [0008](0008-modifica-chirurgica.md) aveva messo la revisione nella firma di `apply_edit`, che è la primitiva dei **provider** — l'editor salva il buffer intero, cioè chiama `write_document`, che non portava niente, quindi il salvataggio **copriva** una scrittura altrui che il watcher non aveva visto. La prima casella **non si esegue, si riformula**: nominava un canale che porta il modello alla webview, e la [0018](0018-chi-vede-il-modello-parsato.md) ha deciso che non ci sarà — la live preview decora un *buffer*, il modello è del *file* — quindi il secondo livello passa alla [§4.4](../roadmap/18-editor-e-tastiera.md#44-due-parser-per-la-stessa-sintassi), dove è già scritto dal lato giusto. Terza volta di fila che una voce ferma va rimisurata, e con un modo nuovo: le due precedenti trovavano ciò che la voce aspettava **caduto**, questa trova ciò che chiedeva **deciso di no**. `write_document` prende una `base: Option<Revision>` e rende la revisione **prodotta**. Opzionale qui e obbligatoria in `apply_edit` non è un compromesso: un edit non **esiste** senza la revisione su cui è calcolato, una riscrittura totale è compiuta da sé — un importer non corregge un testo che ha letto, lo **detta**, e una base inventata è una guardia che dice sempre sì. Il ritorno paga due volte: rende la guardia una **catena** (senza, il secondo salvataggio nominerebbe la base d'apertura e fallirebbe contro sé stesso) e riempie `DraftInfo::base`, che la [0088](0088-cio-che-non-e-ancora-successo.md) aveva dovuto lasciare `null` perché ricalcolare FNV-1a in TypeScript sarebbe stata una seconda verità — il criterio di leva è **due caselle che si pagano una volta sola**. Nessuna delle due metà è additiva (arità e tipo di ritorno), e il ripiego — una `write-document-based` accanto — è **scartato**: lascerebbe per sempre due modi di scrivere un documento, di cui uno cieco. Si **ritaglia la linea di base** (`wit/frozen/0.1.0.wit`), che prima del freeze è l'uscita onesta. Il confronto è col **disco** e non con l'anagrafe — una guardia che si fidasse della cache direbbe di sì proprio nel caso che deve prendere — e la lettura in più si paga solo se la base c'è. Di là dal confine un conflitto **non è un disco pieno**: il secondo si riprova, il primo no perché riprovare è la sovrascrittura appena impedita, e ciò che manca è una **decisione**. Niente dialogo modale: buffer sporco che aspetta come nel recupero bozze, e due **comandi della shell** ([0077](0077-una-scorciatoia-e-una-chiave.md)) che nominano *cosa si perde* e non hanno scorciatoia. «Tieni il mio» **azzera** la base invece di rileggerla, o sarebbe la sovrascrittura di prima con un giro in più. Nessun terzo modo di dire «è cambiato sotto»: `cambioSotto` resta l'avviso precoce e smette di essere ciò da cui dipende la correttezza | 2026-08-04 |
| [0090](0090-una-sequenza-e-una-modalita-che-scade.md) | Una sequenza è una modalità che scade | §18.2 — **chiude la voce, e con lei le voci native della [seduta 18](../roadmap/18-editor-e-tastiera.md)**. La voce diceva che uno stato, un timeout, un annullamento e la regola del prefisso «non si esprimono nella sintassi che la `CommandSpec` dichiara oggi»: le prime quattro cose sono vere e sono state fatte, l'ultima è **falsa**. `keybinding` è un `Option<String>` dalla [0009](0009-registro-dei-comandi.md) e il contratto non dichiara una *sintassi* — dice per iscritto che «chi assegna i tasti è la shell» — quindi `"Mod-k d"` ci sta senza chiedere niente: **zero firma e zero Rust**, non un byte di là dal confine. La voce aveva confuso il *tipo* con la *sintassi*, che non è mai stata nel contratto ma in quaranta righe di TypeScript: quarto caso di fila di una voce da rimisurare, e con un modo ancora nuovo — non «caduto» né «deciso di no», ma **mai stato vero**. L'esempio `g d` è **ineseguibile** e sparisce con la premessa: è un gesto vim, e sotto questa tastiera c'è un editor in cui `g` è testo di qualcuno. Si prende il modello **VS Code**, e la regola ha due metà che si tengono: il primo accordo porta un modificatore per la ragione di sempre, il secondo **può essere nudo proprio perché il primo non lo era** — `Mod-k` apre una modalità che dura due secondi, si vede e ha una porta d'uscita, e dentro quella finestra la `d` non è di nessuno. «La shell non ha modi» resta vero al singolare: non ne ha *in cui si sta*, e ne apre uno *che scade*. Timeout **2000 ms** — sopra un gesto deliberato, sotto il tempo in cui si distoglie lo sguardo — e la scadenza **non esegue niente**, o la sorpresa arriverebbe due secondi dopo l'ultimo tasto. `Escape` è trattato **prima** del registro e non si può contendere: una via d'uscita che una scorciatoia potesse rubare non sarebbe una via d'uscita. Il tasto che non continua niente si **consuma**: chi ha premuto `Mod-k` ha lasciato il gesto di scrivere, e una `x` in mezzo a una frase è l'unico esito imprevedibile. Il banco ha trovato ciò che il progetto non vedeva: i **modificatori da soli non annullano**, o `Mod-k D` si romperebbe a ogni maiuscola. Sul prefisso vince il **corto**, per tre ragioni di cui pesa la terza: un tasto che funziona oggi non deve rallentare domani, chi arriva paga, e soprattutto la cosa si decide **guardando il registro fermo** — quindi `prefissiOscurati` la dice all'avvio invece di lasciarla scoprire premendo, che è il modo in cui una sequenza resterebbe non onorata di nascosto. Lo stato è una variabile di `ui/keyboard.ts` e **non un secondo registro** (di registri ce n'è uno dalla [0077](0077-una-scorciatoia-e-una-chiave.md)); il modulo nasce perché tre responsabilità in `main.ts` sarebbero monolite (§1.2), con la **regola** pura in `avanza` e il DOM di là. Le sequenze hanno reso visibile un difetto vecchio: un accordo malformato era **escluso** dal conteggio dei conflitti invece che **segnalato**, e da quando una scorciatoia è una stringa che l'utente scrive a mano è un silenzio che non si può tenere — `accordiRifiutati` lo dice, e `Ctrl-k`, che passava valendo `k`, diventa un rifiuto. La shell **non spende una sequenza propria**: tredici comandi con tredici tasti liberi non sono il momento, e il primo cliente è l'utente. La seconda casella **si trasferisce** alla [§16.3](../roadmap/16-crate-sdk-banchi-di-prova.md) dopo aver misurato la terza strada che la voce non aveva guardato — un `CommandProvider` di prossimità registrato dall'host — che si rompe in cinque punti, di cui quattro sono lavoro e il quinto una **contraddizione**: i provider si registrano per vault e `keys.*` è di scope `Vault`, ma `shell.vault.open` esiste *prima* di ogni vault | 2026-08-04 |
| [0091](0091-un-orario-di-parete-non-e-un-intervallo.md) | Un orario di parete non è un intervallo | §22.4 — **chiude la voce, e con lei la [seduta 22](../roadmap/22-cosa-sa-dire-un-abbonamento.md)**. `TimerSchedule` cresce **in coda** con `at-wall-clock(wall-clock)`, e il caso è la parte facile: `timer-schedule` non è in `frozen/0.1.0.wit` — nato con la [0069](0069-cosa-sa-dire-un-abbonamento.md) e mai pubblicato — quindi niente da ritagliare, e in coda per la regola della [0088](0088-cio-che-non-e-ancora-successo.md) (l'ordine dei casi **è** il discriminante). La parte vera è **dove va la regola di una sveglia che il contratto non può calcolare da sé**: `nth_after` restituisce secondi *dalla registrazione*, e quanti ne manchino alle nove è funzione di *che ore sono adesso* — un ingrediente che quella firma non riceve senza smettere di essere pura. Delle tre risposte misurate si prende la terza: **una seconda regola accanto alla prima**, `WallClock::next_after`/`latest_upto`, che l'ora civile la **riceve** invece di leggerla. Firma pubblicata **non toccata**; `nth_after` risponde `None` a un orario di parete, e perché quel `None` non si confonda con quello di un `after` finito c'è `wall_clock()` — un caso dichiarato e non onorato sarebbe la versione con l'orologio della bugia che la [0077](0077-una-scorciatoia-e-una-chiave.md) e la [0090](0090-una-sequenza-e-una-modalita-che-scade.md) rifiutano. **Quinta volta che una voce ferma si rimisura**, di nuovo con la premessa mai stata vera: la voce escludeva il locale perché «dice come si scrive un'ora, non in che fuso si vive», e il `Locale` di questo repo dice **tutte e due** — `locale.timezone` è il nome IANA, è già un'impostazione, e ha già la scala vault → macchina → sistema. I tre candidati non erano tre risposte: erano tre strati già montati, e leggerli ha risparmiato una chiave nuova. Il fuso quindi è **della macchina** per default — «alle 9» vuol dire «quando comincio a lavorare», e ogni macchina fa già girare il proprio scheduler — con un terzo strato nuovo, `zone: option<string>`, per le sveglie il cui significato è **ancorato a un posto** (il digest delle 9 dell'ufficio di Roma, che un vault condiviso fra due paesi non sapeva dire). Un nome che il tzdb non conosce **non fa suonare** e non ripiega su UTC. **L'ora legale non vuole un campo**: le 2:30 che esistono due volte suonano una volta per l'invariante «un'occorrenza è la sua data civile e non il suo istante», e le 2:30 che non esistono si spostano in avanti della durata del salto (*compatible* di RFC 5545) — la sveglia non perde mai un giorno. Il campo serve a una domanda che la voce **non faceva**: cosa fa un'occorrenza passata mentre nessuno guardava. `catch_up_seconds: u64` è una **finestra** e non una bandiera, e la differenza si vede sulla macchina riaccesa dopo due giorni — che così suona **zero** volte, non due. Nell'host nasce `parete.rs`, la **seconda sorgente di tempo**: le due non si mescolano — l'attesa resta sempre monotona, perché aspettare è «per quanto» e mai «fino a quando» — e si toccano in un punto solo. Un difetto trovato scrivendo: con la sola *ultima occorrenza* una sveglia puntuale con finestra `0` non suonava **mai**, perché ogni suonata sarebbe stata un recupero; *cosa ho considerato* e *cosa sto aspettando* sono due domande, e adesso due campi. **`jiff` misurato: dieci pacchetti**, da 541 a 551 — dieci volte il conto di `tracing` ([0062](0062-il-log-e-il-pavimento-l-evento-e-la-porta.md)), scritto in `Cargo.toml` invece che nascosto — e **solo in `fub-host`**: `fub-abi` resta senza dipendenze di date, che è la conseguenza diretta di dove è finita la regola. L'alternativa a zero crate è scartata per iscritto: «alle 9 a Roma» su una macchina in UTC è sbagliato di un'ora per metà anno, e una data formattata male si vede mentre una sveglia in ritardo no. Resta una casella: il recupero **attraverso un riavvio** vuole un posto dove persistere l'ultima occorrenza onorata | 2026-08-04 |
| [0092](0092-una-base-si-dichiara.md) | Una base si dichiara | §23.11 — **chiude la voce**. `write_document` prendeva `base: Option<Revision>`; adesso prende un `WriteBase` a due casi nominati, `DescendsFrom(revision)` e `Dictated`. I modi di scrivere restano **due**: cambia che si **dicano**. Con l'`Option`, scrivere ciechi era ciò che succedeva **omettendo** un argomento — cioè il default, e un default non lo sceglie nessuno. L'argomento della [0089](0089-da-cosa-e-partita-una-scrittura.md) per cui era opzionale **non è stato riaperto** ed è la ragione per cui `Dictated` esiste invece di sparire: una riscrittura totale può essere compiuta da sé, e una base inventata è una guardia che dice sempre sì. Ciò che non reggeva era la *forma*: la 0089 si è chiesta **se** la guardia esistesse e ha risposto bene, non si è chiesta **come si sbaglia a non usarla** — e la risposta era «non si sbaglia, si dimentica». Ne viene un fatto scritto invece che nascosto: **la linea di base è ritagliata due volte sulla stessa firma in tre commit**, col paragrafo nuovo accanto a quello della 0089 in `frozen/0.1.0.wit` e la riga nella tabella di [wit-congelato](../architecture/wit-congelato.md). Fra i due ritagli non c'è stato nessun rilascio: paga il repo, non chi ha compilato un plugin. È il criterio della [0007](0007-contesto-di-sessione.md) — «un flag che chiunque può dimenticare di leggere protegge meno di un campo che, quando non è vero, non c'è» — letto dall'altro verso: **quando c'è una scelta, la si nomina**; terza applicazione di fila dopo la [0091](0091-un-orario-di-parete-non-e-un-intervallo.md). I chiamanti sono passati **uno per uno** e non hanno la stessa risposta: **dettano** il ripristino di una versione (guardarlo con la revisione corrente vorrebbe dire rifiutarlo ogni volta che c'è qualcosa da ripristinare, cioè sempre), l'import (e con `ConflictPolicy::Replace` la sovrascrittura è per di più *richiesta*), `create_document` e `create_note` (chiedere una base sarebbe chiedere l'impronta del nulla); **discendono** la shell e l'`apply_edit` del doppio dell'SDK, che scrive il testo su cui ha appena calcolato gli edit. `Workspace::write_document` a due argomenti **sparisce**: era la stessa trappola in casa — due firme per la stessa domanda, di cui una cieca e più corta — e lasciarla avrebbe chiuso la voce a metà. Prezzo misurato: duecentonovantadue punti dichiarano, duecentosettantaquattro dicono `Dictated`, e prima erano duecentosettantaquattro scritture cieche che non si potevano elencare. Nella shell il difetto si ripeteva un piano sopra: `Buffer.base` era `string \| null` e il `null` teneva insieme «non so da cosa discendo» e «ho scelto di sovrascrivere» — adesso le due si dichiarano dove **succedono**, e il comando IPC prende un `base` **non opzionale** (un campo mancante è un errore di deserializzazione, cioè una shell che dimentica smette di scrivere invece di scrivere di nascosto). **Il ritrovamento**: il sospetto che `wit_additivity` non vedesse un parametro *ritipato* è caduto eseguendo — `prefix` confronta le coppie (nome, tipo) e il messaggio nomina già il ritipo — ma fra le rotture simulate quella non c'era: mancava la **prova**, non il presidio, e adesso sono **venti**. Sesta volta di fila che rileggere una voce contro i sorgenti la cambia. E il pericolo che la voce nominava è **messo in scena**: accanto alla [0030](0030-il-rilevamento-si-puo-chiedere.md), il banco *è* un vault con `watching: false` — se lo fa dichiarare da `VaultStatus` invece di assumerlo — e prova che la stessa scrittura, sullo stesso documento cambiato sotto, dà **due esiti opposti** a seconda di quale caso si è nominato: una proprietà che un `Option` non può esprimere, perché il secondo esito è la sua assenza | 2026-08-04 |
| [0093](0093-le-selezioni-sono-n-e-il-buffer-e-uno.md) | Le selezioni sono N, e il buffer è uno | §23.4 — **chiude la voce**, meno una casella residua nominata. `ViewContext.selection` era un `Option<Selection>` e `Selection` ne descriveva **una**; adesso il campo è `selections: Option<SelectionSet>`, e `SelectionSet` è un `variant` a due casi — `Anchored`/`Floating` — ognuno con una **primaria** e una lista di **secondarie**. Cinque tipi dove ce n'era uno, e non è «metterci una lista»: da uno a molti cambiano due cose che nella forma di prima non si vedevano. **La premessa della voce era falsa**, ed è la sesta volta di fila (0087–0093) che rileggerla contro il sorgente la cambia prima di progettarla: «la primaria è la prima della lista, che è la regola di CodeMirror» — non lo è. `EditorSelection` ha `ranges` **e** `mainIndex`, e la sua documentazione dice che quell'indice è *«usually the range that was added last»*, cioè col gesto per cui il multi-cursore esiste (Alt-click) la primaria è **l'ultima aggiunta**. «La prima per convenzione» non era gratis: era una conversione che **perde** quale fosse la primaria — nominarla costa un campo, dedurla costa un dato. **Il fatto che ha spostato il resto**: il multi-cursore non era una funzione da costruire — `basicSetup` di CodeMirror porta `allowMultipleSelections`, la selezione rettangolare e `Mod-d` **da sempre**, quindi chi usa Fub tre cursori li fa e li vede. Fuori era la sola facoltà di **dirlo**: `paneContext()` leggeva `selection.main` e gli altri due morivano lì. Quindi non è una firma preparata per una funzione futura — la bugia che la [0077](0077-una-scorciatoia-e-una-chiave.md), la [0090](0090-una-sequenza-e-una-modalita-che-scade.md), la [0091](0091-un-orario-di-parete-non-e-un-intervallo.md) e la [0092](0092-una-base-si-dichiara.md) hanno rifiutato quattro volte di fila — è una firma che **recupera** ciò che l'app già faceva. **La primaria è un campo** (quarta applicazione di fila del «ciò che si sceglie si nomina», dopo 0007→0091→0092), e non per igiene: rende vero per costruzione che un insieme **non è mai vuoto**, mentre `list` + indice rappresenta due stati che non esistono — la lista vuota e l'indice fuori range. «Nessun cursore» resta l'`Option` della [0007](0007-contesto-di-sessione.md): la lista vuota non diventa un secondo modo di dirlo, smette di esistere. **Lo span sale sopra la lista**, ed è la parte che valeva il verbale: la condizione che lo annulla è *il buffer è sporco*, cioè una proprietà del **buffer** — una per pannello, e infatti `paneContext()` legge `dirty` una volta sola. Con N selezioni la forma di prima direbbe che possono cadere una alla volta, e non è vero; ora un insieme metà posizionato e metà no **non è rappresentabile**, perché un provider che agisse solo sulle posizionate farebbe due dei tre punti che l'utente vede, cioè metà di ciò che gli è stato chiesto. Prezzo dichiarato: chi vuole il solo testo passa per due casi — ma dei cinque consumatori **tre** vogliono le coordinate e per loro il `variant` è un guadagno. I cinque sono passati **uno per uno** e non hanno la stessa risposta: `selection.wikilink` avvolge **ogni** punto (una `EditRequest` sola, quindi tutti o nessuno e un solo undo) e dice **quante**, perché i cursori vuoti restano fuori ma non di nascosto; `note.task.toggle` legge la **primaria** e non è la stessa sottrazione — la sua posizione è un *argomento* scalare in una `CommandSpec` pubblicata, e farne una lista è una decisione di firma che non si prende di straforo (**è la casella residua**); il pannello statistiche **somma e dice quanti punti**, contando i testi uno per uno prima di sommarli perché concatenarli attaccherebbe l'ultima parola di una alla prima dell'altra; l'outline segna la sezione della primaria, perché in tre punti si *seleziona* ma in uno solo si *sta*; `Session::invalidate` le azzera tutte, che è la stessa cosa che dice il tipo. `changes()` guarda l'**insieme**: muovere uno di N cursori invecchia le view che seguono la selezione, ed è voluto e scritto. **Il costo misurato invece che dichiarato**: `paneContext()` gira a ogni battuta di tastiera e `charToByteIndex` è una scansione dall'inizio, quindi 2N estremità sarebbero N attraversamenti dello stesso documento per tasto premuto — `charToByteIndices` ordina una volta e percorre una volta, e N cursori costano quanto uno. **Il ritaglio** è scritto: `frozen/0.1.0.wit` porta il paragrafo e [wit-congelato](../architecture/wit-congelato.md) la riga — e dice una cosa che vale più della facciata, cioè che la 0007 questo ritaglio lo aveva **previsto più piccolo di com'è**: *una rinuncia dichiarata non è una rinuncia dimensionata* | 2026-08-04 |
| [0094](0094-un-tetto-che-si-fa-sentire.md) | Un tetto che si fa sentire, e un permesso che smette di travestirsi da dato | §23.12 — **chiude la voce**, e con lei l'ultima P0 prima del freeze di M4. `random-bytes` era `func(n: u32) -> list<u8>`: la [0039](0039-il-locale-e-il-caso.md) le aveva messo un tetto (`MAX_RANDOM_BYTES = 1024`) e aveva scelto di applicarlo **zitto**, quindi chi ne chiedeva quattromila ne riceveva mille e l'unico modo di saperlo era misurare la lunghezza di ciò che era tornato. Adesso rende `result<list<u8>, plugin-error>` e **chi riceve `ok` riceve esattamente `n` byte**. **La premessa della voce era incompleta**, ed è la settima volta di fila (0087–0094) che rileggerla contro il sorgente la cambia prima di progettarla: i significati non erano due ma **tre**. `host/guard.rs` rispondeva `Vec::new()` a chi non aveva `Capability::Env` — col commento «il vuoto è la sola risposta onesta» — e l'argomento è giusto ma confrontato col nemico sbagliato: il vuoto non competeva con dei byte fissi, competeva col **dire di no**, e arrivava a chi chiama indistinguibile dal troncamento. I due si correggono in modi **opposti** («hai chiesto troppo» si corregge chiedendo meno, «non ti è permesso» non si corregge affatto), quindi il vuoto era una **politica travestita da dato** — la famiglia della [0013](0013-elenco-delle-capacita.md) e della [0021](0021-il-confine.md), non quella di un tetto, ed è il difetto più grave dei due. Il doppio dell'SDK lo aveva perfino istituzionalizzato con un costruttore `senza_entropia()` che lo presidiava. **Nessun tipo nuovo entra**: `bad-args` e `permission-denied` erano già le due frasi giuste, e un `random-error` suo — la quarta strada, valutata — avrebbe voluto una localizzazione sua e un rendering suo contro la [0041](0041-un-errore-e-testo-che-qualcuno-legge.md). **Il tetto NON entra nel contratto**, ed è la metà che si vede meno: un numero pubblicato è una promessa congelata, mentre ciò che il confine deve garantire è che superarlo si **dica** — la forma dell'`overflow` della [0034](0034-il-freno-e-il-raggruppamento.md), dove si pubblica la perdita e non la soglia, ed è la risposta alla terza casella su quale delle due forme sia *la* regola. Così 1024 resta alzabile senza rompere nessuno. **Il criterio della 0039 non è rovesciato**: proteggeva che un id legittimo non smettesse di nascere, e le tre forme chiedono 16, 10 e `len` byte — tutte sotto il tetto, compreso il caso esatto di `MAX_RANDOM_BYTES` che i test asseriscono. Confondeva «non fallire» con «non dirlo». **Un censimento fermo da settantatré verbali**: `guard.rs` intitolava «Le cinque capacità che non sanno dire di no» e `traits.md` ne diceva sei, ma quelle senza `Result` erano **sette** — `user_locale` e `random_bytes`, nate con la 0039 *dopo* che il conto era stato fatto dalla 0021, non ci si erano mai aggiunte. La regola era scritta e il censimento che la faceva rispettare no. `user_locale` **resta muta a ragione**, e la differenza fra i due fallback è il criterio che ne esce: *un fallback muto è onesto quando la risposta nulla è già un caso del dominio* (il locale di default **è** la risposta del contratto per «nessuno me l'ha detto») *e disonesto quando è un valore inventato per occupare il posto di un errore* (nessuna semantica dice «zero byte di caso»). **Un guadagno fuori piano**: `JobHost` può ora delegare da `read_result`, quindi la cancellazione della [0032](0032-il-runner-dei-job.md) arriva anche qui — prima un job annullato che chiedeva byte li riceveva, perché la firma non aveva un posto in cui dire di no. Una capacità senza esito non è solo una che non si può negare: è una che non si può **fermare**. **Il presidio**: `the_ceiling_holds_and_does_not_fail` asseriva la frase esatta che la voce contesta — che il tetto reggesse *senza fallire* — ed è diventato `the_ceiling_says_no_instead_of_truncating`; due test nuovi nel `Guard`, che servivano lì perché `ReadOnly` **concede** `Env` e il presidio delle capacità simulate non la esercita. **Il ritaglio** è scritto: paragrafo in `frozen/0.1.0.wit` accanto a quelli che c'erano e riga in [wit-congelato](../architecture/wit-congelato.md) — la terza della seduta, e la più piccola dei tre ritagli di firma | 2026-08-04 |
| [0095](0095-cosa-guardo-e-cosa-sto-scrivendo.md) | Cosa guardo e cosa sto scrivendo sono due domande | §23.5 — **chiude la voce**, ed è la prima presa senza che a ordinarla fosse il freeze: le P0 sono finite con la 0094, e questa aveva un **ordine scritto dentro di sé** (va decisa prima della §23.3). `Capability::Env` era «sapere che ore sono e cosa guarda l'utente» con `permission()` a `None`; adesso sono **tre** famiglie — `Env` (orologio e caso, nessun permesso, sono fatti della **macchina**), `Session` (`fub:read-session`) e `SessionSelection` (`fub:read-selection`) — e `active_context()` è **il solo metodo del contratto con due cancelli**: senza `Session` rende `None`, con `Session` e senza `SessionSelection` rende il contesto con `selections: None`. Le famiglie del `Guard` passano da quattordici a **sedici**; nessuna firma cambia, quindi **nessun ritaglio**. **Le tre premesse della voce sono cadute tutte e tre**, ed è la nona rimisurazione di fila (0087–0095): il tipo che nomina non esiste più — la [0093](0093-le-selezioni-sono-n-e-il-buffer-e-uno.md) l'ha smontato tre commit fa — e la voce ne esce **peggiorata**, perché dove passava un testo adesso ne passano N (non «cosa c'è selezionato» ogni 150 ms, ma *tutti i punti in cui l'utente sta lavorando*); il **prezzo che dichiarava non esiste**, perché `stats.rs` chiama `read_document` per contare le parole e ha `read-vault` da sempre — e con lui cade l'unico argomento che la voce dava per la famiglia separata, che resta comunque la scelta giusta per un'altra ragione; l'**aggravante sull'allowlist non è vera oggi**, perché nessun parametro viene letto da nessuno (è la casella del §7.1), e la ragione che la voce ne dà si **rovescia**: a differenza di `Query`, un `ViewContext` un path da confrontare ce l'ha ed è `doc`, quindi la sessione è filtrabile per prefisso e il canale dati no — ritrovamento scritto nella casella del [§7.1](../roadmap/07-il-confine.md#la-casella-rimasta). **Perché non `read-vault`**, che è la strada che la voce raccomandava per prima *perché non inventa niente*: il costo di non inventare niente non era un nome in più nell'elenco, era **la scomparsa della scelta** — appoggiarcela rende impossibile dire «può cercare nelle mie note, non può leggere quello che sto scrivendo adesso». Un permesso riusato è economico finché la sua grana è quella giusta. **Due permessi e non uno per una misura**: `ViewContext.pane` ha **zero lettori** in tutto il repo e `mode` ne ha uno che vuole tutto il resto — quindi tenerli fuori dal cancello sarebbe il caso che nessuno usa, rifiutato sei volte di fila da 0077, 0090, 0091, 0092, 0093 e 0094 — mentre `doc` e `selections` hanno due gruppi di clienti veri (indice della nota e backlink vogliono la nota, contatore e wikilink vogliono il testo). Il `DocId` **cade col resto**: è un path, i path sono ciò che `read-vault` governa, e un diario si tradisce anche solo dai titoli. **Una clausola nuova al criterio della 0094**: qui la risposta nulla non è quella vera (`selections: None` significa già «nessun cursore»), eppure il fallback muto regge — *un fallback muto è onesto anche quando la risposta nulla non è quella vera, purché chi la legge abbia già in mano il motivo*, e un manifest è l'unico posto in cui questo capita. **L'invariante «una famiglia, un trait» cambia forma**: `HostEnv` ne porta tre, perché le tre cose escono da una firma sola e un trait in più non spacca un record in due — la scomposizione della [0021](0021-il-confine.md) qui non era disponibile, e spaccare il record (la terza opzione) costava una firma pubblicata prima del freeze per una simmetria interna. Diventa **«nessun trait senza almeno una famiglia»**, che è ciò che il compilatore presidiava davvero. Contando è saltato fuori che a sedici i bit dell'`u16` di `CapabilitySet` sono **finiti esattamente**: la diciassettesima famiglia vuole un `u32`, e senza la riga che lo dice `1 << cap` andrebbe in overflow — in debug con un panic, in release in silenzio. **Il WIT è toccato solo nei commenti**, perché ne portava uno diventato falso: `interface host-env` diceva che orologio e contesto *«si negano insieme»*, cioè descriveva il difetto al confine come se fosse voluto; adesso dice che **un'interfaccia non è un cancello, il cancello è il permesso**. **Il presidio**: `denying_the_selection_leaves_the_note_visible` è la voce intera in un nome, e `the_clock_and_the_session_are_no_longer_the_same_gate` è il test che ieri era **impossibile**. Resta fuori il pannello che i permessi li mostra — è il pezzetto di §7.6 senza lettore, come `registeredPanels()` per la [0042](0042-il-catalogo-della-shell.md) | 2026-08-04 |
| [0096](0096-una-bozza-non-e-una-nota.md) | Una bozza non è una nota | §23.10 — **chiude la voce**, ed è la seconda presa senza che a ordinarla fosse il freeze. `IndexQuery::Drafts` passava da `Capability::Query`, cioè da `fub:read-vault`: **qualunque plugin che potesse leggere un documento salvato poteva leggere ciò che l'utente stava scrivendo in quel momento**, con la stessa spunta nello stesso manifest. Adesso c'è `Capability::Drafts` con `fub:read-drafts`, e quella variante passa **di lì e non da `Query`**. Famiglie da sedici a **diciassette**; nessuna firma cambia, quindi **nessun ritaglio** e il WIT non è toccato nemmeno nei commenti (un permesso è una chiave di `option-map`, non un tipo — è la forma che la [0017](0017-chi-disegna-cio-che-il-core-non-conosce.md) ha scelto proprio perché l'insieme dei permessi non è chiuso). **La voce chiedeva di essere decisa insieme alla §23.5 e non lo è stata**: la 0095 l'ha chiusa da sola un commit fa, quindi l'*insieme* era già saltato e questa è la «seconda volta» che la prima casella prevedeva. È un **quarto modo** in cui una voce invecchia, accanto ai tre che la seduta aveva censito — *una voce che dichiara un ordine e viene scavalcata da quella con cui doveva stare insieme* — e la lezione è che **un ordine scritto in una voce va cercato anche nelle voci che la nominano**, non solo in quella che si sta per fare. **Il caso migliore c'era davvero**, ed è stato contato invece che assunto: di `IndexQuery::Drafts` esistono chi la serve, chi la conta in manutenzione, i campioni del mirror e due test — **nessuna feature ufficiale, nessun `ViewProvider` registrato**. Il cliente vero è il recupero dopo un crash, che è della **shell**, e la shell chiama `ws.query_index` diretto sul workspace **senza `Guard`**: non è un plugin. `PluginPermissions::core()` resta invariata, perché il core le bozze non le legge e un permesso preso «per completezza» è ciò che la riga di `write-settings` lì accanto ha già rifiutato. **Prezzo misurato: zero righe di chiamante.** **Una classe a sé, per tre ragioni**: la 0088 scrive che *«il testo che l'utente non ha ancora salvato è il dato più privato che un vault contenga»* e da lì nega la scrittura **per sempre**, ma l'argomento con cui concedeva la lettura — *«leggere non è cambiare»* ([0085](0085-leggere-non-e-cambiare.md)) — protegge l'**integrità** mentre la minaccia che quella frase nomina è la **riservatezza**, e contro quella leggere è il verbo che conta; un documento salvato lo si legge **nominandolo**, mentre `Drafts` non prende un `DocId` e rende **tutte le bozze col testo dentro** (la stessa aggravante che la 0095 ha trovato per la selezione, da un'altra strada); e una bozza è **l'unica copia**, quindi *un permesso di troppo costa a un plugin una riga di manifest, un permesso di meno costa all'utente il testo che stava scrivendo*. **La casella che non regge, ed è l'unica**: diceva che se è una classe a sé «il permesso è uno solo e copre entrambe», cioè insieme alla selezione. No — i permessi della 0095 governano la **sessione** (vivo quanto il pannello col focus, pubblicato a ogni movimento del cursore), una bozza **sta sul disco**, **sopravvive alla sessione** e **non riguarda la nota aperta**: riusarli sarebbe lo stesso fuori-grana che la 0095 ha rifiutato per `read-vault`. **Al posto di `Query`, non sopra**, ed è la scelta di prodotto: cumulativa era la forma ovvia (la simmetria di `undo_last`, due `check` su un metodo solo) e si è scartata contando le frasi che l'utente può dire — al posto se ne dicono **due** (*«puoi cercare nelle mie note, non ciò che sto scrivendo adesso»* e *«puoi ritrovare ciò che non ho salvato, il resto del vault no»*), sopra una sola, e la seconda **è l'unico cliente che questa domanda abbia mai avuto**: un recupero vuole le bozze e nient'altro, e `DraftInfo` porta già `exists`, `base` e `current`. Che non sia una **scala** verso il vault è presidiato. Il cancello è comunque strettamente una **sottrazione**: nessun plugin guadagna niente. **Il `Guard` legge un argomento per la prima volta**, e non spacca il canale dati della [0019](0019-il-canale-dati.md) né allarga la `Policy`, che continua a rispondere a nomi: è la mossa di `undo_last` applicata a una richiesta invece che a un effetto. `query_capability` è **esaustiva su `QueryKind` e senza `_`** — una famiglia di query nuova non compila finché non si è detto sotto quale permesso passa, perché con un ramo di scarto sarebbe atterrata su `Query` restando verde, che è **esattamente** come `Drafts` ci è atterrata e ci è restata per otto verbali. **I bit sono finiti**: `CapabilitySet` era un `u16` e a sedici erano esattamente tutti, quindi la diciassettesima famiglia lo porta a `u32` — senza il cambio `1 << cap` sarebbe andato in overflow, in debug con un panic e **in release in silenzio**, cioè concedendo una famiglia a chi non l'aveva dichiarata. **A vederlo è stato un presidio scritto ieri**, l'`assert` in coda a `i_discriminanti_coprono_ogni_famiglia` che la 0095 aveva messo *perché* i bit stavano finendo: ha fatto il suo mestiere una riga prima del danno, e adesso dice `u32::BITS` con accanto *allargare il tipo, non togliere l'assert*. Ed è meccanico per una proprietà che va scritta: `CapabilitySet` **non si persiste** — si ricalcola da `ALL` a ogni registrazione — quindi non c'è una migrazione dietro, e il giorno che lo si salvasse quella proprietà se ne andrebbe con la prima riga. **Il presidio**: `denying_drafts_leaves_the_rest_of_the_index_readable` e `granting_drafts_alone_does_not_open_the_index` sono i due versi della leva, e il secondo è quello che la forma cumulativa avrebbe reso impossibile; `the_two_permissions_are_not_the_same_key` verifica che `read-drafts` non sia un nome nuovo davanti al cancello vecchio. Il presidio delle capacità simulate non ha voluto una riga, perché calcola l'insieme atteso da `ALL` filtrato da `ReadOnly` e `Drafts` è **concessa** in simulazione — leggere non è un effetto. **Non è una difesa da un plugin ostile** (registro della seduta 22): a M4 un nativo gira in-process, e il valore del cancello è la **dichiarazione**, non l'imposizione. Resta fuori il pannello che i permessi li mostra — `fub:read-drafts` è comparso in `PluginInfo` da solo, ma nessuna superficie lo rende ancora leggibile a chi decide — e un'allowlist di prefissi su questo permesso, che è del [§7.1](../roadmap/07-il-confine.md#la-casella-rimasta) | 2026-08-04 |
| [0097](0097-un-recinto-che-vale-anche-quando-nessuno-guarda.md) | Un recinto che vale anche quando nessuno guarda | §23.3 — **chiude la voce**, meno una casella residua nominata, ed è la terza presa senza che a ordinarla fosse il freeze. La [0013](0013-elenco-delle-capacita.md) aveva tenuto `http_fetch` fuori con **due bloccanti nominati** — *«servono prima §9.1 perché sia utile e §7.3 perché sia sicura»* — ed erano caduti tutti e due, il primo con la [0027](0027-il-lavoro-lungo-vede-il-vault.md) e il secondo con la [0021](0021-il-confine.md), che aveva scritto perfino la riga d'innesto: **nessuno se n'era accorto per settantasei verbali**. Adesso `HostNetwork::fetch` è la quindicesima famiglia del contratto e la **diciottesima** del `Guard`. Ma la riga che vale il verbale è un'altra: **`fub:network` porta come parametro una allowlist di host, e da questo commit quell'elenco si legge** — è il **primo parametro di permesso che questo repo onori**, dopo che la [0017](0017-chi-disegna-cio-che-il-core-non-conosce.md) gli aveva dato la forma e ottanta verbali l'avevano lasciato lì come un dato che nessuno interrogava. **Perché da qui e non dai prefissi di path** (la casella del [§7.1](../roadmap/07-il-confine.md#la-casella-rimasta), più vecchia e più citata): i due divari non sono la stessa cosa — un `read-vault` ristretto a `Progetti/` che legge tutto è un recinto che perde, additivo e con un criterio già scritto; un manifest che dice «mi connetto solo a api.acme.com», **mostrato all'utente** e **accettato dall'utente**, che poi consenta qualunque host è una **frase falsa scritta dall'app**. La differenza è chi ha letto la promessa: un prefisso è una restrizione che un plugin si dà, un host è una destinazione che l'utente ha approvato — e mentire a un firmatario è di un'altra specie. Più una ragione di sequenza: la capacità **nasce adesso**, e farla nascere col parametro già onorato costa quanto farla nascere senza. **`Policy::denies_host` è stretta di proposito**, e la firma generica «questo bersaglio, per questa famiglia» è stata scartata due volte: perché sarebbe la firma preparata senza chiamante che 0077→0096 rifiutano da otto verbali, e perché sarebbe stata **sbagliata** — un path si confronta per prefisso dentro una radice che è dell'utente, un host per nome dentro uno spazio che non è di nessuno, quindi una funzione sola avrebbe avuto due semantiche, che è **esattamente** il timore che la 0021 scrive nel suo bloccante (*non nascere con due idee di cosa sia un filtro*). Il timore non si applica a due funzioni su domini disgiunti; si sarebbe applicato in pieno alla firma che sembrava prepararlo. **Tre righe rendono l'allowlist vera invece che decorativa**, e la voce non ne nominava nessuna: **i redirect non si seguono** — un host dichiarato che risponde `302` verso uno che non lo è porterebbe fuori dal recinto senza che nessuno l'abbia deciso, e un client che li segue lo farebbe **in silenzio** perché l'allowlist non ce l'ha né deve averla; qui il `3xx` torna a chi ha chiesto e seguirlo è una **seconda chiamata**, che ripassa dal cancello —, **`*.` è obbligatorio** perché una `ends_with` nuda regalerebbe a chi dichiara `acme.com` anche `evil-acme.com`, che è il dominio di qualcun altro, e **le credenziali si scartano** perché `https://api.acme.com@evil.example/` fa leggere due indirizzi diversi a un umano e a una macchina. **`http` in chiaro è rifiutato tranne verso l'anello locale**, e l'eccezione è una scelta di prodotto e non una comodità: `http://localhost:11434` è dove gira un **modello locale**, cioè l'unico modo di usare l'AI senza mandare le proprie note a qualcuno — la regola scritta per proteggere l'utente lo avrebbe spinto verso la scelta peggiore per lui. **`fub:network` senza parametro = qualunque host**, e ribaltarlo è stato scartato per iscritto: sarebbe la sola chiave la cui assenza di parametro significa il contrario che altrove, cioè romperebbe la proprietà per cui una mappa sola governa quattro sedi (*chi ne impara una le sa tutte*); ciò che cambia resta nella **frase che l'utente legge accettando**. **Il dry-run la nega**: là un servizio esce dalla simulazione perché gira con le capacità di chi lo offre, qui **l'effetto non è nell'host** — un `POST` crea qualcosa dall'altra parte e perfino un `GET` viene contato e fatturato da chi risponde, cioè la sola specie di effetto che questo processo non può ritirare nemmeno volendo (`run_command` resta concesso: è una catena che l'host governa, non un mondo che non conosce). **Byte e non testo, che è la [0087](0087-il-testo-che-sta-dentro-gli-allegati.md) letta al contrario** per una differenza vera — un file sul disco non dice di che codifica è, **una risposta HTTP sì** — ma metà della rete risponde `image/png`, quindi un corpo `string` costringerebbe l'host a decidere per tutti; il `content-type` sta **fra gli header**, e un campo accanto sarebbe lo stesso dato in due posti (la trappola che la [0007](0007-contesto-di-sessione.md) descrive per `active-document`). **Un `4xx` è `Ok`**: l'errore è *non aver potuto chiedere*, ed è la distinzione della [0041](0041-un-errore-e-testo-che-qualcuno-legge.md) applicata al filo. **I tre ritrovamenti**: (1) **la rete è la prima capacità la cui durata non la governa l'host**, e la disciplina del prestito che regge per tutte le altre qui si rompe — ogni riga di `jobs.rs` delega dentro `reading`/`writing`, microscopico per una lettura, ma una `fetch` dura quanto la rete e tenere il lock affamerebbe **chi scrive**, che è il difetto contro cui la [0024](0024-chi-legge-non-aspetta-chi-legge.md) ha scelto l'`RwLock`, su una chiamata **che il vault non lo tocca affatto**; quindi il lock si prende per un istante e si molla, il `Guard` è lo stesso di tutti gli altri montato fuori, e il permesso si **rilegge adesso** invece di catturarlo all'avvio del job (`&self` e non `&mut self` è la proprietà che lo permette) — **resta la casella**: fermare la richiesta **già partita**, che oggi aspetta il tetto di tempo dell'host; (2) **il presidio ha preteso la riga da solo** — `every_structural_capability_is_refused_by_the_same_gate` è diventato rosso appena `ReadOnly` ha negato `Network`, seconda volta in due commit dopo l'`assert` sui bit dell'`u16` della 0096, ed è il **primo giro in cui la riscrittura della 0094 ha pagato**: con la copia dell'insieme atteso invece del calcolo da `ALL`, aggiungere una famiglia avrebbe lasciato il test verde e il buco aperto; (3) **`reqwest` era già nel lockfile e si è scelto `ureq` lo stesso** — ce lo porta `tauri`, quindi sta in `fub-app`; riusarlo costerebbe meno lock e **di più in tutto il resto**, perché è asincrono e vorrebbe `tokio` dentro `fub-host`, che non ha un runtime e non ne vuole uno per una capacità sola, mentre `ureq` è bloccante ed è la forma giusta per un thread del pool che sta lì **apposta per aspettare**. Conto **misurato e scritto nel `Cargo.toml`**: venti pacchetti (551 → 571), il doppio di `jiff`, la dipendenza più cara che questo workspace abbia preso — e otto dei venti sono `platform-verifier`, scelto contro `webpki-roots` perché con le radici imbarcate un utente dietro una **CA aziendale** scoprirebbe che «solo Fub» non si connette **senza nessun modo di rimediare dall'app**: otto pacchetti per non togliergli una decisione che ha già preso sul proprio computer. `gzip` è acceso e costa **zero pacchetti** (`flate2` c'era già): senza, `ureq` non chiede la compressione **e non la sa disfare**, e un plugin che si scrivesse l'header da sé riceverebbe byte che il contratto non ha modo di decodificare; `cookies` resta fuori perché in un client condiviso sarebbe stato che passa **fra plugin diversi** senza che nessuno l'abbia dichiarato. **Nessun ritaglio**: interfaccia nuova, tipi nuovi, nessuna firma toccata — e l'argomento non è che `wit_additivity` sia verde (quello è il segnale), è che il freeze chiude l'elenco delle capacità **alla sottrazione, non alla crescita**. **Non è una difesa da un plugin ostile** (registro della seduta 22): a M4 un nativo gira in-process e `std::net` non passa dal `Guard` — il valore del cancello è la **dichiarazione**. E qui quella dichiarazione **non la legge nessuno**: `fub:network` con la sua allowlist è comparso in `PluginInfo` da solo, terza volta di fila dopo 0095 e 0096, e pesa più delle altre due perché quelli erano permessi binari mentre questo ha **un parametro che è il permesso stesso** — quindi smette di essere una riga in fondo a un verbale e diventa la **§23.17** contata in todo.md | 2026-08-04 |
| [0098](0098-un-permesso-si-vede-e-si-nega.md) | Un permesso si vede, e si nega uno per uno | §23.17 — **chiude la voce**, e chiude insieme una riga che quattro verbali avevano scritto in fondo a sé stessi: *«resta fuori il pannello che i permessi li mostra»*. I tredici permessi che un manifest può dichiarare adesso **si leggono** nella scheda dei componenti, uno per riga e in italiano, col parametro di fianco quando ne portano uno che si onori — e ognuno ha un interruttore. **Una chiave d'impostazione per ogni coppia componente-permesso**, fabbricata dal kernel e con `true` come default: è la [0077](0077-una-scorciatoia-e-una-chiave.md) applicata a un'altra domanda, e le due alternative scartate sono le stesse (una lista `"com.acme fub:network"` è un formato dentro un formato, un `SettingKind::Map` è firma a ridosso del freeze). La terza ragione, che là non c'era, è ciò che ha reso questa voce piccola: negare un permesso **eredita da solo** la provenienza del valore, l'azzeramento — che è come si *riconcede*, quindi non c'è un secondo comando —, l'evento che avvisa le altre finestre, il non essere scrivibile da un programma e il file in cui vive. **Zero righe nuove per cinque comportamenti.** L'asimmetria con le scorciatoie è scritta: un id di comando è unico da sé, un nome di permesso è lo **stesso per tutti**, quindi il componente deve entrare nella chiave e l'unico posto che il §7.4 gli lascia è la fessura del namespace — ne segue che **anche una feature ufficiale nomina col proprio id**, l'unico posto del repo in cui il core non usa la licenza di nominare nudo (che esiste per le chiavi *dell'applicazione*, e qui non c'è niente dell'applicazione: ogni permesso è di esattamente un componente). **La chiave la possiede il componente, e il valore gli sopravvive**: spegnerlo ne toglie lo schema, quindi la chiave sparisce — se sparisse anche il valore, spegnere e riaccendere sarebbe il modo di **ridargli tutto**, un giro che si fa con due clic e per sbaglio. Non sparisce, perché togliere uno schema non è cancellare un valore, ed è il presidio più importante del commit (le tre occasioni in fila: negare, spegnere e riaccendere, riaprire il vault). **La negazione è una sottrazione sulla mappa del manifest**, non un secondo elenco letto accanto, e le tre proprietà che ne discendono un campo `denied` dentro la politica non le avrebbe avute: non può **concedere** (una mappa a cui si tolgono chiavi non ne acquista — quindi nemmeno un vault che arriva da fuori, ed è perché questa chiave può vivere nel vault senza aspettare la §23.13: il caso peggiore è il default che l'utente ha già accettato); nega insieme il *se* e il **dove** (tolta `fub:network` cade l'allowlist con lei, e un elenco parallelo avrebbe avuto il caso in cui i due non sono d'accordo — che per quella chiave significa *qualunque host*); e non c'è un secondo ordine di cancelli da allineare. **Vale adesso**, ed è la 0097 letta dalla parte opposta: là si rilegge il permesso a ogni chiamata perché la politica può cambiare, qui si riscrive la politica nel momento in cui cambia — e il ricalcolo sta in `announce_setting` e non nei due chiamanti, perché *azzerare una chiave negata è precisamente il modo in cui si riconcede*, e sta **prima** dell'evento perché chi lo riceve può chiamare. **La frase la scrive la shell**, e questa è la riga di sicurezza: l'etichetta della spec fabbricata è la **chiave** del permesso, non una frase, perché un `Text` di quella spec si risolverebbe nel catalogo del suo proprietario (§12.1) — cioè un componente potrebbe presentare `fub:read-drafts` come «migliora i suggerimenti», l'unica riga dell'app in cui il testo che protegge l'utente lo scriverebbe la parte da cui lo si protegge. Il costo sono due elenchi, tenuti insieme da un presidio che legge **il file della shell** e lo confronta col contratto (terzo della specie dopo il tema e la memoria, e con la posta più alta: un permesso che il contratto conosce e la shell no è un permesso *che nessuno mostra*). **Il parametro si mostra e non si edita**, e la ragione è un trabocchetto misurato: in `Granted::new` un elenco vuoto significa *qualunque host*, quindi una UI che lasciasse togliere gli host uno per uno trasformerebbe «nessuno» in «tutti» **al gesto in cui qualcuno sta chiudendo**; restringere è la casella del §7.1, che eredita il caso da non sbagliare. **La rilettura ha cambiato la voce in due punti**: una misura che la voce chiedeva era già sbagliata *dentro la voce* — i permessi con un parametro sono **quattro** e non uno, e ciò che di `fub:network` è unico è che il parametro **lo legge qualcuno** —, e delle due strade da scegliere (accettazione all'installazione, o ispezione nelle impostazioni) la prima **non esiste**: a M4 non c'è un'installazione, e un momento di accettazione senza un momento di arrivo sarebbe teatro, del genere che insegna a cliccare «accetto». Nascerà col caricatore di M5, da questo stesso elenco e con queste stesse frasi. **Non è una difesa da un componente ostile** (registro di 0095, 0096 e 0097: a M4 un nativo gira in-process e `std::fs` non passa dal `Guard` — il valore del cancello è la dichiarazione), e **non è un modo di spegnere un componente**: l'interruttore che lo spegne è accanto, ed è la ragione per cui negare un permesso a una feature ufficiale non ha avuto bisogno di un'eccezione — **spegnerla era già permesso, e negarle un permesso è meno che spegnerla**. Nessuna firma toccata, **nessun ritaglio** | 2026-08-04 |
| [0099](0099-una-rinomina-che-non-ha-visto-nessuno.md) | Una rinomina che non ha visto nessuno | §23.1 — **chiude la voce**. Chi rinomina una nota mentre Fub è **chiuso** non ha nessuno che accoppi i due path: alla riapertura una nota risulta sparita e ne risulta nata un'altra, e lo spazio per-documento, le versioni, l'icona e — l'unica copia di un testo mai salvato — la **bozza** restano attaccate a un nome che non esiste più. Adesso l'apertura **riconosce dal contenuto** ciò che il rilevatore non ha potuto vedere. **La terza strada che la [0043](0043-il-path-e-la-chiave.md) non aveva guardato**: quel verbale ha scartato *un'* implementazione dell'id stabile — la tabella `path → id` — e ne ha concluso giustamente che è «il path con un costume addosso»; ma la riassociazione non deve passare da un id, passa dal **contenuto**, e il materiale era già tutto su disco per opera di due decisioni che non si nominano a vicenda (l'anagrafe durevole della [0046](0046-l-anagrafe-del-vault.md) porta l'impronta di ieri, `index_batch` calcola quella di oggi leggendo — `Revision::of_bytes`, [0087](0087-il-testo-che-sta-dentro-gli-allegati.md)). Costo in letture di disco: **zero**, ed è ciò che ha permesso di farlo all'apertura invece che dietro un comando. **Le tre regole**: *uno a uno o niente* (due file identici comparsi senza che sparisse niente sono una copia; con N spariti e N comparsi l'accoppiamento non è unico); *nel dubbio non si accoppia*, che è il verso **opposto** alla [0085](0085-leggere-non-e-cambiare.md) — là una rilettura di troppo costa un file aperto, qui un accoppiamento sbagliato consegna il testo non salvato di una nota a un'altra, e **la regola del dubbio non è una costante del repo, è una funzione di cosa costa sbagliare**; e la terza, che la voce non prevedeva, *nel dubbio non si **raccoglie** nemmeno* — perché non accoppiare avrebbe voluto dire lasciare quei dati alla raccolta tre righe sotto, cioè a un `remove_dir_all`: delle due mosse, quella irreversibile si sospende. **Un difetto trovato accanto, peggiore della voce**: `finish_index` non riconciliava un'indicizzazione interrotta («un insieme incompleto non si dichiara completo») e tre righe sotto **raccoglieva comunque** — quindi «annulla» sulla prima indicizzazione di un vault grande, o l'app chiusa mentre gira, toglieva dal disco lo spazio per-documento di ogni nota non ancora guardata. Due righe difendibili separatamente il cui prodotto non aveva guardato nessuno (la forma della §23.3 dentro una funzione sola), con l'asimmetria dalla parte sbagliata: **riconciliare su un insieme parziale svuota un derivato, raccogliere su un insieme parziale cancella dati che nessuno rifà**. La guardia sta ora dentro la raccolta e non nel chiamante, e `Ready` essendo il default non chiude la porta a chi non ha aperto niente. **L'ordine delle due righe è la decisione**: prima si riconosce, poi si raccoglie — per la raccolta una rinomina ad app chiusa è indistinguibile da una cancellazione, e invertirle vorrebbe dire cancellare i dati un istante prima di sapere di chi sono. **Fuori dall'accoppiamento e non per dimenticanza**: il file vuoto (zero byte non sono una prova, sono l'assenza di una prova — ed è la nota appena creata, cioè quella su cui una bozza è l'unica copia), il cestino (una nota cestinata non è sparita), gli allegati (un'impronta non ce l'hanno **affatto**: è la casella del §14.1, che da oggi ha un **cliente** invece di una giustificazione) e un'anagrafe illeggibile (il ricongiungimento è una capacità di un derivato, e con lui si perde — per un giro, e in silenzio). Ciò che il kernel sposta è **lo stesso codice** della rinomina vista, estratto in `migrate_side_data`: tenerlo in due copie sarebbe stato il difetto che la [0044](0044-lo-stato-per-documento.md) ha appena finito di togliere, rifatto *dentro* il kernel. Chi tiene stato fuori dallo spazio dichiarato — il versioning — lo viene a sapere dallo **stesso** `Event::DocumentRenamed` della rinomina vista, e i tre dati autorevoli si spostano **prima** perché la coda può troncare ([0034](0034-il-freno-e-il-raggruppamento.md)). Nessuna notifica, di proposito: l'esito riuscito è che **niente sembra storto**. Nessuna firma toccata, **nessun ritaglio** | 2026-08-05 |
| [0100](0100-i-tasti-che-arrivano-da-fuori.md) | I tasti che arrivano da fuori | §23.13 — **chiude la voce**. La [0076](0076-le-impostazioni-vivono-nel-vault.md) aveva smontato l'argomento di rischio sulle impostazioni del vault con una frase che quel giorno era vera — *«visibile e reversibile»* —, e la [0077](0077-una-scorciatoia-e-una-chiave.md) ha poi fatto di una scorciatoia una chiave come le altre, dentro il vault. Il prodotto delle due non l'aveva guardato nessuno: **un vault che arriva da fuori rimappa i tasti di chi lo apre**, e un tema sbagliato si vede mentre una scorciatoia spostata si scopre premendola. **Due delle tre strade della voce sono cadute rileggendola**: `per_machine()` farebbe **mentire il vocabolario** (oggi `Machine` vuol dire «il valore del vault è ignorato con un avviso», e la 0076 aveva riservato quella parola a ciò che «non può dipendere da un vault aperto», falso di una scorciatoia); e il «livello macchina vince» la voce lo dà per esistente — *«già la forma dei due livelli della 0036 letta al contrario»* — ma la **0076 quella scala l'ha cancellata apposta**, quindi rimetterla per una famiglia sola è riscrivere «prima guardo qui, poi lì». Anche il criterio della terza era sbagliato: «un vault **mai visto**» non è la domanda, perché un vault aperto ieri riceve tasti nuovi stanotte da un sync e la riga nel registro nasce alla prima apertura. La domanda giusta è *«ho già visto **questi** tasti»*, confrontando il valore di ieri con la lettura di oggi — la forma della [0099](0099-una-rinomina-che-non-ha-visto-nessuno.md). Adesso una scorciatoia che questa macchina non ha mai visto è **sospesa**: il valore sta nel file, il file non si tocca, `resolve` risponde `Default` finché nessuno decide. Il criterio che la voce chiedeva non taglia in due ma in **tre specie**, per che cosa può fare il valore peggiore: *una sottrazione non concede* (`plugins.disabled`, i permessi della [0098](0098-un-permesso-si-vede-e-si-nega.md) — il peggio è il default già accettato), *si vede e si disfa* (tema, `locale.*`, pesi della ricerca), *cambia cosa fa un gesto dell'utente* (oggi solo `keys.*`). **Il prezzo misurato ribalta il senso della voce**: `vault.undo` è l'**unico** comando che dichiari una scorciatoia, quindi un vault estraneo non ruba un tasto — **arma** comandi che non ne avevano, e `trash.empty` non ha parametri e porta `.irreversible()`. La risposta si dà **una chiave alla volta**: si adottano *le chiavi mostrate* e non l'insieme dei sospesi, rifiutare le **toglie dal file**, e scrivere una chiave risveglia quella e nient'altro. Chi sospende non è il kernel ma `fub-host`, l'unico che vede insieme il file del vault e `keys_seen` nel registro — l'unico campo che non descrive il vault ma il rapporto fra quella macchina e quel vault. Difetti trovati accanto: rimappare dal pannello **non aveva effetto** fino alla riapertura della palette (nessuno riascoltava `setting_changed`), e scrivere una scorciatoia di proprio pugno la faceva richiedere alla riapertura — il falso positivo che insegna a cliccare via le domande, corretto con una *porta della persona* nell'host. Nessuna firma cambia, nessun tipo nuovo, nessuna riga di WIT. | 2026-08-05 |
| [0101](0101-una-voce-non-e-un-passo.md) | Un'operazione a metà lo dice, e una voce di undo non è un passo | §23.14 — **chiude la voce**. Tre verbali dichiaravano lo stesso buco su tre superfici senza nominarlo come lo stesso: la [0011](0011-il-lotto.md) («se una delle N scritture fallisce le altre restano fatte»), la [0041](0041-un-errore-e-testo-che-qualcuno-legge.md) («il successo parziale non è esprimibile») e la [0045](0045-l-undo-ha-due-pile.md), la peggiore perché il danno arriva dopo. **Tre premesse della voce su cinque erano false.** I posti sono **due** e non tre: `Workspace::batch` non ha un esito, e la 0011 stessa aveva già fatto del lotto di chi chiama *la sua invocazione di comando*. «Solo due parole, riuscito e fallito» è falso: il parziale si diceva già in **tre** comandi, con tre elenchi di argomenti diversi e **solo dentro la notifica** — il buco non era che non si potesse dire, era che era prosa e non dato, coi guasti appiattiti a mano in un `format!("{doc} ({e})")` che buttava via la **specie** dell'errore. E la strada dichiarata più economica — un `Trouble` accanto all'esito, [0052](0052-cio-che-va-storto-e-un-evento.md) — è l'unica **strutturalmente incapace** di chiudere l'undo, che la voce chiamava *il caso che decide*: un evento si emette adesso, e la voce di undo si legge mesi dopo. Sceglierla avrebbe prodotto un giro chiuso per metà. Il difetto più grave stava **fuori dalla voce**, per la seconda volta dopo la [0099](0099-una-rinomina-che-non-ha-visto-nessuno.md): il `?` dentro il ciclo di `undo_last` lasciava applicati i passi fatti, **non provava** quelli dopo e rispondeva con un errore nudo — mentre la voce era già uscita dalla pila. Peggio del danno descritto: non «undici su dodici credendo dodici», ma «quattro su dodici, senza dirlo, e senza più il modo di riprovare». Adesso: `Partial { attempted, done, failures }` con i guasti **uno per uno** e il `PluginError` intero (la specie è metà dell'informazione: un `conflict` si riprova, un `permission-denied` no), `Partial::of` che risponde `None` quando non è mancato niente — un esito che si dichiara a metà senza esserlo insegna a cliccare via gli avvisi, lezione della [0100](0100-i-tasti-che-arrivano-da-fuori.md) —, il conto **appaiato dall'host** alla voce di undo nell'unico momento in cui i due pezzi sono ancora insieme (forma della [0098](0098-un-permesso-si-vede-e-si-nega.md)), e `undo_last` che restituisce un `Undone` con **due** conti: com'era l'operazione e com'è andato l'annullamento. Le risposte diventano quattro dove erano due, e **se non è cambiato niente resta un errore** — la 0094 letta su un esito. Il prezzo misurato invece che temuto: nella linea di base `command-outcome` ha due campi e `undo-last` non c'è, quindi il campo è additivo e il ritaglio non tocca lo snapshot. | 2026-08-05 |
| [0102](0102-i-byte-non-stanno-nel-record.md) | I byte di un trasferimento non stanno nel record, e leggerli è posizionale | §23.6 — **chiude la voce**. La [0006](0006-import-export-come-trait.md) dichiarava il prezzo in una riga e mezza — «sorgente e artefatti stanno in memoria, e uno `stream` al confine resta additivo» — e lo scusava con una condizione: un vault da 4 GiB non entra «finché un job non vede il vault». La [0027](0027-il-lavoro-lungo-vede-il-vault.md) quel buco l'ha chiuso, quindi **la scusa era scaduta** e nessuno era tornato a leggerla. Delle tre forme che la voce elencava, due sono **strutturalmente incapaci**, e la ragione stava già nella casella successiva della voce stessa: un contenitore zip — `.docx`, `.epub`, `.odt` e mezzo mondo dei backup — tiene la directory **in fondo**, e uno `stream<u8>` di WASI o un cursore tenuto dall'host sono tutti e due sequenziali. Chi sa solo andare avanti non sfoglia un archivio, lo scarica: una lettura di sorgente dev'essere **posizionale**. Adesso `ImportSource.content` è `bytes` **oppure** `streamed { handle, len, prologue }` — restare in memoria non è vietato, è il caso comune, ma la scelta si **dichiara** invece di essere l'unica —, il prologo di 8 KiB serve al dispatch a riconoscere una firma senza aprire un giro di letture, e `len` è ciò che l'host ha visto *prima*, quindi `read_all` si ferma su una lettura vuota e non sul conto. Dal lato export un artefatto si **versa** in un `ArtifactSink` mentre lo si produce, e c'è una strada sola: chi chiama sceglie il comportamento scegliendo il sink, così non c'è il ramo che si dimentica. `TransferRead` è la sedicesima famiglia e `Capability::Transfer` la diciannovesima; **niente permesso** (un handle non si costruisce, si riceve, e nomina la sorgente che l'utente ha appena scelto — chiedere una spunta insegnerebbe a spuntare, [0100](0100-i-tasti-che-arrivano-da-fuori.md)) e sta in **`ReadApi`**, perché servire 4 GiB sotto prestito esclusivo è alla lettera la fame di chi scrive misurata dalla [0024](0024-chi-legge-non-aspetta-chi-legge.md). Il `READ_CHUNK` non entra nel contratto ([0094](0094-un-tetto-che-si-fa-sentire.md)). Il difetto peggiore stava **fuori dalla voce**, per la terza volta dopo la [0099](0099-una-rinomina-che-non-ha-visto-nessuno.md) e la [0101](0101-una-voce-non-e-un-passo.md), ed è ciò che rende falsa la frase con cui la voce si dichiarava P1: la regola «una funzione nuova è additiva» di `wit_additivity` era stata scritta guardando le interfacce che il plugin **importa** e applicata anche alle dodici che **esporta**, dove una funzione in più non è qualcosa da ignorare ma qualcosa da **fornire** — un world che la pretende è un world che un componente già compilato non soddisfa. Ne erano passate due verdi, `index::up-to-date` ([0047](0047-la-cartella-esiste-nel-kernel.md)) e `view::interests` ([0033](0033-la-grana-di-un-abbonamento.md)), e nessuna delle due è nella tabella dei ritagli. | 2026-08-05 |
| [0103](0103-un-registro-dice-cosa-e-successo.md) | Un registro dice cosa è successo, non cosa c'era scritto | §23.9 — **chiude la voce**. La [0067](0067-il-registro-di-cio-che-e-successo.md) aveva dichiarato il proprio prezzo — «un registro delle mutazioni nomina i path di ogni nota toccata e quando lo è stata» — e la riga accanto nessuno l'aveva sommata: `JournalOp::Edited` portava `inverse: EditRequest`, cioè **il testo sostituito**. Sommate, le due dicono che dentro il vault c'era un file in chiaro, non spegnibile, che nessun gesto dell'utente raggiungeva, con dentro frammenti delle sue note che **sopravvivevano alla cancellazione delle note da cui venivano**. La domanda della voce — *«l'annullamento vale il prezzo?»* — era **mal posta**, e misurarla l'ha rovesciata: quel prezzo è **zero**. `undo_last` legge la pila in memoria della [0045](0045-l-undo-ha-due-pile.md) e il registro non compare nel suo corpo; i tre lettori del journal in produzione usano un campo solo, un intero; `inverse` aveva **un consumatore in tutto il repo, ed era un test**. E il rollback di lotto che il registro avrebbe abilitato era **già** parziale per costruzione, perché `Written` — il salvataggio, la mutazione più frequente — un inverso non l'ha mai avuto. Adesso `Edited` porta l'**impronta** (`EditFootprint { span, replaced }`): un audit ha ancora *quando, chi, dove, quanto* — e in più il **conto** degli edit, che l'inverso perdeva fondendoli —, e per *cosa c'era scritto* c'è il versioning, che si spegne e si cancella. Tre righe che sono decisioni: il campo si chiama `footprint` e non `inverse` (un nome che promette di tornare indietro è un nome che qualcuno applicherà), `is_invertible` risponde `false` anche per `Edited` (le due varianti che non si annullano sono ora **le due che porterebbero testo**), e `report.inverse()` sulla strada del disco non si chiama più affatto, così i byte non passano nemmeno per una variabile. Ciò che resta — path e tempi — si **dichiara** invece di spegnersi: `journal.retention.days` (zero, il default, vuol dire *per sempre*, perché cancellare per default un dato **autorevole** ([0048](0048-una-radice-sola.md)) che nessuno ha chiesto di cancellare non è difendibile), potato alla dichiarazione dello schema e a ogni cambio della chiave (il punto della [0098](0098-un-permesso-si-vede-e-si-nega.md)), col taglio **più avanti dei due** rispetto al tetto e una scansione sola fino al confine di lotto; e `vault.clear-journal` in `maintenance.rs` ([0086](0086-una-cronologia-e-la-sua-porta.md)), l'unico dei quattro con `irreversible()` e un `summary` nel piano, perché il journal era **l'unico dato dell'utente dentro il vault che nessun gesto dell'utente raggiungeva**. Chi pota non perde ciò che non capisce — una riga che non si sa leggere ma si sa datare si valuta, una che non porta `at` **ferma** la scansione —, chi svuota perde tutto apposta. Il journal **non si spegne** e la 0067 regge per intero: la domanda che la voce trattava come una era due, e *se registra* non si tocca. Il difetto peggiore stava **fuori dalla voce per la quarta volta di fila**: `il_registro_non_porta_dentro_il_documento` affermava un universale — «il registro non contiene il testo dei documenti» — esercitando **due varianti su sei**, e cinquanta righe più su, nello stesso file, un altro test *dimostrava il contrario* riapplicando l'inverso. Lezione stretta e riusabile: **un presidio che afferma un universale va letto contro l'enumerazione di ciò su cui è universale**; ora il `match` è esaustivo e senza `_`. Nessuna riga di WIT, nessuna migrazione: la versione di schema sta su ogni riga e le `Edited` vecchie si scartano contandosi. | 2026-08-05 |
| [0104](0104-la-superficie-di-scrittura-si-presta.md) | La superficie di scrittura si presta, e un invariante che non sa dove finisce è una scusa | §23.2 — **chiude la voce**. L'invariante di questo repo — *«una feature ufficiale è ciò che scriverà un plugin di terzi»* — la [0017](0017-chi-disegna-cio-che-il-core-non-conosce.md) l'aveva già trovata falsa una volta; la voce sosteneva che lo fosse una seconda, e che **sei verbali** avessero messo la superficie di scrittura dalla parte della shell senza che nessuno sommasse il risultato. Misurata, la diagnosi è **sbagliata**: riletti contro la domanda giusta — *quell'argomento vale contro un terzo che porta la propria superficie, o solo contro un terzo che tocca la nostra?* — quattro dei sei non sfiorano il caso, e la [0018](0018-chi-vede-il-modello-parsato.md) lo **concede per iscritto** (*«chi disegna un editor tiene il proprio testo»*); la [0082](0082-una-porta-per-chi-cerca.md) esclude l'editing dal proprio campo in una riga, la [0045](0045-l-undo-ha-due-pile.md) è neutrale per costruzione, la [0088](0088-cio-che-non-e-ancora-successo.md) protegge le bozze **altrui** e un terzo scriverebbe le proprie; la casella della [0078](0078-i-riquadri-sono-un-fatto-della-shell.md) che chiedeva un riquadro per una *view* l'ha chiusa la [0079](0079-il-grafo-esce-dall-overlay.md), e la [0090](0090-una-sequenza-e-una-modalita-che-scade.md) scarta la domanda invece di rispondere. Ciò che impedisce l'editing di terzi sono **due assenze, e nessuna è stata decisa**: nel contratto non esiste **nessun** evento di tastiera — un provider riceve un `UiAction`, cioè un gesto già interpretato, e sotto una superficie di scrittura l'interpretazione *è* il lavoro — e `Html`/`WebView` sono riservati a `Trust::Core` per ragioni che riguardano il **contenuto attivo**, non l'editing. Una decisione si discute, un'assenza non si trova: ecco perché la riga non era scritta. La risposta è quella che costa di più, ed è scelta apposta — **«l'editor è della shell» vuol dire *questo* editor, non *l'editing*: la superficie si presta**, perché l'alternativa avrebbe reso false in un colpo le righe con cui questo repo descrive sé stesso, e un progetto che le scrive e poi tiene per sé la superficie su cui l'utente passa il novanta per cento del tempo non ha un invariante, ha un'insegna. Le due porte **non** si aprono qui, perché progettarle senza un cliente è il difetto che la seduta 22 ha contestato a chi l'aveva aperta: il primo cliente ha un nome — la modalità vim, il cui verdetto in [funzionalità future](../appendix/funzionalita-future.md) passa da `da decidere` a lavoro collocato. Il difetto peggiore stava **fuori dalla voce per la quinta volta di fila**: il metro a tre voci di [plugin-boundary.md](../architecture/plugin-boundary.md) si dichiara esaustivo, e un editor di terzi **non inciampa in nessuna delle tre** — passa il metro e resta impossibile, perché il metro pesa un **costo** e queste sono assenze di **superficie**. Ora ha una **quarta voce** (*se la superficie esiste*) e, accanto, un **buco dichiarato** nella forma della [0064](0064-il-supporto-sta-sotto.md): i buchi diventano due, e la [0067](0067-il-registro-di-cio-che-e-successo.md) voleva restassero uno — non è una deroga, è il caso per cui la forma era stata inventata, perché qui la deduzione sbagliata la produce l'invariante stesso. E l'invariante impara **dove finisce**, che è la parte con del codice dentro: il difetto non era che fosse falso, era che non sapeva su quale insieme fosse universale — la stessa forma che la [0103](0103-un-registro-dice-cosa-e-successo.md) ha trovato in un test. Contate: le feature ufficiali stanno su **quattro** superfici delle **dieci** che il contratto nomina, e sei non hanno nessun dogfooding — `conformita.rs` le contava sulle **view**, ma un plugin di terzi si attacca a una **superficie**. Tre presidi: `ViewSurface::ALL` in `fub-abi` (l'argomento di `Capability::ALL`), `il_dogfooding_dichiara_fin_dove_arriva` con un `match` esaustivo — una superficie nuova **non compila** finché qualcuno non la classifica — e il banco delle superfici che estrae l'elenco dal **mirror generato**. Le due direzioni non si equivalgono: «una superficie esercitata non può essere dichiarata scoperta» si pretende sempre, l'inversa dipende dalle feature accese (§16.3) e per quella basta il compilatore. Il numero che nessuno confrontava: l'intestazione del banco della shell diceva *«sette superfici su otto»*, ed erano dieci da prima che quella riga fosse scritta — *esaustivo a memoria, non per costruzione* (§16.7), e un elenco tenuto a memoria non mente dicendo il falso, mente **tacendo una riga**. Nessuna riga di WIT, nessun tipo nuovo, nessuna firma: la voce diceva «non c'è una firma da scrivere» e su questo aveva ragione — sbagliava soltanto la diagnosi di *perché*. | 2026-08-05 |
| [0105](0105-una-porta-si-nomina-e-un-presupposto-si-compila.md) | Una porta si nomina, e un presupposto si fa verificare dal compilatore | §23.15 — **chiude la voce**. La [0032](0032-il-runner-dei-job.md) aveva scritto la riga esatta del rischio — *«`catch_unwind` presuppone che il panico srotoli; se un giorno il workspace impostasse `panic = "abort"` questa è la riga da rileggere»* — cioè una **casella indirizzata a nessuno**, che chiede a un lettore futuro di ricordarsi di una frase nel momento in cui non la sta leggendo. **Il nesso della voce è falso, e il fatto vero è peggiore**: Cargo **ignora** `panic` per i profili `test` e `bench` — l'harness ha bisogno dello srotolamento per raccogliere i fallimenti —, quindi `il_panico.rs` non sarebbe «la prima vittima», resterebbe **verde** attestando una rete che nel binario spedito non esiste più. Un test che abortisce si nota, uno che passa a vuoto no: è la specie della [0054](0054-il-banco-del-lato-provider.md) vista da un verso nuovo, e la regola che se ne ricava è che **un banco non prova le condizioni sotto cui non gira**. Da qui discende che il presidio chiesto dalla voce — *«un test che legga il profilo effettivo»* — **non è scrivibile**: un test gira sempre sotto `unwind`, e leggere il `Cargo.toml` come testo vedrebbe il file e non il flag, perché `RUSTFLAGS=-Cpanic=abort` e un `.cargo/config.toml` gli passano sopra la testa ed è il modo *normale* in cui un packager impone un profilo. La risposta è un `#[cfg(panic = "abort")] compile_error!` dentro `fub-kernel`: del crate che si compila, quindi vede tutti e tre i canali; del compilatore e non della suite, quindi non chiede che qualcuno esegua i test — la lezione della [0104](0104-la-superficie-di-scrittura-si-presta.md) applicata a un `cfg`, **un presidio il cui solo mestiere è non compilare vale più di un `assert` che qualcuno deve eseguire**. E non è un divieto per sempre: il giorno che il profilo lo si vuole davvero, la risposta è isolare i componenti fuori dal processo (§24.2, o il guest WASM di M5), e sta scritto nel messaggio dell'errore, che è il solo posto dove chi inciampa lo leggerà. Il difetto peggiore stava **fuori dalla voce per la sesta volta di fila**: la 0032 aveva scritto anche *«otto porte, e sono tutte quelle da cui si entra in codice di un plugin»*, un criterio dichiarato **esaustivo** e tenuto a mano; misurate, le porte sono **tredici** — e una di loro, `IndexProvider::up_to_date` ([0046](0046-l-anagrafe-del-vault.md)), è **nata dopo** quel conto, l'ha passato accanto senza toccarlo ed è in produzione dentro ogni riapertura di vault. Il presidio dei numeri in prosa della [0072](0072-un-numero-si-scrive-accanto-a-come-si-ricava.md) non poteva prenderla, perché non esisteva niente da cui contare: *esaustivo a memoria, non per costruzione* (§16.7). Adesso le porte sono un **dato** — `fub_kernel::safety::Gate` con `Gate::ALL` —, `calling`/`caught`/`reporting` prendono `gate: Gate` e `detail: &str` invece di una frase scritta a mano al sito (**la porta dice il verbo, il sito dice il soggetto**), e due `match` senza `_` reggono il resto: chi apre una porta nuova **non compila** finché non le dà una frase, e **non compila** finché non dichiara dove quella porta è provata o perché non lo è — la forma di `il_dogfooding_dichiara_fin_dove_arriva` su un insieme diverso, perché **un conto non sa quante cose esistano fuori di lui; il compilatore sì**. Il censimento non ha dichiarato cinque buchi, li ha chiusi: `Service`, `SyntaxRule`, `IndexUpToDate`, `IndexForget` e `IndexReconcile` hanno adesso un banco ciascuna, e due hanno una metà che non era ovvia — un panico **non deve** diventare `Unserved` (che significa «nessuno offre questo servizio», ed è il consiglio *«installa il plugin»* dato a chi il plugin ce l'ha già), e un `up_to_date` che pania fa **rileggere** il documento, perché un indice che pania non può dichiarare di essere aggiornato. Terzo presidio: una porta che riceve un dettaglio lo **nomina**, cioè la riga con cui la 0032 aveva motivato il `who` applicata al **cosa**. Nessuna riga di WIT e nessun profilo di release aggiunto: un panico resta un **difetto**, non una condizione, e nel contratto non ci va. **La verifica del rosso ha corretto due affermazioni di questo verbale**: il censimento dichiarava `CustomRender` provata *altrove* in un file che esiste e dove **nessun renderer pania** — guardava il nome del file invece del suo corpo, cioè il difetto della voce ripetuto un piano sotto, e adesso quella porta ha un banco vero —, e l'elenco diceva di prendere «una variante aggiunta e mai messa in `ALL`» mentre **non la prende**, né lui né i due presidi da cui la forma viene (l'ancora della lunghezza è sempre una variante nominata a mano, e in Rust stabile le varianti non si contano). A prenderla è un conto **da fuori**: questo commit registra `porte-verso-un-terzo` e `superfici-di-vista` in `conteggi.mjs`, e la regola che ne esce corregge in parte la 0104 — **il compilatore prende la variante che non vuol dire niente, il conto prende la variante che nessuno ha elencato**, e servono tutti e due. Riparata nello stesso commit anche la **seconda** zona cieca di `Capability::ALL`, da cui quella forma era stata copiata: due righe scambiate gli sfuggivano. | 2026-08-05 |
| [0106](0106-un-formato-si-presenta.md) | Un formato si presenta, e un elenco che nessuno riconta è un ricordo | §15.3 — **chiude la voce**. Il buco che la voce descriveva era uno e vero — il sidecar del cestino (`kernel/vault.rs`) era un record di un campo, scritto e riletto senza che niente dicesse di quale epoca fosse — ma i formati versionati **non erano nove: erano dieci**. `DIAGNOSTICS_VERSION` è nato **con** il campo e col commento «§15.3» già scritto accanto, cioè da qualcuno che aveva letto questa voce e l'aveva applicata prima del suo turno, e non lo contava nessuno. E la tabella che li elenca in [versionamento.md](../versionamento.md) era sbagliata in **cinque righe su nove**: `vaults.rs:39` era a 40, `entries.rs:86` a 89, `settings.rs:51` a 82, `versioning.rs:147` a 253, `journal.rs:112` a 156 — più della metà mandava a leggere un'altra riga, e la riga sbagliata di un sorgente vivo non dà errore, **dà una risposta**. Adesso il sidecar porta il suo `v`, e con lui una regola che la voce non chiedeva ma che serviva per scriverlo: negli altri formati una versione ignota è un errore che si pronuncia, qui un sidecar di una versione ignota **vale come un sidecar che non c'è** e la voce si ripristina in radice col nome de-timbrato — perché ciò che il rifiuto rumoroso protegge, *non perdere quello che l'utente aveva scritto*, qui non è in gioco (una voce cestinata da Obsidian un sidecar non ce l'ha mai avuto). **Il rifiuto in avanti si dice quando tacere farebbe perdere qualcosa, e si tace quando il degrado è già la risposta prevista del formato** — e la regola dice anche quando cambiare idea: il giorno in cui quel file porterà qualcosa che il degrado non sa rifare, il campo su `TrashEntry` sarà da scrivere. Il difetto peggiore stava **fuori dalla voce per la settima volta di fila**, ed è la famiglia della [0105](0105-una-porta-si-nomina-e-un-presupposto-si-compila.md) vista dal verso opposto — là un conto mancava, qui c'era, era verde e in CI passava: `schemi-su-disco` contava `const SCHEMA_VERSION`, cioè **un nome invece di una proprietà**, e chi aveva chiamato la sua costante `DIAGNOSTICS_VERSION` non aveva sbagliato niente — un conto che guarda un nome si elude senza volerlo. Il repo diceva dieci in [on-disk-layout.md](../architecture/on-disk-layout.md), nove in versionamento.md e nove nel presidio, e i tre non si guardavano. Tre attori, come la 0105 ha insegnato: il conto ora guarda la proprietà, un secondo conto (`schemi-in-tabella`) conta le righe del documento e i due stanno nella stessa frase — se un formato nasce e non si documenta, divergono —, e `crates/fub-app/tests/schemi_su_disco.rs` (`include_str!` come `dieta_ipc.rs`: se un file si sposta **non compila**) confronta tabella e sorgenti **nei due versi**, riga per riga e valore per valore. Quei numeri di riga erano verificabili e nessuno li verificava: stanno nel *testo* del link e non nel frammento dell'URL, cioè fra i link che `check-doc-links` conta e non controlla. Due cose tenute apposta: i **tre nomi** del campo (`v`, `version`, `schema_version`) non si uniformano, perché costerebbe una migrazione di file già sui dischi delle persone per zero valore — di un numero di schema non conta come si chiami il campo che lo porta, conta che ci sia e che qualcuno lo confronti; e **nessuna riga di WIT**, perché pretendere la versione *dai plugin* è una regola che non protegge nessuno (chi rilegge quello spazio dopo un aggiornamento è il plugin). Due previsioni appese a questa voce cadono: `write_atomic` l'aveva già spostata la [0065](0065-una-scrittura-o-c-e-o-non-c-e.md), che aveva **misurato sbagliata** la promessa della [0036](0036-le-impostazioni-e-i-tre-stati.md), e la **corruption detection** non è di qui — un numero di schema dice *quale formato* sono quei byte, non *se* sono integri, e un file troncato a metà porta la sua versione giusta in testa. Resta un **buco dichiarato**, il terzo (0064, 0104): un formato che nasce senza costante **e** senza riga non lo prende nessuno dei due presidi, perché entrambi contano chi si è dichiarato — a prenderlo servirebbe un tipo che ogni scrittura durevole attraversi, e non c'è perché **dalla stessa porta passano i file di Fub e i file dell'utente**, e il markdown di una nota un numero di schema non deve averlo. **La verifica del rosso ha corretto il verbale tre volte.** I due presidi nuovi erano **un presidio solo travestito da due**: guardavano la stessa sintassi, e `pub(crate) const PROVA_VERSION: u32` — la visibilità più comune di questo codebase — passava verde da entrambi; adesso riconoscono qualunque visibilità e qualunque larghezza dell'intero. I due banchi che la voce indicava come **il modello** non provano la cosa che conta: rinominando il campo `body` in `corpo` senza toccare `SCHEMA_VERSION` la suite resta verde su 110 banchi — si esprimono *in termini di* `SCHEMA_VERSION` e mai della **forma che quel numero versiona**, cioè provano che il numero funziona, non che salga quando deve, ed è il difetto di questa voce nella sua forma più pura, dentro il suo esempio; ora `IMPRONTA_DELLO_SCHEMA` scrive la forma accanto al numero e `lo_schema_non_cambia_senza_che_il_numero_salga` la confronta. E `dieta_ipc.rs`, il presidio da cui questa forma è **copiata**, ha la stessa zona cieca dove costa di più: provato costruendo il caso, un secondo `#[tauri::command]` in un altro file del crate montato con un `.plugin()` passa verde da tutti e otto i suoi banchi ed è raggiungibile dal webview — riparato nello stesso commit con `file-con-superficie-ipc`, perché **un presidio che legge un file sa quel file, e ciò che sta in un altro file lo prende solo un conto che cammina la cartella**. | 2026-08-06 |
| [0107](0107-il-caso-di-una-lettera.md) | Il caso di una lettera: chi è candidato, chi ha ragione, e chi non può avere ragione | §23.8 — **chiude la voce**. La [0004](0004-il-grafo-e-i-link-non-wiki.md) aveva deciso bene — `resolution_key` fa trim, NFC e minuscolo perché *«il vault sincronizzato fra macOS e Linux è lo stesso vault»* — e il prezzo stava dall'altra parte: su un filesystem case-sensitive `Nota.md` e `nota.md` sono due file veri, e per il grafo erano una chiave sola. Non un link che non risolve: un link che risolve **al file sbagliato**, senza lasciare traccia. **Il nesso su cui la voce si reggeva è falso**: il ramo «esatto» di `resolve_path_key` confrontava `resolution_key(id) == key`, cioè **due chiavi già minuscolate**, e non poteva distinguere il caso nemmeno in linea di principio — non c'era una regola da riusare, c'era una funzione che non esisteva, ed è la ragione per cui questa voce non si era mai chiusa da sé. Adesso `exact_key` sta accanto a `resolution_key` senza sostituirla, e la differenza si scrive in una riga: **`resolution_key` dice chi è candidato, `exact_key` dice chi ha ragione fra i candidati**. Gli indici restano indicizzati per chiave, quindi `watchers`, `refs_by_key` e tutta l'invalidazione incrementale non cambiano; cambia la scelta fra i candidati, che era `first()` — l'ordine ASCII — e ora è *chi combacia esattamente, e in sua assenza il primo per priorità*, cioè la regola di prima intera: la case-insensitivity non si è ristretta, si è **ordinata**. Gli **alias restano fuori apposta**: dietro un path e un nome pagina c'è un file che il filesystem distingue, dietro un alias c'è una dichiarazione in un frontmatter — due documenti che dichiarano `NASA` e `nasa` sono due rivendicazioni ugualmente valide, lì l'ambiguità è genuina e la priorità resta la risposta. **La seconda premessa falsa vale di più**: il modello per il segnale non poteva essere la [0090](0090-una-sequenza-e-una-modalita-che-scade.md), che è **interamente di shell** e non ha l'anagrafe — ma il posto c'era già ed è migliore, `IndexQuery::VaultHealth`, che dalla [0046](0046-l-anagrafe-del-vault.md) ha grafo e anagrafe insieme. Da lì `HealthCheck::CollidingPaths`, terzo caso in coda a un enum di due (additivo, nessun record cambia), che cammina l'**anagrafe** e non i documenti perché `foto.PNG` e `foto.png` collidono come due note, usa la chiave del path **intero con estensione** (`nota.md` e `nota.txt` si distinguono benissimo, e segnalarli è il rumore che rende inutile un controllo di salute) ed emette una issue per **ogni** membro del gruppo, perché appenderla a uno solo vorrebbe dire sceglierlo — la stessa asimmetria arbitraria che la voce è venuta a togliere. La ragione che rende quel controllo indispensabile **nessuno l'aveva scritta**: `resolve_key` consulta l'indice dei path solo se la chiave contiene `/`, quindi per due file nella **radice** del vault non esiste nessun wikilink che disambigui — quando l'ambiguità non è esprimibile nella lingua dei link, l'unica risposta possibile è dirla. Il difetto peggiore stava **fuori dalla voce per l'ottava volta di fila**, e non è una risoluzione a schermo: `link_rewrite_plan` sceglie il riferimento che Fub **scrive su disco dentro i documenti di terzi**, e giustificava la seconda delle sue due forme con *«il path senza estensione è sempre univoco»* — falso, la chiave di `path_index` è `resolution_key(strip_ext(…))` e `sub/Altra.md` e `sub/Altra.txt` la condividono. Le forme sono tre e la condizione ora si **verifica**; la strada elegante — non dedurre la forma ma **provarla**, chiedendo al grafo se torna a `to` — **non è percorribile**, e lo si scopre solo provandoci: il piano si calcola *prima* che il rename sia applicato, il grafo conosce ancora `from` e non ha mai sentito nominare `to`, quindi ogni candidato risulterebbe sbagliato e si scriverebbe sempre la forma più lunga. Riparate due bugie che si somigliano — quel commento e il modulo `path_policy.rs`, che sotto «Cosa NON è duplicato qui» dichiarava *«un vault che contiene `Nota.md` e `nota.md` è già ambiguo per il grafo… e la risposta è una sola perché la domanda è una sola»*, dichiarando **coperto** ciò che non lo era: `resolution_key` non rileva l'ambiguità, la **collassa in silenzio**, e le domande erano tre. **Entrambe affermavano un'univocità che era la conclusione che si voleva, non un fatto misurato.** La verifica del rosso ha corretto il commit due volte: togliere la terza forma della riscrittura **non rendeva rosso niente** su 110 banchi — il caso non è raggiungibile con un solo formato registrato, e serve un secondo provider, che è la normalità appena un plugin ne porta uno —, e la zona cieca di `HealthCheck::ALL` costruita apposta ha mostrato che `[BrokenLinks, BrokenLinks, CollidingPaths]` **passava** le due metà ovvie del presidio della [0104](0104-la-superficie-di-scrittura-si-presta.md), perché la lunghezza torna e ogni elemento è al suo posto: lo prende solo l'aritmetica dei discriminanti. E un **secondo difetto fuori dalla voce**: l'elenco dei controlli che il rapporto diagnostico esegue erano tre righe scritte a mano, e il presidio chiedeva `json["health"].is_array()` — cioè un array con un controllo su tre lo soddisfaceva. Il rapporto ora itera `HealthCheck::ALL`, ed è la forma della 0104 e della [0105](0105-una-porta-si-nomina-e-un-presupposto-si-compila.md) su un terzo insieme: **un presidio che chiede «è della specie giusta?» a una risposta che è un elenco non sta guardando l'elenco.** Il mirror TS di `resolution_key` non cambia (la shell non risolve link, chiede `IndexQuery::Resolve`), e non si è scritto «è raro, si lascia» — la §23.16 usa questa voce come precedente sulla parola «raro». Nessuna riparazione automatica: quale dei due file abbia il nome sbagliato è la domanda a cui nessuno ha una risposta, e il vault è di chi lo possiede. | 2026-08-06 |
| [0108](0108-una-data-la-dichiara-chi-possiede-il-vault.md) | Una data la dichiara chi possiede il vault, e ciò che sembra una data si dice | §23.7 — **chiude la voce**. La [0003](0003-modello-del-documento.md) aveva deciso bene — *«solo l'ISO-8601 a larghezza fissa è una data»*, perché *«un parser tollerante trasformerebbe in date le stringhe dell'utente»* — e il prezzo lo paga **il** caso d'uso: questa app si apre su vault altrui, e un vault altrui porta `2026-7-5` o `05/07/2026`, che restano `Text` mentre il filtro non trova, il raggruppamento non raggruppa e **nessuno dice perché**. Ciò che cambia **non è la tolleranza del parser**: è **chi dichiara il formato** — un formato dichiarato da chi possiede il vault non è un indovinello, ed è la differenza esatta fra questa voce e la cosa che la 0003 ha rifiutato. L'ISO si legge sempre e **per primo**, una dichiarazione **aggiunge** una lettura e non ne cambia nessuna, e senza dichiarazione il comportamento resta identico byte per byte. **La premessa sulla collocazione era falsa, e non è un dettaglio**: la voce e la misura la davano per la quinta riga di `locale_settings()`, ma le quattro chiavi `locale.*` sono definite da una proprietà sola — *il sistema ha una risposta*, e il loro default è `AS_SYSTEM` — mentre qui la risposta del sistema sarebbe **sbagliata per costruzione**: `05/07/2026` su una macchina italiana è il cinque luglio e su una americana il sette maggio, quindi lo stesso vault sincronizzato fra due computer porterebbe **due date diverse per lo stesso byte** — il difetto che la [0004](0004-il-grafo-e-i-link-non-wiki.md) ha rifiutato per i link (*il vault sincronizzato fra macOS e Linux è lo stesso vault*). Il formato è un fatto **dei file**, non di chi guarda: da qui `properties.date-format` e non `locale.*`, il default **«solo ISO»** e non «come il sistema», e il livello **vault** che qui non è l'inerzia della [0076](0076-le-impostazioni-vivono-nel-vault.md) ma l'unica scelta possibile. Non è `program_writable`: un componente che potesse dichiararlo cambierebbe il valore di ogni proprietà data di ogni nota senza toccare un file. Si dichiara un **ordine** (`dmy`/`mdy`/`ymd`) e non un formato con segnaposto: il separatore non è mai stato l'ambiguità — `05/07/2026` e `05-07-2026` sono la stessa scrittura — mentre `05/07/2026` e `07/05/2026` sono la **stessa stringa** letta come due giorni diversi, ed è esattamente ciò che nessun parser può dedurre. Il parser dichiarato è rigido quanto l'ISO su un insieme diverso (stesso separatore fra `/`, `-` e `.`, anno a **quattro** cifre perché `05/07/26` chiederebbe di indovinare il secolo), e `jiff` non entra: la [0091](0091-un-orario-di-parete-non-e-un-intervallo.md) lo vincola a `fub-host`, e un parser di formati **dichiarati** sono quattro righe di confronto. **Il pezzo di progetto vero è come il formato arriva al parser**, che sta in `fub-abi` e non legge impostazioni: si **passa**, e `Frontmatter::property`/`properties` più le quattro funzioni di `rules::properties` prendono un `&DateFormats`. La versione senza parametro **non** è stata tenuta, ed è la scelta che costa: un `property(key)` sopravvissuto sarebbe la strada che ogni chiamante nuovo prende senza accorgersene, e qui il presidio è **il compilatore**, l'unico dei tre attori (0105) capace di prendere il chiamante che *non c'è ancora*. Il valore si rilegge **a ogni domanda** dalle impostazioni condivise: un indice che rispondesse con com'era al montaggio direbbe che il filtro non trova **anche dopo** che l'utente ha riparato la causa. **Il difetto peggiore stava fuori dalla voce per la nona volta di fila, e di una categoria peggiore di quanto la voce dicesse**: non solo il filtro e il raggruppamento, ma l'**ordinamento** — `compare` rende `None` per specie diverse e `order_of` lo mappa su `Equal`, quindi su un vault misto (metà ISO, metà no: lo stato normale di una migrazione) il comparatore **non è un ordine**: con `2026-07-05`, `5/7/2026` e `1/1/2020` si ha `a == b`, `a == c` e `b < c`, e un `sort_by` con un comparatore incoerente rende una permutazione che nessuno ha deciso. Non un ordine sbagliato riconoscibile: un ordine **plausibile**. Stessa forma per `contains`, che su una `Text` fa sottostringa e quindi **si inverte** a seconda del formato del vault. Il segnale è `HealthCheck::UnrecognizedDates`, quarto caso in coda a un enum di tre — additivo, `health-check` è nel frozen e nessun record cambia — che cammina i **documenti** e non l'anagrafe (simmetria opposta alla [0107](0107-il-caso-di-una-lettera.md): una proprietà sta in una nota) ed emette una issue **per documento**, perché il gesto che ripara è uno. Il rilevatore **non è una seconda regola**: è lo stesso parser con tutti gli ordini insieme, e la riga che tiene questa decisione insieme alla 0003 è che **un parser largo che produce un valore inventa, uno che produce una domanda avvisa**. Chi ha dichiarato non si sente ripetere ciò che quel formato legge, perché un controllo rumoroso è un controllo spento. Tre cose tenute fuori apposta: nessuna **riparazione automatica** — qui, a differenza della 0107, sarebbe pure fattibile, e proprio per questo va detto che riscrivere il frontmatter di note altrui per far funzionare un filtro è il gesto che questo progetto promette di non fare —; l'**orario** accanto a una data non-ISO, perché dichiararlo vorrebbe dire dichiarare anche il fuso, che è della macchina (0091) e non del file; e i **nomi dei mesi**, che restano una **casella** perché un nome di mese è una parola di una lingua e le tabelle per locale non ci sono. Il ritaglio era il punto delicato e la misura lo conferma: nove tipi della famiglia sono nel frozen, e questa decisione non ne tocca nessuno perché il formato è **dato** in un `SettingValue` e non firma — se fosse stato un campo su `property-date`, che è la strada che sembra più diretta, sarebbe stato **major** e la voce non si sarebbe potuta chiudere prima del freeze. La verifica del rosso ha dato un difetto **fuori dalla voce** della specie già nota: il banco che la misura dava per «copertura per costruzione», `le_impostazioni_del_core_parlano_anche_loro`, nomina `locale_settings()` **a mano**, quindi una famiglia nuova del kernel gli passa accanto restando verde — e il suo gemello `cataloghi_del_core()` aveva **già** perso `maintenance::catalog()` senza che nessuno se ne accorgesse (*esaustivo a memoria, non per costruzione*, §16.7). Allungati tutti e due invece di allungare una bugia, e riparato lì dentro un difetto vero: pretendevano ogni chiave in **ogni catalogo** invece che in ogni **lingua**, cioè non avrebbero mai permesso a due cataloghi della stessa lingua di sommarsi, che è precisamente ciò che il montaggio fa. E un ramo che **non** rendeva rosso niente — la larghezza massima di un campo numerico — ha avuto il suo caso scritto (`5/007/2026`) invece di essere tolto. | 2026-08-06 |
| [0109](0109-un-conteggio-che-non-si-sa-non-e-un-nome-solo.md) | Un conteggio che non si sa non è «un nome solo», e una suite che si svuota è verde | §23.16 — **chiude la voce, e con lei la seduta 23**. La [0065](0065-una-scrittura-o-c-e-o-non-c-e.md) aveva deciso bene — temp+rename+fsync, e **sul posto** dove l'inode ha altri titolari, perché *«i due danni non sono uguali: il file troncato vuole un crash durante la scrittura ed è visibile, il nome staccato avviene a ogni salvataggio e non lo vede nessuno»* — e il prezzo stava nel **rilevamento**: `condiviso()` contava `nlink` sotto `#[cfg(unix)]` e su tutto il resto rispondeva `false` **costante**, con un commento accanto che lo ammetteva per esteso. Su Windows un file con più nomi prendeva la rename, la rename ne staccava uno, e il secondo nome restava fermo al contenuto vecchio senza errore e senza avviso: il «danno certo e muto» che quel verbale diceva di voler evitare, non evitato lì. **Un commento non prende nessuna decisione** — e finché il tipo era un `bool`, «ne ha uno solo» e «non lo so» erano lo stesso valore. I casi sono **quattro** e adesso hanno un nome: `NomiDelFile::{Nessuno, Uno, PiuDiUno, Ignoto}`, la stessa forma della [0094](0094-un-tetto-che-si-fa-sentire.md) — *i significati erano tre e non due* — trovata stavolta su una funzione che nessuno guardava perché sembrava un predicato. Con i casi distinti la regola diventa una funzione **pura** (`come_scrivere`) e la riga che vale la voce si può finalmente scrivere: **`Ignoto` sceglie come `PiuDiUno`**, cioè davanti a un dubbio si paga il danno che si vede — non una scelta nuova, ma l'argomento della 0065 preso fino in fondo, che prima non si poteva nemmeno porre. Il verso conservativo che la voce chiedeva di *scartare avendolo detto* — in-place **sempre** su Windows — è scartato con una **misura invece che con una stima**: il conteggio là si può avere (`GetFileInformationByHandle` → `nNumberOfLinks`), quindi non c'è nessuna atomicità da togliere a una piattaforma su tre, e il verso conservativo resta ridotto al suo campo legittimo, `Ignoto`. Anche la domanda della supply chain ([0001](0001-supply-chain-e-sbom.md)) ha un numero: `windows-sys 0.61` era **già nell'albero** (tirato da tauri) e dipende dal solo `windows-link`, che c'è pure lui — dichiararlo sotto `[target.'cfg(windows)'.dependencies]` muove **una riga** di `Cargo.lock`, mentre l'alternativa era una `extern "system"` a mano con la propria `BY_HANDLE_FILE_INFORMATION`, una struct di dodici campi il cui layout sbagliato **non fa rumore**: restituisce un numero, e il numero è quello sbagliato — cioè la specie di difetto che questa voce esiste per togliere, reintrodotta dal rimedio. **La premessa sul presidio era falsa, e la realtà è peggiore della diagnosi**: la voce diceva *«nessun test di questo repo gira su Windows, quindi il caso non è solo scoperto: è inosservabile»*, e invece `matrix.os` contiene `windows-latest` da prima che quella riga fosse scritta — il job esiste, gira `cargo test --workspace`, ed è passato verde per tutta la vita del progetto **proprio perché** i quattro presidi che avrebbero interrogato il caso sono `#[cfg(unix)]` e là non venivano nemmeno compilati. **Una suite che si svuota in silenzio è indistinguibile da una suite verde**, ed è una specie che nessuno dei tre attori prendeva: il compilatore no, perché il codice che non c'è compila; il test no, perché il test che se ne accorgerebbe è precisamente quello che manca; il conto nemmeno, perché nessuno l'aveva scritto. La misura ha cambiato il progetto a metà strada: la forma iniettabile che sembrava ovvia — *«un `fn(&Metadata) -> bool`»* — **non può esprimere il caso Windows**, perché là il conteggio sta dietro un **handle** e il file va aperto, cioè serve il *path* che un `&Metadata` non porta; la differenza fra le due piattaforme non è quanto costa la risposta, è a quale oggetto si pone la domanda. Da lì `FsStorage::write_con(path, bytes, rilevatore)`, che è il corpo di `write` e **restituisce la strada presa** — non un gancio di prova travestito da API, ma l'unica cosa osservabile di questa scelta che non richieda di guardare un inode, cioè l'unica presidiabile dove gli inode non ci sono. Tre attori, come la [0105](0105-una-porta-si-nomina-e-un-presupposto-si-compila.md) ha insegnato: il `match` su `NomiDelFile` (un quinto caso non compila finché qualcuno non decide cosa farne), cinque banchi nuovi **nessuno dei quali `#[cfg(unix)]`** — con i quattro vecchi tenuti dov'erano, perché provano la cosa che i nuovi non provano, che il conteggio *vero* sia giusto —, e il conto `durabilita-su-ogni-piattaforma`, che è **il presidio del presidio**: se qualcuno riportasse questa metà sotto la piattaforma il conto scenderebbe e `check-prosa` diventerebbe rosso, mentre `cargo test` su Windows resterebbe verde, perché è esattamente ciò che non sa vedere. In CI, dal job Linux, `cargo check -p fub-kernel --all-targets --target x86_64-pc-windows-msvc`: sei secondi, e **una FFI che non compila è una FFI che non è stata scritta**. Il presidio che ha fatto il proprio mestiere senza essere chiamato è `abi_and_kernel_stay_agnostic`, rosso alla prima compilazione su una dipendenza che sulla macchina che esegue il test **non entra mai nell'albero**: la vede perché guarda il **manifesto e non la macchina**, ed è il verso giusto — una dipendenza che entra solo su una piattaforma è precisamente quella che nessuno rilegge. Nessun segnale all'utente, e la ragione è simmetrica: quando il conteggio si sa la scrittura fa la cosa giusta, quando non si sa la fa **anche**, e un segnale si scrive quando all'utente resta qualcosa da decidere. Resta un **buco dichiarato**, il quarto (0064, [0104](0104-la-superficie-di-scrittura-si-presta.md), [0106](0106-un-formato-si-presenta.md)) e non è una casella: che `nNumberOfLinks` risponda giusto su un handle aperto **come lo apre Fub**, che la `MoveFileEx` sotto `std::fs::rename` si comporti come si presume con più nomi sullo stesso inode, ReFS, SMB e i percorsi UNC. **Dichiararlo è chiudere la voce, non lasciarla aperta.** E non si è scritto «è raro, si lascia», che era il vincolo che la [0107](0107-il-caso-di-una-lettera.md) aveva lasciato in eredità: raro finché il vault sta su una macchina sola e non lo tocca nessun altro strumento — e gli hardlink dentro un vault li mettono precisamente gli strumenti che questo progetto promette di non ostacolare. | 2026-08-06 |
| [0110](0110-la-struttura-non-e-una-preferenza.md) | Cosa c'è nel vault lo dichiara il vault, e la struttura non è una preferenza | §15.6 — **chiude la voce, e con lei la seduta 15**. La voce diceva che il difetto era la **costante** (`IGNORED_DIRS`, un `&[&str]` nel sorgente di `vault.rs`) e che la [0036](0036-le-impostazioni-e-i-tre-stati.md) aveva dato a quel dato dove stare. La costante però era il **sintomo**: il difetto è che quella lista metteva nella stessa specie due esclusioni che non si somigliano affatto, e finché erano una sola «escluso» voleva dire insieme *ciò che nessuno può cambiare* e *ciò che nessuno può scegliere* — cioè il peggio delle due. Adesso sono due. **La struttura** — `.fub/` dove Fub scrive, `.trash/` che è il cestino condiviso con Obsidian, il temporaneo di una scrittura atomica — non è una preferenza di nessuno: mostrarla vuol dire indicizzare l'indice, riesumare come documenti le note appena cestinate, e dare un `DocId` a un file che fra un istante non esiste più; è `e_struttura`, ed è il primo `if` di ogni domanda. **La preferenza** — che `node_modules/` e `.git/` non siano note, che i dotfile si vedano — è dato per-vault, e sono due chiavi dichiarate: `files.excluded-folders` (una `List`, di cui la 0036 scriveva già nel commento «*gli id dei plugin spenti, le cartelle escluse*») e `files.show-hidden`. `IgnorePolicy` è un **valore** — l'elenco *più* la risposta sui nascosti — risolto dalle impostazioni **a ogni domanda** e non al montaggio, per la ragione della [0108](0108-una-data-la-dichiara-chi-possiede-il-vault.md); chi non ne ha prende il default, che è esattamente il comportamento di prima. Di livello **vault** ([0076](0076-le-impostazioni-vivono-nel-vault.md)) perché descrive *questi file*, e non `program_writable` per una ragione più stretta di quella del tema: un componente che aggiungesse una cartella all'elenco **toglierebbe dal vault le note che ci stanno dentro**, senza toccare un file. **Il difetto peggiore stava fuori dalla voce, ed è la voce stessa a crearlo**: `il_temporaneo_di_una_scrittura_non_e_un_documento` dichiarava di presidiare *l'incastro fra due moduli*, e invece chiedeva alla funzione **pura** se quel nome fosse ignorato — ricevendo un `true` che arrivava dal ramo «comincia per punto», cioè dal ramo che questa voce permette di spegnere. Il giorno in cui un vault mostra i nascosti, il temporaneo di ogni salvataggio diventa un documento per la scansione **e il presidio resta verde**. Adesso interroga la politica più permissiva che un vault possa dichiarare, e il temporaneo ha smesso di nascondersi dietro il punto: la sua forma intera è una regola (`e_temporaneo_di_scrittura`), scritta **accanto a chi compone il nome**, perché chi conosce una forma è chi la scrive. **La premessa sui symlink era falsa**: `std::fs` non li segue — dalla [0058](0058-un-nome-che-nasce.md) la specie si chiede con `file_type()` — quindi non mancava un comportamento, mancava che fosse **deciso**. Non si seguono, e il verso opposto è scartato *avendolo detto*: seguirli vuole l'identità di un nodo (`dev`+`ino`), che il `VaultStorage` non ha **di proposito** (§15.1), e senza quella `a/collegamento -> a` è una camminata che non torna — un interruttore così sarebbe la facoltà di appendere l'apertura del vault, e una facoltà così non è una facoltà. Il presidio costruisce l'anello sul filesystem vero, perché quel difetto non fallisce: si pianta. Le altre quattro politiche (§9.1, §18.1, §23.2, e il `.gitignore` del §3.1) ereditano il valutatore e la regola che le tiene insieme, scritta adesso che c'è la prima: **si compongono per sottrazione** — nessuna ripesca ciò che il vault non contiene, e nessuna ridefinisce cos'è la struttura. Il campo su una query di listing che la voce temeva come «la parte da decidere prima del freeze» **non serve**: mettere la politica sul vault risponde alla domanda invece di rimandarla — un plugin vede lo stesso vault che vede l'utente, e un flag per-chiamata sarebbero due idee di cosa la cartella contiene. Il WIT congelato non si tocca in nessun punto. **La zona cieca l'ha trovata la verifica del rosso, e non era nella voce**: togliendo da `core_settings()` la riga che monta una famiglia di impostazioni del kernel non diventa rosso **niente** — le chiavi spariscono dal pannello, chi le legge prende il default in silenzio (che è la regola giusta per un vault che non dichiara niente) e i due banchi dei cataloghi guardano *dalle chiavi verso le frasi*, mai al contrario. Vale identico per `properties`, `journal` e `locale`. Da lì `ogni_chiave_che_il_kernel_dichiara_e_montata_dal_core`, e siccome il rimedio è a sua volta un elenco a mano — lo stesso per cui `maintenance` è stato fuori da `cataloghi_del_core()` a lungo, segnalato dalla [0105](0105-una-porta-si-nomina-e-un-presupposto-si-compila.md) — i due elenchi hanno adesso il loro attore: i conti `cataloghi-del-kernel` e `impostazioni-del-kernel`, la sola specie di presidio che vede un **catalogo mancante** invece di una chiave mancante. | 2026-08-06 |
| [0111](0111-il-budget-e-un-tetto-sul-lavoro.md) | Il budget è un tetto sul lavoro, non sui fatti | §20.5 — **chiude la voce, e con lei la seduta 20**. Il budget del dispatch (mille e ventiquattro consegne per drenaggio) esiste per fermare due handler che si rimbalzano eventi senza convergere, cioè per mettere un tetto al **lavoro**; quando scattava faceva `self.pending.clear()`, e buttava la coda intera senza chiedersi cosa ci fosse dentro. Gli altri due freni del canale — il tetto degli arretrati del bus e la raffica del ponte — `Event::is_recoverable` la guardano; il terzo no, e un `Event::Trouble` è dichiarato **non** recuperabile perché porta l'unica copia di un fatto. Adesso il troncamento **degrada** invece di svuotare: ciò che si riscopre riguardando il vault diventa un `Overflow` («riconcilia da zero», più forte di ognuno dei singoli eventi buttati), ciò che porta l'unica copia di un fatto si consegna lo stesso. La regola non è stata scritta una terza volta: `degrade` era in fondo al ponte verso la shell ed è salita in `fub_abi::rules::events`, accanto alla maschera di un abbonamento, perché è la stessa domanda — *chi riceve cosa, quando non può ricevere tutto*. **Le due strade che la voce chiedeva di scegliere non erano alternative**: servono tutt'e due, in due punti diversi. La prima (conservare) vale per ciò che è già in coda quando il budget finisce — una fotografia, quindi finita, quindi consegnabile per intero senza un secondo budget da indovinare; la seconda (dire quanti) per ciò che gli handler emettono *mentre* ricevono quel tratto finale, che non si può consegnare — la coda deve terminare — ma si può contare, e il conto esce in un ultimo `Overflow`. Applicata da sola, la prima **spegne il segnale** proprio nel caso per cui il budget esiste: un ping-pong di `custom`, che non sono recuperabili, non lascia niente da buttare, quindi nessun `Overflow`, quindi un troncamento muto — e il presidio storico del troncamento diventa rosso. **Il difetto peggiore stava fuori dalla voce, di nuovo**: i posti da cui un evento sparisce erano **quattro** e non tre, e il quarto stava a quattro righe dal terzo — `drop_pending`, che scartava senza contarlo ciò che gli handler emettevano *gestendo* l'`Overflow`, guasti compresi. La voce non poteva vederlo perché guardava chi **decide** di troncare invece di chi **butta**, e il presidio che se ne accorge non è un test ma un conto che legge il sorgente da fuori (`code-che-si-svuotano`): il test che vedrebbe un `self.pending.clear()` è proprio quello che chi lo scrive non ha scritto. Nessuna firma, WIT congelato intatto. Due cose dichiarate e non chiuse: l'**ultimissimo giro** non si può raccontare (ciò che un handler emette *ricevendo* l'`Overflow` di congedo si scarta senza dirlo, perché dirlo vorrebbe dire un altro evento) e `EventMask::all()` **non contiene `Trouble`** — di proposito, ed è del §20.2: chi si abbona «a tutto» i guasti non li riceve, quindi il troncamento che non li butta più si vede solo se qualcuno li ha chiesti per nome. | 2026-08-06 |
| [0112](0112-un-e2e-contro-un-host-finto-prova-il-cablaggio.md) | Un e2e contro un host finto non prova l'app: prova il cablaggio | §17.2 — **chiude la voce**. Le due metà della voce non chiedevano lo stesso presidio: il testo diceva «e2e dell'app reale (tauri-driver/Playwright)», la riga in corsivo «gira contro l'host finto della 1.3». A decidere è stata la seconda, e non per il costo. Un runner di browser è una decisione di supply chain — `@playwright/test` porta un binario scaricato a parte che nessun SBOM di questo repo saprebbe dichiarare ([0001](0001-supply-chain-e-sbom.md)), `tauri-driver` vuole un'app impacchettata e un webdriver di sistema diverso per piattaforma, cioè un presidio che gira su una macchina e non su tre — ma la ragione vera è **cosa proverebbe in più**: delle tre cose che restano fuori (il kernel fa ciò che gli si chiede, il ponte serializza, la webview disegna) la prima ce l'ha già `cargo test`, meglio di qualunque click, e le altre due diventano il **buco dichiarato n. 5**. Ciò che invece nessuno provava, e che non chiede un browser, è il **cablaggio**: chi si monta prima di chi, quale porta attraversa un gesto, con quali argomenti — cioè ciò che vive *fra* i moduli, ed è esattamente la classe di difetti che la [0015](0015-la-forma-della-shell.md) dichiarava di non poter vedere. La shell si monta **intera** (`main.ts` vero, `index.html` vera) contro `frontend/src/host/finto.ts`, un vault in memoria con tre regole: è un **modulo intero** e non un pezzo di modulo (il tipo di ritorno è `typeof import("./ipc")`, quindi una porta nuova non compila finché il finto non la sa rispondere — e il compilatore ha già preso tre record sbagliati, fra cui un campo nato con la [0101](0101-una-voce-non-e-un-passo.md) che nessun mock scritto a mano avrebbe avuto); non conosce **nessuna feature**; e ciò che non sa fare **lancia**, perché un finto accomodante fa passare un e2e mentre la shell chiede la cosa sbagliata. **La premessa era falsa**: «l'host finto della 1.3» non è mai esistito — la 0015 diceva che quel giro lo *rendeva possibile* — e al suo posto c'erano quattro `vi.mock` scritti a mano, con due o tre metodi ciascuno e nessuno tipizzato. Falsa anche, e scritta in tre posti, «questa shell non ha un ambiente DOM nei test»: `happy-dom` c'era già, con cinque file di prova. **I due difetti veri stavano dentro i gesti**, e sono la ragione per cui la voce valeva il giro. L'albero dei file ha **un** ascoltatore di tastiera sul contenitore, e la rinomina in posto mette un campo di testo *dentro* una voce dell'albero: Invio confermava la rinomina **e** faceva `click` sulla riga, cioè riapriva il path vecchio — una tab fantasma, e il salvataggio automatico successivo che **ricrea il file appena rinominato**; le frecce erano lo stesso difetto in tono minore, il cursore non si muoveva dentro il nome che si stava scrivendo. La regola sta nel contenitore e non nel campo: **i tasti di un campo sono del campo**. Il secondo l'ha trovato la verifica del rosso: togliere la migrazione del buffer sulla rinomina non rendeva rosso **niente**, perché da dentro questa finestra `renameDoc` mette in salvo i buffer prima di chiedere — quel codice ha un caso solo, la rinomina che arriva **da fuori** (un `mv`, un sync, un'altra applicazione), e là il timer del debounce portava il nome vecchio nella sua chiusura: allo scadere non trovava più il buffer e usciva, lasciando la battuta dell'utente in RAM senza che nulla lo dicesse. Da lì il settimo gesto, e la riparazione. Il conto `gesti-della-shell` guarda il file da fuori perché una suite si svuota anche senza un `cfg` — un `it.skip` messo «per un attimo» basta, e `it.skip(` non è `it(` ([0109](0109-un-conteggio-che-non-si-sa-non-e-un-nome-solo.md)). Zero WIT, zero dipendenze nuove; una riga di produzione per il presidio, `main.ts` che **esporta l'avvio**, perché un boot che non si può attendere si prova dormendo un tempo a caso. | 2026-08-06 |
| [0113](0113-il-banco-conta-le-operazioni.md) | Il banco conta le operazioni, perché su una macchina condivisa il tempo non è un segnale | §17.1 — **chiude la voce, e con lei la seduta 17**. La voce chiedeva «benchmark su vault sintetici grandi (10k/100k note) in CI, con soglie: tempo di apertura, ricerca, memoria», e ne nomina tre che sono tutti tempi. **Il verso ingenuo è scartato avendolo detto** ([0109](0109-un-conteggio-che-non-si-sa-non-e-un-nome-solo.md)), e non per prudenza: il repo aveva **già** provato quel verso e lo aveva visto fallire. Il presidio della §8.4 ([0026](0026-due-query-insieme.md)) confronta due tempi nella stessa corsa e sulla stessa macchina — la forma *migliore* di una misura di tempo — e in CI ha dato 0,97 su ubuntu e 0,89 su windows con la suite verde in locale, perché a trenta millisecondi per colonna il tempo se lo prendono lo spawn dei thread e lo scheduling: il rapporto aveva smesso di misurare la propria proprietà per misurare il vicino di banco. Se non tiene quella forma, una soglia assoluta tiene meno — o è così larga da non scattare mai, e allora è un presidio che passa a vuoto, o scatta su un runner carico e la prima volta che qualcuno la sposta «per farla tacere» è morta senza che nessuno lo dichiari. Il criterio che resta: **un presidio deve misurare qualcosa che la macchina non cambia**. Quindi **operazioni**: quante volte si attraversa il confine per aprire un vault di N note (`ceil(N/512)`, e non N), quanti parse costa **riaprirlo** intatto (zero, non una frazione), quante allocazioni costa una pagina di venti righe — e di quest'ultima non si guarda il valore ma se **cresce col vault**, che è un rapporto fra due misure sulla stessa macchina e non una soglia. La memoria si conta con un `#[global_allocator]` **per thread**, perché `cargo test` gira in parallelo e un contatore condiviso misurerebbe il vicino, cioè il difetto da cui il banco nasce. **Due premesse false.** «Vault sintetici grandi (10k/100k note)»: non servono, ed è la caduta che ha reso la voce chiudibile da un portatile — un conto esatto è esatto a qualunque taglia, e seicento note sono il minimo che attraversa il lotto **due** volte, cioè il minimo che distingue «a lotti» da «tutto insieme». «Questo banco ha già un abitante che aspetta»: no — quel presidio misura un rapporto fra due tempi, e un banco che ha deciso di non misurare tempi non lo ospita. **Il difetto peggiore stava fuori dalla voce, un'altra volta**: il doc di `Page` prometteva che chi serve una query tronchi *prima* di costruire il risultato — «un vault con centomila note non deve materializzare centomila righe per mostrarne venti» — e **«deve poter» non è «lo fa»**. Nessuna delle nove famiglie paginate dell'indice del kernel lo faceva: venti righe dell'anagrafe costavano seicentotto allocazioni su trecento note e milleduecentonove su seicento, due per nota. Il difetto non era nemmeno nascosto — `Paged::window` è documentato come «*ritaglia una risposta già in memoria*», sessanta righe sotto la promessa che lo contraddice, e in mezzo non c'era nessun presidio. La riparazione sta nel posto che tutti attraversano: **`Paged::from_source`** conta la sorgente e costruisce **solo la finestra**, in una passata sola; `make` è un **argomento** e non un `.map()` sull'iteratore, perché `from_source` arriva in fondo per contare e un `.map()` verrebbe applicato a tutti — sarebbe la stessa linearità con un nome nuovo — e il `total` resta quello vero, perché uno `skip`/`take` da solo darebbe la finestra giusta e una barra di scorrimento che mente. Le strade sono adesso **tre**, e quale sia percorsa è un fatto misurato: paginare alla sorgente (tantivy), `from_source` (chi ha una mappa e un filtro), `window` (chi deve **ordinare** o **aggregare** prima di tagliare, dove la linearità non è uno spreco: è la domanda). Convertita **una** famiglia, e il perché è una sola è la parte utile: `Entries` è l'anagrafe, la fonte è un iteratore e la clonazione sta dentro `make` — dopo, quarantaquattro allocazioni su trecento note e quarantaquattro su seicento. Le altre due candidate sono cadute misurando: `Drafts` è stata convertita e **rimessa com'era** (la linearità sta a monte, in `drafts.read()`, e il `map` *sposta* il testo invece di copiarlo — guadagno zero, e la verifica del rosso l'ha confermato togliendolo senza che diventasse rosso niente in centoundici binari: un ramo di produzione che nessun presidio può difendere non si tiene); `Folders` costa **otto allocazioni per nota** — duemilaquattrocentotto su trecento, quattromilaottocentonove su seicento — e `from_source` non la salverebbe, perché il prezzo non sta nel costruire ciò che si butta ma dentro `make`, che per ogni cartella conta il proprio sottoalbero. È il **limite** della forma, e va detto: la finestra toglie ciò che si costruisce di troppo, non ciò che costa costruire ciò che si tiene. **Il secondo difetto l'ha trovato il banco contando**: la riapertura a caldo attraversava il confine `ceil(N/512)` volte con un lotto **vuoto** ogni volta, cioè a M5 una serializzazione per dire niente. Nessun altro presidio poteva vederlo, e non per distrazione: delle nove spie del repo che implementano `on_documents_indexed` nessuna conta le chiamate — o ignorano `docs`, o lo iterano registrando una riga **per documento** — appiattendo il confine del lotto che il §20.1 esiste per rendere raro — con quelle spie `FEED_BATCH = 1` è indistinguibile da 512, e la frase accanto alla costante («*riduce di tre ordini di grandezza gli attraversamenti di un reindex da 100k note*») era una promessa senza nessuno che la tenesse. **Buco dichiarato n. 6**: `due_ricerche_stanno_nell_indice_insieme` resta `#[ignore]`, e questo verbale smette di prometterle un banco che verrà. La proprietà è vera e non è esprimibile come conto — contare chi è *dentro* `query` non distingue il caso buono da quello cattivo, perché con un `Mutex` interno ci starebbero in due lo stesso, uno fermo ad aspettare; il compilatore non la vede (`Send + Sync` chiede che chiamare da N thread sia *lecito*, non *parallelo*) e un conto sui lock del sorgente prenderebbe la variante sbagliata, perché una query un lock lo prende davvero ed è giusto che lo prenda. Nello stesso giro è caduta una frase falsa che quel file dichiarava di sé: il doc di `commit` si diceva «l'unico punto in cui una query tocca un lock», mentre `text_query` prende i pesi a **ogni** ricerca di testo — e il commento accanto ai pesi chiudeva con «*il banco della seduta è lì per smentirmi se sbaglio*». Zero WIT (un costruttore su un tipo esistente), zero dipendenze nuove (l'allocatore che conta è venti righe di `std`). **Due zone cieche cercate costruendo il caso**: il banco interroga una famiglia sola (di qui la misura di `Folders`, fatta apposta per vedere se il verde nascondesse qualcosa — e nascondeva), e il contatore è per-thread, quindi se il lavoro caro emigra la misura non lo segue: stesso lavoro, milleduecento allocazioni sul thread del test e **sei** su un figlio, cioè una sparizione in verde. Sta scritta in testa al banco. Casella nuova: le **otto** famiglie che restano non hanno una riga di banco. | 2026-08-06 |
| [0114](0114-una-finestra-non-si-omette.md) | Una finestra non si omette, e ciò che resta fuori si dice | §2.9 — **chiude la voce**. La voce chiedeva virtualizzazione, rendering incrementale dell'anteprima e «il numero che dice se è ora», e arrivava con un'eredità: centocinquantasei righe di shell scritte da un giro interrotto a metà, verdi e **mai lette da nessuno**. Sono state **tenute**, perché l'idea sotto è la stessa forma della [0092](0092-una-base-si-dichiara.md): la finestra di una domanda al canale dati diventa il **primo argomento e non ha default**, così chiedere tutto è un caso da nominare invece di un'omissione che succede — e le tre superfici che ne approfittavano non le aveva scelte nessuno (la palette chiedeva l'anagrafe intera per riempire un `<datalist>` di suggerimenti, il menu «nuovo spazio» ogni cartella a ogni profondità, un livello d'albero tutte le note della cartella). Ciò che una finestra lascia fuori l'albero **lo dice**, perché un livello troncato in silenzio è una cartella che sembra averne duecento quando ne ha seimila. **La sesta premessa era falsa, e non era una premessa sul codice**: un commento prometteva che `SENZA_FINESTRA` «si conta da fuori (`conteggi.mjs`, `finestre-aperte`)» e quel conto **non esisteva** — un presidio promesso e mai scritto, che è peggio di nessun commento. Renderlo vero è costato una voce nel registro; renderlo difendibile è costato un **tipo**: con una costante di stringa il tipo era il letterale, quindi scrivere `"senza-finestra"` al posto della costante compilava e apriva una finestra in più senza una riga in più da contare. Adesso è un `unique symbol`, che non si scrive: si nomina — dal conto al compilatore. **La virtualizzazione non è quello che si è fatto, e sta scritto**: virtualizzare vuol dire disegnare ciò che si vede, e cosa si vede è una domanda di layout che in `happy-dom` non esiste (buco n. 5 della [0112](0112-un-e2e-contro-un-host-finto-prova-il-cablaggio.md)); si è fatta la metà che sta *prima* del layout, cioè quella che la voce stessa nominava. Della **seconda casella** una metà si è fatta e una è **dichiarata**: `loading="lazy"` sulle immagini si è potuto fare senza layout — la shell non calcola cosa si vede ma può dichiarare che non vuole deciderlo lei — e sta nel punto unico in cui dell'HTML entra nella webview (§3.6), accanto al `rel`/`target` che un `<a>` esterno riceve dalla stessa riga; sugli embed il *lazy* vuole ancora layout, ma misurando è saltato fuori di peggio, che layout non ne vuole: la **profondità** dell'idratazione era limitata e la **larghezza** no, e le due si moltiplicano — dieci note che ne trascludono altre dieci facevano partire diecimila `render_embed` per un documento che di note distinte ne nomina venti. Il memo dura la singola idratazione e la sua correttezza è la promessa che `FormatProvider::render_html` fa di sé. Il **rendering incrementale** resta casella residua per due misure: la sua precondizione ([0018](0018-chi-vede-il-modello-parsato.md)) è lavoro di `fub-abi` e del provider, non di strato shell, e soprattutto **il suo primo cliente non esiste** — `updatePreview` gira quando cambia il documento e quando si entra in Lettura, mai a ogni battuta, perché `PaneMode` è esclusivo e si rende il sorgente *salvato*: se si rifà quando è cambiato il documento, non è cambiata nessuna parte, sono cambiate tutte. Il rimando al §17.1 per «il numero» era **scaduto**: la [0113](0113-il-banco-conta-le-operazioni.md) ha deciso l'opposto, e la forma che dice di più non è una soglia ma un'**uguaglianza** — due vault che differiscono per quattromila note disegnano lo stesso albero. **La verifica del rosso ha prodotto metà di questo lavoro**: dei quattro rami del diff ereditato uno solo era rosso e **tre erano verdi** — la finestra della palette (il banco filtra le domande dell'*avvio* e `listDocuments` è pigra: la riparazione non è un test, perché un pannello che si apre a comando lo si può sempre non aprire, ed è il conto che vede la variante che nessuno ha elencato), `altreCartelle` (il banco usava un vault **piatto**: metà del troncamento non era esercitata da niente) e il menu «nuovo spazio», interamente scoperto, stringa compresa. **Il difetto peggiore stava fuori dalla voce per il quindicesimo giro**: il type-check della shell **non ha un nome** — `vite build` traspila senza controllare i tipi, `vitest` nemmeno, e la coppia che tutto il repo cita (`npm run test && npm run build`) non lo esegue; il comando viveva solo dentro `ci.yml` come `npx tsc --noEmit`. Non è un sospetto: due errori di tipo scritti **dentro questo giro** sono passati verdi attraverso cinquecentotredici test e una build riuscita. Adesso `npm run typecheck` esiste e la CI esegue quello. Zero firme, zero tipi di là dal confine, WIT congelato intatto, nessuna dipendenza nuova. | 2026-08-06 |
| [0115](0115-la-verita-e-la-dichiarazione.md) | La verità è la dichiarazione, e i parser non erano due | §4.4 — **chiude la voce, e con lei la seduta 18**. La voce chiedeva chi fosse la verità fra il parser Lezer del buffer e il modello del file, e la risposta è **nessuno dei due**: con la [0104](0104-la-superficie-di-scrittura-si-presta.md) la live preview è *una* superficie di scrittura fra quelle possibili — un terzo può portare la sua — quindi le sue regex non possono essere la verità nemmeno per lei; con la [0018](0018-chi-vede-il-modello-parsato.md) il modello non può esserlo per un buffer sporco, che al di qua del confine non conosce nessuno. Ciò che resta, e che vale per **tutte** le superfici, è la **dichiarazione**: il vocabolario di `options::syntax` più il trigger di chi ne ha uno. **Il titolo della voce era la premessa più grossa, ed era falsa**: i parser non erano due. Dentro `frontend/` la stessa sintassi era scritta **tredici** volte in **sei** costrutti, in tre moduli che non si parlavano — `livepreview.ts` per decorare, `editor-commands.ts` per i gesti, `completions.ts` per il popup — e non erano tre copie ma tre regole **diverse**: su `> - [ ] x` la live preview disegnava una casella che `Mod-Enter` non spuntava e Invio non continuava (una regex accettava il prefisso di citazione, l'altra la leggeva come citazione); su `-··[ ] x` una vedeva una todo e l'altra un bullet (uno spazio esatto contro l'uno-a-quattro di CommonMark). E la terza copia divergeva **dal modello**: `RE_TAG` esigeva inizio riga o `[\s([{]` prima del `#` mentre la regola del contratto vieta solo un carattere **alfanumerico**, quindi su `vedi.#tag`, `(#altro)` e `_#terzo` il modello indicizzava un tag che l'editor non decorava; la classe del nome includeva `\p{M}`, quindi su un `#Café` decomposto — quello che arriva da un vault sincronizzato con macOS — il tag *visto* era più lungo di quello *indicizzato*. Una quarta divergenza è saltata fuori misurando: entrambe le regex accettavano solo `[ xX]` mentre il provider ha `relaxed_tasklist_matching` acceso apposta, quindi `- [/] in corso` è una task nel modello e non nell'editor — e `taskChecked`, che in `mirrored.ts` esiste **per** gli stati personalizzati e li documenta uno per uno, non poteva riceverne nemmeno uno. **La sola premessa vera è quella che regge la cura**: `SyntaxRuleSpec` porta davvero il trigger come dato, e il `==` di `livepreview.ts` era letteralmente `HighlightRule::spec().trigger` riscritto a mano — cosa che stava scritta nel doc della regola. Da qui quattro mosse. `SyntaxForm` nel contratto (un nome e un `Option<SyntaxTrigger>`, dove `trigger` assente non vuol dire «nessuna forma» ma «la forma è nella grammatica del provider»: dice **dove finisce** ciò che si può generare) servito da `Workspace::syntax_forms`, che è `format_of` per chi disegna invece di parsare. `sintassi.generated.ts` emesso da un **montaggio vero** (in `fub-host` per la ragione di `le_view_ufficiali.rs`: la domanda non è quali regole esistono ma quali sono **montate**), così `==` non è più scritto nella shell — provato cambiando il trigger a `%%` e vedendo cinque test diventare rossi. Il riconoscimento del `#tag` **sale nel contratto** (`fub_abi::rules::tag::scan_tags`), e l'argomento per cui non poteva stare in `fub_sdk::scan` era già scritto tre righe più sotto, nel doc di `parse_wikilink_inner`; da lì diventa una regola **rispecchiata** in `mirrored.ts`, legata dalla fixture. E nella shell la sintassi si riconosce in **un posto solo**, `rules/sintassi.ts`, con le tre specie di regola dichiarate: generata, rispecchiata, scritta una volta (il wikilink e la voce di lista — quest'ultima è una regola di *gesto*, che nel modello non esiste, quindi una gemella sarebbe una gemella di niente). **Il canale a runtime non c'è, ed è la casella residua**, con il perché misurato invece che addotto: il generato è compilato, quindi conosce le regole del core e non quelle di un plugin di terzi; la rotta giusta è una variante di `IndexQuery` e non un comando IPC (che sarebbe visibile alla shell e non a un plugin, cioè il contrario di ciò che la 0104 ha appena deciso), ma chi serve `IndexQuery` è `CoreIndex`, che ha `Arc<FormatRegistry>` e **non** il `SyntaxRegistry` — quello vive dentro `DocumentStore`, sotto il prestito di chi scrive, ed è attraversato a **ogni** parse: condividerlo è una decisione sulla concorrenza del kernel ([0024](0024-chi-legge-non-aspetta-chi-legge.md)), non un pezzo di questa voce. **Il corpus è la parte che si paga adesso**: `il_corpus.rs` emette ciò che il modello dice delle **stesse** sorgenti che già confronta col modello e che `transfer_e2e.rs` già fa uscire e rientrare da un vault, e `corpus.test.ts` ci passa la passata della shell — la mossa della [0060](0060-il-modello-dice-il-vero-sui-byte.md) applicata all'altro asse. Sta dentro `il_corpus.rs` e non in un binario suo perché `corpus/mod.rs` non ha un `allow(dead_code)` di proposito, e un terzo binario che ne usasse una parte avrebbe reso rumoroso quel guardiano. Tre divergenze **dichiarate**: il modello inventa un tag dentro `[[#Sezione]]` (già dichiarata dal lato Rust, e qui ha ragione la shell); un `\r\n` diventa `\n` nel buffer di CodeMirror e da lì gli offset scalano di uno, mentre un `\r` **nudo** non sposta niente — quale delle due forme costasse non era ovvio, ed è emerso dichiarando tutti e tre i casi «cr» e vedendone **due** diventare rossi perché non divergevano; la shell decora dentro il **frontmatter** (`relazione: "[[Nota]]"` diventa un wikilink cliccabile), e non si ripara qui perché per escludere il frontmatter la shell dovrebbe **riconoscerlo**, cioè scrivere una seconda grammatica di `fub:frontmatter` — il moltiplicatore; nel file generato quel confine si vede a occhio, `fub:frontmatter` ha `trigger: null`. **Il difetto peggiore stava fuori dalla voce per il sedicesimo giro, e sono due.** `Mod-click` su `[[Nota#Sezione]]` apriva la nota **in cima**: la live preview faceva `interno.split("#")[0].trim()` e buttava heading e blocco, mentre lo stesso link cliccato in Lettura arrivava alla sezione perché di là il payload viaggia nei `data-wikilink-*` del render Rust — due risposte per lo stesso link, cioè la §4.4 nella sua forma più piccola; `openWikilink` di `document.ts` accettava già i tre campi dalla [0049](0049-una-posizione-dentro-un-documento.md), e si fermavano un piano sopra. Il secondo è **nel presidio che stavo scrivendo**: il corpus saltava i casi che il modello legge **vuoti**, cioè l'unica forma in cui si vede una passata che riconosce *di troppo* — misurato, togliendo alla shell l'esclusione delle righe di codice il corpus restava verde su tutte e sessantatré le sorgenti. Adesso nessun caso si salta e il corpus ha tre sorgenti nuove fatte apposta (un recinto e un codice inline che **contengono** ciò che fuori sarebbe sintassi, più i tre confini della regola dei tag sulla stessa riga). **Rosso**: sette rami, uno alla volta. Uno solo ne ha reso rosso **uno** — il bersaglio del wikilink — perché la via del click non ha un presidio (il ponte e la webview sono il buco n. 5 della [0112](0112-un-e2e-contro-un-host-finto-prova-il-cablaggio.md)): là dove non arriva un test il difetto si toglie invece di presidiarlo, e il nome dell'attributo che viaggiava in due grafie (`"data-fub-page"` di qua, `dataset.fubPage` di là, trecento righe in mezzo) è diventato una costante. `check-prosa` non sapeva leggere **«una»**: in italiano il numero concorda col nome, e le cose che questo repo conta sono per metà femminili — senza quella riga l'unico modo di scrivere il vero era scriverlo sbagliato. WIT congelato **intatto** (un record nuovo che nessuna interfaccia espone ancora), nessuna dipendenza nuova, nessun comando IPC nuovo. Zone cieche dichiarate: il generato conosce il core e non i terzi; il corpus confronta tre famiglie e non i costrutti che Lezer conosce da sé; la voce di lista non ha gemella, quindi il suo unico presidio è il banco della shell; e **nessun attore vede una quattordicesima regex** scritta domani in un modulo nuovo — il conto che la prenderebbe leggerebbe i commenti, ed è la specie di conto cieco che il giro precedente ha misurato dodici volte. | 2026-08-06 |
| [0116](0116-lo-scope-di-una-chiave-segue-la-vita-di-chi-la-dichiara.md) | Lo scope di una chiave segue la vita di chi la dichiara | §16.3 — **chiude la voce, e con lei la roadmap di M4**. Delle due caselle rimaste, la prima — lo **split in crate** — non era lavoro in attesa: era lavoro **tenuto fuori con una condizione** («il primo import fra due moduli di feature che non sia un link di documentazione»), e dalla [0073](0073-una-condizione-che-nessuno-valuta.md) quella condizione la valuta un banco che quando diventa rosso dice *la voce si è sbloccata*. Verificata oggi: verde, i moduli sono dieci e non si nominano fra loro. Quindi la voce **si chiude lasciando quella casella dov'è**, che è il criterio già scritto in `todo.md` — *una voce aperta è lavoro da decidere, una casella residua è lavoro già deciso da fare* — e qui non resta niente da decidere. La seconda casella era il cliente arrivato dalla [0090](0090-una-sequenza-e-una-modalita-che-scade.md): **la scorciatoia di un comando di shell non si riconfigura**. Le sue premesse erano tutte **vere** (prima volta in molti giri): i cinque ostacoli che la 0090 aveva misurato per la strada del `CommandProvider` di prossimità reggono uno per uno. A cambiare è la conclusione: **non sono cinque ostacoli, è uno visto da cinque lati** — nascono tutti dall'aver chiesto un *comando* quando ciò che serviva era una *chiave*. I primi tre esistono solo se il comando di shell entra nel registro del kernel, dove non deve entrare (un comando che il kernel elenca e non sa invocare è la bugia dentro il registro che la [0077](0077-una-scorciatoia-e-una-chiave.md) rifiuta); il quarto — la fixture cieca a un provider nuovo — non si chiude aggiungendo un confronto ma **spostando la sorgente**; e il quinto, che la 0090 chiamava una contraddizione, non è una contraddizione della strada: è **la regola mancante**. La regola: **lo scope di una chiave segue la vita di ciò che la dichiara.** Un comando del kernel esiste finché un vault è montato — lo dichiara un provider, che si registra per vault — quindi la sua chiave sta nel vault e viaggia con lui ([§23.13](0100-i-tasti-che-arrivano-da-fuori.md)); un comando della shell esiste finché l'app è aperta, e `shell.vault.open` è la prova per assurdo, perché una sua chiave di vault nascerebbe solo *dopo* che un vault è aperto e vivrebbe dentro il vault che serve ad aprire. Le `keys.shell.*` sono quindi **di macchina**, non per eccezione ma per conseguenza. **Il difetto peggiore stava fuori dalla voce per il diciassettesimo giro di fila**, e l'ha scoperto la domanda *dove vive questa chiave quando la finestra è vuota*: `SettingScope::Machine` **manteneva la sua promessa a metà**. Il suo doc-comment dice, del log, che «deve valere anche quando un vault non si apre, che è precisamente il caso in cui serve» — e il *valore* stava davvero nel file della macchina, ma lo **schema** stava dentro il `SettingsStore` di un vault, cioè nell'unico posto che sparisce esattamente in quel caso: `log.level` era leggibile e scrivibile **solo con un vault aperto**, perché `set_setting` passa da `with_session` e la lettura da `IndexQuery::Settings`, che vuole un `Workspace`. Chi apriva Fub su una finestra vuota per capire perché un vault non si apre non poteva alzare il livello del log. Da cui, in ordine: `MachineSettings` acquista uno **schema** (e rifiuta una spec di scope `Vault`, che risponderebbe col default per una chiave il cui valore vero sta altrove); senza vault il **canale dati** serve le righe di macchina, e la regola sta in `Host::query_index` e non nel comando IPC, perché una regola che un banco non può interrogare non è presidiabile; **scrivere si dice lo stesso**, con l'host che emette `setting_changed` con `Actor::User` al posto del `Workspace` che non c'è — senza quella riga una scorciatoia rimappata a finestra vuota resterebbe scritta, riletta e mostrata giusta mentre la tastiera risponde a quella vecchia; la **tabella degli accordi della shell passa in Rust** e di là arriva generata (`shell-keys.generated.ts`), che è la [0056](0056-un-elenco-che-e-la-sorgente.md) applicata dove non lo era — e da lì i due registri si guardano insieme **anche di qua**, dove il registro del kernel è in casa; e il pannello disegna le righe di shell con la **stessa** `disegnaRiga` di tutte le altre, mettendoci il nome, che di là non c'è. `i_cataloghi` ha detto di no — «un'etichetta cablata dentro uno schema è prosa che nessun catalogo raggiunge» — e ha ragione senza coprire questo caso: la chiave la dichiara il core, ma la frase l'ha scritta la shell ([0040](0040-chi-localizza.md)), e portarne una copia qui sarebbe lo stesso titolo tradotto due volte in due posti che nessuno confronta. L'etichetta è quindi l'**id**, che è un dato, e il presidio si è **circoscritto** invece di indebolirsi (la mossa della [0071](0071-una-feature-si-spegne-dove-si-dichiara.md)): l'esenzione si calcola da `shell_keybinding_specs()` e si pretende **esatta**. **Rosso**: dieci rami, tutti e dieci hanno reso rosso qualcosa, più l'ordine dell'avvio. Uno vale per sé: mettendo un accordo di shell su `Mod-Alt-z` diventano rossi il banco Rust dei due registri e il gemello TS, mentre con `Mod-n` non succede niente — i comandi ufficiali del kernel che dichiarano un accordo sono **uno**, e nemmeno quello lo sapeva nessuno. Zone cieche dichiarate: un id in tabella che nessun pannello registra resta verde da tutte e due le parti (il verso stretto lo prende il compilatore, il largo no); e **il banco e2e della shell non smonta ciò che monta** — `document` è uno per file e nessuno toglie i suoi ascoltatori di tastiera, quindi un tasto premuto nel nono gesto rispondeva con la tastiera dell'ottavo e il presidio passava verde *anche togliendo la riga che doveva difendere*: le due asserzioni che ne dipendevano sono state riscritte sul registro invece che sul tasto. WIT congelato intatto, nessuna dipendenza nuova, nessun comando IPC nuovo. | 2026-08-06 |
| [0117](0117-un-termine-non-si-sovrappone-a-se-stesso.md) | Un termine non si sovrappone a se stesso | Difetto di [«I difetti da correggere»](../todo.md) — `occurrences::locate` prometteva occorrenze «senza sovrapposizioni fra occorrenze uguali» e ne dava di sovrapposte: `aa` in `aaaa` erano tre span, e il `dedup` non ne toglieva nessuno perché **sovrapposti non è uguali**. La premessa scritta accanto alla riga colpevole era **falsa**, e sembrava vera perché il banco che nomina la passa lo stesso: il riavvio da `span.start` diceva di servire alla sovrapposizione fra termini *diversi* (`arch` dentro `architettura`), ma quella non dipende da lì — i termini si scandiscono uno per uno e `from` torna a zero a ogni termine, quindi la coda saltata non poteva mai far perdere l'occorrenza di un altro. Comprava solo la sovrapposizione di un termine **con se stesso**. Si è scelto di cambiare il comportamento e non la frase perché a decidere è **chi consuma gli span**: nessuno evidenzia con questi (gli `highlights` della riga vengono da chi indicizza), ma ogni occorrenza diventa una **riga cliccabile** dell'elenco (`frontend/src/rules/risultati.ts:44`) che porta il cursore a `span.start` (`frontend/src/panels/doc-search.ts:158`, `frontend/src/panels/search.ts:165`) — cercare `--` in un righello `\|-----\|` offriva quattro righe da premere per due posti a cui andare, con la seconda dentro la prima, e i doppioni mangiavano il tetto `MAX_PER_DOC`, che è del **documento**. Precedente già in casa e mai riconosciuto come decisione: `fub_features::commands::occurrences` riparte da `end` e lo dice. La regola: **dentro un termine le occorrenze non si sovrappongono, fra termini diversi sì** — il confine è la scansione, che riparte da `span.end` e comincia da zero. `next_boundary` sparisce con la riga che serviva. **Rosso**: rimessa la riga vecchia, il banco nuovo dice `[0,2),[1,3),[2,4)` contro `[0,2),[2,4)`; e nell'altro verso il banco di `arch` resta verde — la prova che conta è la coppia, perché un corpus può essere cieco a chi riconosce **di troppo** tanto quanto a chi riconosce di meno. | 2026-08-06 |
| [0118](0118-una-chiusura-non-cattura-cio-che-il-riconciliatore-aggiorna.md) | Una chiusura non cattura ciò che il riconciliatore aggiorna | Difetto di [«I difetti da correggere»](../todo.md) — un `text_input` riusato dal riconciliatore (§2.8) aggiornava il valore e mandava l'azione del **primo** disegno: i suoi due ascoltatori si registravano una volta sola, con l'`ActionRef` catturato nella chiusura. Non è l'accumulo di ascoltatori che era stato riferito, ed è peggio: un ascoltatore accumulato fa due volte la cosa giusta e si vede, uno invecchiato fa una volta la cosa sbagliata. La descrizione era **falsa per difetto**: non erano i campi di testo — `scatta` lo chiamavano sette specie di nodo più i bottoni di un `radio`, e in `aggiorna` nessuna delle sette rilegava l'azione; e un campo che *perdeva* l'azione continuava a mandarla, che è il gemello esatto della riga sul focus che `collega` ha in casa dalla prima stesura. Delle due riparazioni si è scelta quella che **toglie alla chiusura ciò che invecchia**, non quella che toglie e rimette l'ascoltatore a ogni giro: la seconda funziona e va rifatta a mano dal prossimo che ne aggiunge uno, senza che nessuno gli dica che doveva. L'ascoltatore cattura ora solo l'elemento e il nome dell'evento — due cose che non cambiano mai — e legge azione e handler da una mappa che la riconciliazione aggiorna. Tre conseguenze: `ascolta` è l'**unica porta** ed è idempotente per evento (senza quella proprietà la cura avrebbe prodotto davvero l'accumulo che la diagnosi descriveva); `azioniDelCampo` è l'unico posto dove un campo prende i suoi ascoltatori, e la chiamano il disegno e la riconciliazione con la stessa riga, quindi il terzo ascoltatore che qualcuno vorrà vale in entrambe le vite del campo; e **`invia` non prende più un `ActionRef`** — un'azione non si può passare, quindi non si può catturare, quindi non può invecchiare, che è la forma di `SENZA_FINESTRA` nella [0114](0114-una-finestra-non-si-omette.md) applicata a un valore invece che a una costante. La regola: **in un riconciliatore un ascoltatore non cattura ciò che il riconciliatore aggiorna, lo legge quando l'evento scatta** — ed è la regola che la testata di `node.ts` scrive dei *valori* dal primo giorno («lo stato parallelo è la seconda verità che diverge appena il riconciliatore tocca un nodo»), mai applicata alle azioni. **Rosso**: il presidio è un test unitario sul riconciliatore e non un caso del banco e2e, per la zona cieca dichiarata dalla [0116](0116-lo-scope-di-una-chiave-segue-la-vita-di-chi-la-dichiara.md); rimesso il file vecchio, quattro casi su cinque diventano rossi e i due rami sono rossi separatamente (`change` e Invio dicono `prima` invece di `dopo`, tre riconciliazioni di fila dicono `uno` invece di `quattro`, il campo che perde l'azione la manda lo stesso). Il quinto resta verde col codice vecchio e sta scritto: non presidia il difetto, presidia la forma nuova. Ogni caso verifica **anche** che l'elemento sia lo stesso di prima, o il presidio passerebbe a vuoto il giorno in cui i campi si ricostruissero. Zone cieche dichiarate: l'`ActionHandler` di un sottoalbero riusato resta quello del primo montaggio e oggi non morde per una coincidenza che nessuno aveva dichiarato; e nessun attore vede un `addEventListener` scritto a mano domani dentro un ramo di `disegna`. Zero firme, WIT intatto, nessuna dipendenza nuova. | 2026-08-06 |
| [0119](0119-il-piano-si-fa-in-lettura-e-si-applica-in-scrittura.md) | Il piano si fa in lettura e si applica in scrittura | Difetto di [«I difetti da correggere»](../todo.md) — il lotto del watcher prendeva `workspace.write()` e sotto quel lucchetto faceva il parse di ogni file cambiato **e** `flush_indexes()`: chi legge aspettava un'I/O che non lo riguarda. È la regola della [0024](0024-chi-legge-non-aspetta-chi-legge.md) applicata alla porta da cui il vault cambia da fuori, e le due metà non si comportano allo stesso modo. Il **parse** esce dal prestito esclusivo perché ci stava solo per abitudine del chiamante (`DocumentStore::parse` e `Vault::read` prendono già `&self`): nasce `Workspace::plan_sync` sotto `&self`, che restituisce un `ParsedChange` **opaco** — chi ne tiene uno ha per forza già rilasciato il prestito, perché il tipo non ne porta con sé nessun pezzo — e `sync_path_prepared` lo applica. Il **flush** invece *non può* uscire, ed è la premessa falsa del difetto: `IndexProvider::flush` riceve un `&mut dyn HostApi`, che il kernel costruisce su `&mut Workspace` (0021) — gli si dà una fase sua, così fra la mutazione e la durevolezza il lucchetto si rilascia e chi aspetta non aspetta più il lotto intero. Il difetto peggiore **nasce dalla riparazione**: fra le due fasi ci può entrare un salvataggio dell'utente, e applicare un modello parsato prima di quella scrittura la cancellerebbe dalla memoria del kernel lasciandola sul disco — quindi **il piano dichiara cosa credeva di sapere** (l'impronta che l'anagrafe aveva) e chi applica lo verifica, e se non combacia rifà la strada intera. La regola: in `fub-host` un'I/O non si fa sotto il prestito esclusivo, e ciò che si è letto porta con sé la versione dello stato su cui è stato letto. Vale oltre il watcher: dei ventuno `write()` del crate ce n'è un secondo con la forma esatta, `Runner::avanza_apertura` → `Workspace::index_batch`, che è la scansione dell'apertura (nominato, non toccato). **Rosso**: il presidio non cronometra niente — un formato di prova si ferma dentro `parse` e il test fa `try_read()` nel mezzo, sincronizzato da un canale e non da uno `sleep`; con `read()` riscritto in `write()` nella fase 1 il prestito non si concede, e tolto il confronto delle impronte la scrittura dell'utente sparisce dall'anagrafe. L'ordine dei lotti non ha un test perché ha un **tipo**: `ExternalSync::batch` prende `&mut self`, e due lotti sullo stesso sincronizzatore non compilano. | 2026-08-06 |
| [0120](0120-un-lucchetto-avvelenato-si-dice-una-volta.md) | Un lucchetto avvelenato si dice una volta | Difetto di [«I difetti da correggere»](../todo.md) — il runner scriveva `.expect("workspace avvelenato")`, la colla Tauri `.unwrap()` nudo: alla stessa domanda — *cosa si fa quando questo lucchetto è avvelenato?* — rispondevano posti diversi in modi diversi, e nessuna risposta stava scritta. Il difetto non era il panico: era la domanda senza risposta, e il panico ripetuto a ogni IPC senza una riga che dicesse perché. Le risposte non erano due ma **tre**: `config.rs` scriveva `.unwrap_or_else(\|e\| e.into_inner())`, cioè **ricuperava** — ed è il sito che ha reso la cosa decidibile invece che una questione di gusto, perché là il lucchetto serializza due variabili d'ambiente in un test e non c'è niente di storto da lasciare. Da cui il criterio: **la politica non è dei lucchetti, è di cosa il lucchetto protegge**. Un `RwLock` si avvelena *solo* se pania chi tiene il prestito esclusivo ([0024](0024-chi-legge-non-aspetta-chi-legge.md)), quindi «avvelenato» vuol dire **una mutazione fermata a metà**: `into_inner` farebbe passare per buono un indice alimentato a metà e un lotto che nessuno chiude — chi cerca riceverebbe risposte *sbagliate* invece che *mancanti*. Irrecuperabile, dunque; ma detto **una volta** con un messaggio, e poi un `PluginError` a ogni chiamata invece di un panico muto. La porta è `Custodia<T>` — `RwLock` privato al modulo, `.lock()` non esiste — ed è generica perché il secondo chiamante la eredita gratis: **settanta** siti misurati contro i ventisei del campione, di cui trentaquattro sul workspace. Premessa caduta a metà lavoro: **un `RwLock` non è «un `Mutex` con un permesso in più»** — `Mutex<T>` è `Sync` per `T: Send`, `RwLock<T>` solo per `T: Send + Sync`, e appena la mappa delle sessioni è passata dietro la porta il compilatore ha detto che il `Box<dyn VaultWatcher>` lì dentro contava su una proprietà che nessuno aveva scelto (da cui `VaultWatcher: Send + Sync`, e due IPC su due vault diversi che smettono di serializzarsi). Dove la firma un canale ce l'aveva, il veleno ci è entrato (gli errori di chiusura); dove non ce l'aveva, la risposta sta accanto alla riga: **il pool dei job si ferma**, il rilevatore smette di sincronizzare, gli elenchi di comodità rispondono «non ne so». **Rosso**: struttura e comportamento, perché si rompono separatamente — un `RwLock` rimesso a mano diventa rosso col file e la riga, e la porta rimessa a paniare **non fa fallire** il caso comportamentale: gli aborta il thread, che è ciò che l'app faceva a ogni `invoke`. La zona cieca non è teorica, è **successa addosso**: `Custodia::read` rende un `Result`, e su un `Result` si scrive `.unwrap()` — la sostituzione automatica su `fub-app/src/lib.rs` non ha agganciato, il crate ha compilato verde con quattordici `.unwrap()` sulla porta nuova e né il compilatore né clippy potevano dirlo. Da lì il secondo conto, che è un conto perché il tipo qui non può. | 2026-08-06 |
| [0121](0121-l-id-del-contenuto-vive-in-un-altro-spazio-di-nomi.md) | L'id del contenuto vive in un altro spazio di nomi | Difetto di [«I difetti da correggere»](../todo.md), l'**ultimo** della sezione — `id` e `class` del contenuto di una nota sono nell'allowlist del varco unico (§3.6) **apposta**, ma un `id` è globale nel documento e la shell cerca i propri elementi per nome: `getElementById` dà il **primo** in ordine di documento, quindi una nota che porti quel nome se lo prende. Il rimedio non è restringere l'allowlist ma **separare i due spazi di nomi**. Premessa del difetto **falsa**: «una nota che contenga HTML con uno di quegli `id`» — l'HTML grezzo dentro un markdown non torna markup, il parser lo tiene come `custom_kind::HTML` e il renderer lo degrada a un `div` vuoto, e sta scritto in due posti. Sembrava vera perché il sanitizzatore esiste, e l'unica lettura naturale di un sanitizzatore è che dell'HTML non nostro passi (passa: da un tema, da un `UiNode::Html`, da un blocco custom a M5). La via vera è **peggiore**: l'ancora di un heading **è il suo slug** (`parse.rs`), quindi `## save-state` diventa `<h2 id="save-state">` senza una riga di HTML — un titolo basta. Secondo campione contro censimento del giro: il difetto nominava quattro nomi, i nomi della shell misurati sono **sessantuno**, e fra questi ci sono le famiglie *generate* da `identificatore()` (`albero-1`, `campo-3`) che nessuno ha scelto e che una nota può contenere per caso. Seconda premessa caduta: **non c'è codice nostro che risolva un'ancora nel DOM** — `openWikilink` passa dal kernel e arriva per offset in byte ([0049](0049-una-posizione-dentro-un-documento.md)); chi risolve `[testo](#sezione)` è il **browser**. Da cui la riparazione: entrambe le metà stanno dentro il sanitizzatore, e il prefisso non è una costante che due rami si copiano — sopra c'è **una** funzione, `valoreDellAttributo(nome, valore)`, che il cammino sul DOM chiama una volta sola per ogni attributo: i due lati non possono divergere perché non sono due posti. Il `#` nudo del segnaposto wikilink resta nudo; `class` resta com'è, perché non è un nome globale. **Rosso** in tre modi diversi perché si rompono in modi diversi: tolta la riscrittura la collisione è rossa (col contenuto messo per primo di proposito, o il presidio passerebbe da solo); prefissato l'`id` e **non** il frammento — la riparazione fatta a metà — sono rosse le due prove dell'ancora più l'identità fra i due lati, che è un test puro e si rompe per prima; e il **conto** che legge `index.html` e i moduli col `?raw` diventa rosso col prefisso cambiato in `save-`, con soglia perché un conto che smettesse di trovare i nomi passerebbe a vuoto. I presidi del DOM stanno in un file a sé perché l'ambiente di vitest è per file — e la riga «il cammino sul DOM non è testato, questa shell non ha un DOM» era una delle false della [0112](0112-un-e2e-contro-un-host-finto-prova-il-cablaggio.md). Zero firme, nessuna dipendenza nuova, `crates/` intatto. | 2026-08-06 |
| [0122](0122-una-sorgente-non-degrada-si-rifiuta.md) | Una sorgente non degrada: si rifiuta | Difetto di [«I difetti da correggere»](../todo.md) — `Inline::Custom { .. } => {}` in `serialize.rs`, «il gemello inline del frontmatter perso». La domanda vera non era *come degradare* ma **se degradare**: `render_html` scrive una **proiezione** e la [0017](0017-chi-disegna-cio-che-il-core-non-conosce.md) ha risposto giusto per lei — perderci qualcosa costa una visualizzazione; `serialize` scrive **la sorgente**, e lì lo stesso gesto costa il file dell'utente. Da cui la regola che vincola ogni `FormatProvider` che verrà: **una proiezione degrada, una sorgente si rifiuta** — chi non conosce i delimitatori di una `SyntaxRule` (§3.1) non è autorizzato né a inventarli né a buttarli, e torna un `Err` che nomina il kind. Scartata la strada dell'`attrs` verbatim timbrato dal kernel (che avrebbe reso la scrittura totale per costruzione, come lo `span` di `SyntaxMatch`): **il modello si modifica**, e una sorgente ferma nel nodo riscriverebbe il vecchio al posto del nuovo — una perdita peggiore, silenziosa *e* plausibile; per `frontmatter-unparsed` regge solo perché quel blocco è opaco per definizione. Campione contro censimento: il difetto ne nominava **uno**, misurandoli sul corpus erano **nove**, e **sette non hanno a che vedere con `Custom`** — l'`^id` di blocco non scritto in sette varianti (l'unica perdita che si vede da *fuori* del documento: toglie il bersaglio a `[[Nota#^abc123]]` senza che il file sembri cambiato), il blocco `html` che il provider produce da solo e riscriveva come zero byte, il codice inline spezzato dai propri backtick, l'elenco annidato appiattito. Premessa del difetto **falsa**: «l'app risalva la nota e il testo sparisce» — quel giro **non esiste**, `serialize_non_riscrive.rs` tiene un elenco chiuso dei chiamanti e nessuno è codice di produzione; il danno descritto era impedito da un altro presidio, e sembrava vero perché la strada comoda è fatta di quattro chiamate che compilano tutte. Riparazione giusta, urgenza no — ed è il secondo caso. Tre attori scelti per caso: **il compilatore** (via i `..` tolti da ogni pattern: un campo nuovo in `Block` non compila qui, che è esattamente com'era nata persa `anchor`), **il conto** (giro completo sul corpus su tre proprietà che si rompono separatamente — testo, forma dell'albero, ancore) e **il test** (l'`Err` nomina il kind). **Rosso** sette volte su sette reversioni distinte, nessuna cieca. Limite dichiarato: l'ancora esplicita di un *heading* resta irrecuperabile perché nell'albero c'è lo slug, e si ripara nel modello. | 2026-08-06 |
| [0123](0123-lo-slug-di-un-titolo-e-un-posto-non-una-parola.md) | Lo slug di un titolo è un posto, non una parola | Difetto di [«I difetti da correggere»](../todo.md) — due `## Note` nella stessa nota davano lo **stesso** `id`, e `getElementById` come `outline.iter().find(…)` restituiscono il primo: la seconda sezione non era nominabile da nessuna sintassi, e il link ci atterrava sopra senza dire niente. La domanda vera: **lo slug è una funzione del testo di un titolo?** No — è *un posto in un documento*, e «è già preso?» è una domanda sul documento, cioè **stato**. Finché la risposta era sì il banco di conformità asseriva `h.slug == heading_slug(&h.text)` titolo per titolo, cioè **vietava** la disambiguazione a chiunque scrivesse un provider: il presidio che teneva ferma la regola teneva fermo anche il difetto. Regola scelta: il **primo** tiene la forma pura — così un documento senza omonimi ha gli stessi id di ieri, e nessun link già scritto cambia destinazione — e dal secondo in poi si numera in coda (`note-1`), che è la consuetudine di GitHub ma soprattutto è **raggiungibile scrivendola** (`[[Nota#Note 1]]` passa da `heading_slug`). Il numero è la prima forma **libera** e non un contatore per testo: con un contatore, un `## Note 1` vero accanto a due `## Note` avrebbe prodotto due `note-1`, cioè il difetto spostato di una riga. Proprietaria è il **modello**, e il codice lo diceva già: `render.rs` non costruisce nessuno slug, stampa `block.anchor()` — premessa **falsa** dell'ancoraggio del difetto, che mandava la riparazione proprio dove il resolver sarebbe stato obbligato a rifarla; sembrava vera perché il sintomo si vede nell'HTML, ma il renderer è il testimone, non l'autore. Forma della [0121](0121-l-id-del-contenuto-vive-in-un-altro-spazio-di-nomi.md) su un'altra coppia: chi assegna (`HeadingSlugs`) e chi cerca (`heading_matches`) stanno adiacenti nel contratto, perché i posti che risolvevano un `#Sezione` erano **due e diversi** — `position_in` per slug, `section_of` per chiave di risoluzione su slug **o** testo — d'accordo solo per la ragione sbagliata (atterravano entrambi sul primo omonimo) e destinati a separarsi sulla disambiguazione, `resolution_key("Note 1") = "note 1" ≠ "note-1"`. Nel parser **una** chiamata usata due volte: chiamarla due volte darebbe `note` all'outline e `note-1` al blocco, cioè uno stato letto due volte è due stati. Sei siti misurati contro l'uno dichiarato, e i due che contavano — il banco di conformità e il resolver dell'embed — stavano **fuori** dalla frase; lato shell zero, perché il frontend prefissa i nomi ma non li genera. **Rosso** quattro volte su reversioni distinte, fra cui l'allocatore chiamato due volte (il difetto che la riparazione poteva generare) e ognuno dei due resolver rimesso da solo alla vecchia regola. Il corpus prende una voce (`due heading omonimi`): senza, il banco riscritto sarebbe passato senza aver provato niente. Limite dichiarato: l'ancora esplicita di un heading (`## Titolo ^xyz`) resta senza effetto sull'id reso — è una firma, non un difetto, perché `anchor` è uno solo e i candidati sono due. | 2026-08-06 |
| [0124](0124-una-fetta-dell-apertura-e-un-piano-anche-lei.md) | Una fetta dell'apertura è un piano anche lei | Il **secondo sito** che la [0119](0119-il-piano-si-fa-in-lettura-e-si-applica-in-scrittura.md) aveva nominato e non toccato: `Runner::avanza_apertura` teneva `workspace.write()` attraverso `Workspace::index_batch`, che legge e parsa una fetta di documenti dal disco. Il primo sito morde quando qualcuno tocca dei file da fuori; questo morde **all'avvio**, e su un vault grosso è l'app ferma mentre il centro attività racconta un progresso a un'interfaccia che non lo può leggere. Censimento: dei venticinque `write()` sul workspace ce ne sono **quattro** che attraversano un'I/O — quattro contro l'uno dichiarato — e tre hanno **la stessa causa unica**, che non è un difetto ma una firma: `HostApi` si ottiene solo da un `&mut Workspace` (0021), quindi flush, `collect_doc_data` e `Host::read_version` ci restano sotto. Il quarto è il più netto: `VersionStore::read` prende già `&dyn HostApi`, manca solo un modo di ottenerne uno da `&self`. Nel kernel `plan_batch` + `index_batch_prepared` + `ParsedBatch`, che è il `ParsedChange` di un lotto e porta lo stesso nome apposta. **Tre estensioni della 0119, non tre ripetizioni**: l'impronta è **per documento** e non per lotto (una fetta ne porta cento, e buttarla tutta perché l'utente ha salvato *una* nota vorrebbe dire rileggere novantanove file per niente); un documento invecchiato **si butta e basta** invece di rifare la strada, che è il verso opposto alla 0119 — là il piano scartato era l'unica notizia che il file fosse cambiato, qui l'impronta è cambiata perché qualcuno ha scritto *attraverso il kernel*, e quindi ha già parsato, già alimentato gli indici e già timbrato l'anagrafe; e `index_batch` diventa `pub(crate)`, cioè da fuori dal kernel **la forma sbagliata non compila** — il passo che la 0119 non aveva fatto su `sync_path`. La finestra qui è larga: il vault è utilizzabile dalla fine della scansione, quindi «l'utente salva mentre indicizza» non è una patologia ma il comportamento normale di chi apre l'app e comincia a scrivere — senza il confronto delle impronte questo commit avrebbe scambiato una lentezza con una **perdita silenziosa di testo**. Premessa caduta: le due cose che `index_batch` faceva nello stesso `for` sembravano una — leggere e mutare l'anagrafe — e separarle in due fasi ha reso visibile che la prima non aveva niente da mutare. **Rosso** tre volte su reversioni distinte: il prestito (`read()` → `write()`, con `try_read` e non `read`, e il presidio sta *dentro* `runner.rs` perché la proprietà è di una funzione privata — scritto sulla coppia del kernel avrebbe provato il proprio codice), il piano invecchiato (confronto tolto: l'ultimo testo arrivato all'indice è quello di prima del salvataggio), e il verso opposto (`!=` → `==`: la fetta intera buttata, il vault che si apre con la ricerca vuota, e il primo presidio resta verde). Nessun banco cronometrico: ciò che è cambiato non è quante letture si fanno, è **chi aspetta**. | 2026-08-06 |
| [0125](0125-un-albero-riusato-riceve-una-porta-non-un-handler.md) | Un albero riusato riceve una porta, non un handler | La prima delle due zone cieche che la [0118](0118-una-chiusura-non-cattura-cio-che-il-riconciliatore-aggiorna.md) aveva dichiarato e non toccato: l'`ActionHandler` di un sottoalbero riusato restava quello del **primo** montaggio, «per una coincidenza che nessuno ha dichiarato». La coincidenza, nominata qui: `views.ts` fabbrica una chiusura **nuova a ogni ridisegno** — oggetti diversi — e sono tutti **intercambiabili**, perché catturano solo `id` e `montata`, cioè una stringa e un record che vive nella mappa e i cui campi si leggono alla chiamata; `params` non è mai stato scritto da nessuno. La premessa della 0118 era quindi vera per la ragione sbagliata («è vecchia ma identica» suggerisce *la stessa*: è un'altra che fa la stessa cosa), e i suoi cinque casi non potevano vederlo perché riusano tutti **lo stesso** `onAction` fra i due montaggi. Contate **due** catture contro l'una dichiarata, e la peggiore era sotto la frase: `patchTree` non *usava* l'handler vecchio, lo passava a `riconcilia`, che lo **riscriveva nei `legami`** del sottoalbero — un patch disfaceva la 0118 su un pezzo d'albero. La seconda sta fuori dal file: il renderer custom riceve l'handler come argomento e se lo tiene per la vita del widget, e un `custom` con payload immutato **non si riconcilia mai**, quindi è la cattura più longeva della shell. Riparazione: un contenitore ha una **porta**, una funzione sola creata al primo montaggio e mai sostituita, che inoltra a `Montaggio.corrente`; dentro il renderer non circola altro, e chi la cattura cattura un rinvio, non una destinazione — il difetto del renderer custom si è chiuso **senza toccare una riga** di `custom.ts` o `graph.ts`, che è la prova del secondo chiamante. `Porta = ActionHandler & { [PORTA]: true }` con `PORTA` un `unique symbol` mai costruito: un handler nudo passato a `riconcilia`, `disegna`, `collega` o `ascolta` **non compila** — forma della [0114](0114-una-finestra-non-si-omette.md), e simbolo e non stringa per la ragione della [0117](0117-un-termine-non-si-sovrappone-a-se-stesso.md). `renderUiNode` smette di essere esportata (era la porta di servizio da cui rientrava l'handler nudo; nessuno la usava, misurato), e `handlers`/`handlerDi` **spariscono** invece di essere riparati. **Rosso** due volte su reversione del file, un caso per difetto — dopo il patch e attraverso la porta del renderer, il click arriva al primo handler — e i due casi montano lo stesso contenitore con **due handler diversi**, la cosa che nessun caso della 0118 fa; più il marchio provato da solo, con `tsc` che rifiuta un `ActionHandler` dove vuole una `Porta`. Zone cieche che restano: un `addEventListener` scritto a mano dentro `disegna`, `intestazioniSchede` che ricostruisce le linguette a ogni giro (e quindi perde il focus), e il lettore del valore di `select`/`radio` che cattura `node` una volta sola. Zero firme di contratto, WIT intatto, nessuna dipendenza nuova. | 2026-08-06 |
| [0126](0126-un-bus-che-tace-non-lo-scopre-nessuno.md) | Un bus che tace non lo scopre nessuno | L'altra metà della zona cieca che la [0120](0120-un-lucchetto-avvelenato-si-dice-una-volta.md) aveva **dichiarato** e non toccato: «il conto vede `fub-host` e `fub-app`, non gli altri crate». `bus.rs` scriveva `self.subscribers.lock().unwrap()`, e la domanda vera non era «tolgo gli `unwrap`» ma **se la politica della 0120 vale qui**. Non vale, e la 0120 stessa dice perché: *la politica del veleno segue cosa il lucchetto protegge*. `EventBus::emit` non rende niente a nessuno — non c'è un `#[tauri::command]` a valle, non c'è un canale d'errore — quindi «irrecuperabile, poi rispondi di no» diventerebbe **«taci per sempre»**: l'app smette di consegnare eventi, la shell resta ferma sull'ultimo stato, e alla domanda *chi lo scopre?* la risposta è **nessuno**. Col vecchio `unwrap` era peggio ancora, ed è il difetto che ha cambiato la diagnosi: il panico si propagava a chi stava emettendo, cioè a chi teneva il prestito **esclusivo del workspace**, e avvelenava quello — il bus non moriva da solo, **portava con sé ciò che sta sopra**. Decisione: il bus si **riprende** (`into_inner` + `clear_poison`), lo dice una volta con `tracing::error!`, e mette **tutti** gli abbonati in debito di un notice — così ognuno riceve un [`Event::Overflow`] della [0034](0034-il-freno-e-il-raggruppamento.md), che la shell già sa leggere come «riconcilia da zero». `into_inner` qui non è mentire perché ciò che il lucchetto protegge è un **elenco di destinatari indipendenti** e `Vec::retain` lo lascia valido anche se il predicato pania in mezzo; «tutti» perché chi si riprende non sa a che punto dell'elenco il giro si è rotto, e riconciliare per niente costa meno di uno schermo indietro per sempre. Porta: `mod roster`, `Mutex` privato al modulo, `Roster::with` unica via. **`Custodia` non si è spostata**, e la ragione è la prova che comanda: portarla sotto non avrebbe fatto ereditare niente, perché porta con sé la politica che qui non vale — fra i due crate si eredita la **regola**, non il tipo, e la seduta 16 resta come sta. Contati **due** `.lock()` nel kernel contro i due dichiarati, ma dieci file con lucchetti propri; e **una** coppia incrementa/decrementa con una metà fuori dalla porta — quella del commit gemello. **Rosso** tre volte su reversioni distinte: la politica rimessa a `.unwrap()` (`FAILED` sul `PoisonError`, non una morte del thread, perché il panico è nel thread del banco), e i due conti sul sorgente con un `Arc<Mutex<u32>>` nudo aggiunto a `EventBus`. Zone cieche dichiarate: i banchi (tagliati apposta), un lucchetto in un altro file del kernel, e `JobBell` in `dispatcher.rs` che resta a `.expect("campanello avvelenato")` — ragione `Condizione` della 0120, ma senza una porta che la tenga. Zero firme di contratto, WIT intatto, nessuna dipendenza nuova. | 2026-08-06 |
| [0127](0127-la-mutazione-e-il-prodotto-della-scrittura.md) | La mutazione è il prodotto della scrittura, non il suo presupposto | Il difetto misurato dichiarava una premessa e non l'aveva verificata: *«qui l'inversione non è possibile — costa un ripristino del campo»*. **Era possibile**, e sembrava vera per una ragione sola: `write_meta` e `write_index` leggevano da `self`, quindi per scrivere bisognava aver già mutato. Ma sotto c'era un fatto che nessuno aveva contato — `write_index` **già faceva `self.docs.clone()`**: serializzare la mappa che i documenti *avranno* costa esattamente quanto quella che hanno. La forma giusta era scritta due volte nello stesso file e mai letta come regola: `prune` non cancellava, **restituiva**, e `spazza` toglieva solo a indice persistito. Il tombstone era rimasto fuori perché il campo è uno e sembrava troppo piccolo. Decisione: `Inner::docs` cambia in **un posto solo**, `Inner::applica(piano, meta_di, host)` — si costruisce la mappa che i documenti avranno, si scrive, e **solo un disco d'accordo** la installa; chi domani aggiunge un campo a `DocVersions` eredita l'ordine giusto senza sapere che esiste. Il `meta.json` va **prima** dell'indice perché l'autorità è l'indice, e l'ordine inverso lascerebbe l'autorità avanti e la memoria indietro. Con lui `prune` → `pota` (pota il **piano**) e `ensure_dir` → `dir_per` (trova il nome, non lo installa). Contate **sei** mutazioni-prima-della-scrittura contro l'una dichiarata, cinque chiuse insieme perché `applica` non lascia scelta: il dedup, la `push` del ramo normale, il `rename` (che perde una **storia intera**, non un campo), il `tombstone` (che la riconciliazione post-`Overflow` non ritenta mai, perché `is_deleted` legge la memoria), la cartella prenotata; la sesta — `EntryStore::store` nel kernel — resta, ed è dichiarata: cache derivata, errore loggato, si ripara alla riapertura. Il caso dichiarato era il **più irreparabile e non il più grave**: `.take()` consuma, quindi la ritentata trova `deleted_at` già a `None` e non riprova nemmeno. Premesse cadute: l'inversione impossibile, il «costa un ripristino» (costa **zero**, non c'è niente da ripristinare se non ci si è mossi), e il commento sopra `prune` che argomentava bene la potatura e **taceva sulle due righe sopra di sé**. All'utente lo dice già la [0052](0052-cio-che-va-storto-e-un-evento.md): mancava che il messaggio dicesse la verità, non il messaggio. **Rosso** verificato uno per uno rimettendo il vecchio ordine dentro `applica`, e il verso opposto con un `applica` che non scrive mai — dove il banco del ramo felice è **passato a vuoto**, perché uno store riaperto su un disco vuoto risponde «non cancellata» anche di una nota che non conosce: riparato chiedendo prima che il record esista. Zone dichiarate: la finestra fra meta scritto e indice fallito, e il silenzio di `spazza`. Zero firme di contratto, WIT intatto, nessuna dipendenza nuova. | 2026-08-06 |
| [0128](0128-una-versione-di-schema-e-un-tipo.md) | Una versione di schema è un tipo, e un nome non lo fa rispettare nessuno | Chiude il buco che la [0106](0106-un-formato-si-presenta.md) aveva dichiarato leggendo il proprio presidio: `schemi-su-disco` trovava le versioni di schema cercando la parola `VERSION` nel **nome**, e un `const E_SCHEMA_REV` gli passava accanto. La domanda che restava era se il nome fosse una **regola** o una consuetudine, e la risposta stava già scritta nella 0106 due paragrafi sopra la riga che la poneva: `DIAGNOSTICS_VERSION` era sfuggita per un anno, e *chi l'aveva chiamata così non aveva sbagliato niente*. **Una regola che si viola in buona fede non è una regola**: è una consuetudine la cui sanzione cade sul presidio, che smette di vedere un formato e resta verde. Quindi il nome **non** diventa una regola — diventa una regola il **tipo**: `SchemaVersion` in [`crates/fub-abi/src/schema.rs`](../../crates/fub-abi/src/schema.rs), un `u32` con un nome e `#[serde(transparent)]` (su disco non cambia un byte, e non poteva: quei file sono sui dischi delle persone). Le undici costanti lo dichiarano e **gli undici campi dei record lo pretendono**, quindi `v: 1` non compila più; a farlo rispettare è il compilatore, e il conto passa dal nome al tipo. Ne escono due proprietà che il nome non dava: una rinomina non nasconde più niente — misurato, `DIAGNOSTICS_VERSION` → `E_SCHEMA_REV` lascia il conto a **undici** e la suite verde, ed è la prova che conta perché è l'unica in cui il presidio deve **non** diventare rosso — e un intero omonimo che versione non è non entra più. **Un trait con una costante associata è stato scartato ed è la trappola 4**: lo implementerebbe chi già si dichiara, cioè non il caso da prendere; un tipo aggancia perché sta nella firma del campo. Che abbia agganciato lo dice il conto e non l'occhio: rimettendo `u32` su `kernel/vault.rs` scende a dieci e diverge da `schemi-in-tabella`. **Undici era il numero giusto** — con tre un tipo è cerimonia, con quaranta la porta resta mezza attraversata, che è peggio di non averla. Premessa caduta scrivendo: il tipo doveva stare in `fub-kernel` (un numero di schema è dei file di Fub, non del contratto dei plugin) e **non compila**, perché `fub-features` non dipende da `fub-kernel` per invariante presidiata, e due degli undici formati stanno lì; la casa è `fub-abi`, dove sta già `rules/path_policy.rs`, cioè dove stanno le regole su ciò che finisce su disco. WIT intatto, nessuna dipendenza nuova. Due zone cieche dichiarate: quella della 0106 non si muove (un formato senza costante **e** senza riga non lo prende nessuno, e a prenderlo servirebbe un tipo che ogni scrittura durevole attraversi — impossibile, perché dalla stessa porta passano i file di Fub e i file dell'utente), e una nuova, `v: SchemaVersion::new(1)` scritta al volo senza una costante che la nomini: il tipo rende impossibile scrivere `v: 1`, non rende impossibile non dare un nome all'1. | 2026-08-06 |
| [0129](0129-una-feature-spenta-per-difetto-non-e-provata.md) | Una feature spenta per difetto non è provata da nessuno | Nasce da un difetto di cui **nessuno dei tre fatti elencati** era il difetto. *«La versione più larga del workspace»*: `ureq = "3"` è largo ma non unico (`syn = "2"` altrettanto), e la domanda del punto di verità ha già il suo giudice — `check-cargo-versioni.mjs` dice verde e ha ragione, `ureq` sta in un crate solo e la radice non ne dichiara un'altra. *«Unica dipendenza di rete»*: vero, e non è un difetto — la `Cargo.toml` lo argomenta per venticinque righe e misura venti pacchetti di lockfile. *«Una feature che la CI di default non compila»*: **falsa in una riga**, `http-client` **è** nel `default`, e il recinto della [0097](0097-un-recinto-che-vale-anche-quando-nessuno-guarda.md) è il codice meglio presidiato dei tre — `Guard::fetch` sta in un crate **senza nemmeno una cargo feature**, con dieci chiamate di prova (host fuori allowlist, salto di dominio, le credenziali `https://api.acme.com@evil.example/`, il suffisso `evil-acme.com`, lo schema in chiaro, l'anello locale e il suo omografo). Sembrava vera perché `optional = true` si legge come «spento», ma `optional` dice solo che esiste una feature che lo accende — non se è accesa. Il difetto vero stava **fuori dalla riga**, per la nona volta di fila, ed è la domanda che restava: chi garantisce che resti falsa? **Nessuno**, e il costo è una parola. Tolta `"http-client"` dal `default`, `cargo test --workspace` passa da **1328 a 1325** test — spariscono le tre prove di `net.rs`, cioè quelle che tengono in piedi le due decisioni della 0097 (non seguire i redirect, fidarsi del verificatore della piattaforma) — e sparisce `ws.set_network` da `mount.rs`, quindi ogni `fetch` risponde `unserved`, che è la risposta **legittima** di chi non monta la rete. Non diventano rossi né `ignored`: **escono dal conto**, e le righe `test result: ok` restano **119**, `clippy` **0**, tutti gli altri presidi **verdi**. Decisione: **ogni cargo feature dev'essere raggiungibile dal `default` del suo crate**, o dichiarata in `FUORI_DAL_DEFAULT` **insieme al passo di CI che la compila** — un'esenzione senza quel campo sarebbe una firma su una promessa vuota. Presidio in [`check-cargo-feature-default.mjs`](../../.github/scripts/check-cargo-feature-default.mjs), job `invariants`. Non dice che una feature non possa spegnersi — la §16.3 lo verifica già dallo stesso job — dice che non può spegnersi **per difetto**, che è la sola configurazione che nessuno confronta con niente: le due domande sono opposte e il repo aveva solo la prima. Uno script e non un test Rust perché la domanda è del workspace, e un test che vive dentro un crate con feature è soggetto alla stessa classe. Costo **zero**: nessun crate, nessuna feature accesa, nessun secondo di build — la feature *era già accesa*, qui si impedisce di spegnerla senza accorgersene. **Rosso** tre volte: il difetto vero, l'elenco esaustivo provato **togliendo** un elemento (`search` fuori dal `default` di `fub-features`), e il presidio puntato dove non c'è niente, che non dice verde ma «non sto guardando niente». `ureq = "3"` resta com'è, deliberatamente. Zone dichiarate: le feature delle *dipendenze* non si guardano, `--all-features` oggi coincide col `default`, e il conto dei test non è presidiato — un test cancellato a mano esce dal conto come uno spento da una feature. Zero firme di contratto, WIT intatto, nessuna dipendenza nuova. | 2026-08-06 |
| [0130](0130-ogni-tipo-del-contratto-si-vede-dalla-radice.md) | Ogni tipo del contratto si vede dalla radice, e a dirlo non è chi si ricorda | Chiude la [§24.1](../roadmap/24-tre-firme-che-il-freeze-rende-definitive.md), e **due premesse su tre della voce erano false**. I tipi invisibili dalla radice di `fub-abi` non erano sette in `traits.rs`: erano **sessantuno** su duecentoquarantasei — ventuno in `traits.rs`, **quattordici in `model.rs`** (`PropertyValue`, `ListItem`, `TableCell`, `Anchor`, `HeadingSlugs`…), dodici in `arena`, sei in `rules/`, più `Undo`, `DocChanges`, `DocumentFormat`, `TextTolerance`, `Organization`, `SchemaVersion`. Sembravano sette perché l'audit aveva guardato **un file**, e `traits.rs` è il file che si guarda quando si pensa «il contratto» — ma `model.rs` è ciò che un plugin di formato tocca per primo. Tutti e sessantuno erano della **seconda** specie di invisibilità (raggiungibili solo per path lungo), non delle altre due. E **«è una firma, quindi scade» è falso**: un `pub use` è additivo e si aggiunge anche dopo il freeze — a scadere è il *frattempo*, cioè i **centocinque** `use` di altri crate che nel frattempo passano da `fub_abi::traits::`, un modulo di implementazione che la [0053](0053-il-contratto-ha-una-sorgente.md) sta spingendo a spezzarsi. La §24.1 era una P0 per la ragione sbagliata, e va detto. Decisione: quarantatré tipi entrano nel `pub use`, e la **regola** va a un presidio — [`superficie_della_radice.rs`](../../crates/fub-abi/tests/superficie_della_radice.rs) legge `src/` per intero con `syn` e diventa rosso, per nome, su ogni tipo `pub` che non sia alla radice. Un `pub use traits::*` è rifiutato: riesporta tutto e rinuncia a dire cosa è superficie (e per `arena` non compilerebbe, cinque collisioni con `model` e `ui`). **La decisione vera erano i diciotto tipi che alla radice non ci possono stare**, e le due forme erano eccezione **per tipo** (diciotto righe d'allowlist, come `dieta_ipc`) o **per modulo** (due righe con la ragione). Ha vinto la seconda per la prova del secondo chiamante: con l'allowlist di nomi il tredicesimo tipo di `arena` nascerebbe rosso e chi lo aggiunge farebbe tacere il test scrivendocelo — lo stesso gesto per cui il presidio esiste; la ragione («`arena` è la forma al confine, i nomi collidono per costruzione»; «`rules` si chiama col soggetto davanti») è una proprietà del **modulo**. In cambio due pretese: la ragione dev'essere argomentata, e un modulo qualificato non può avere **neanche un** tipo alla radice. **Un presidio è stato tolto, ed è la parte istruttiva**: il verso di ritorno («la radice riesporta un nome che non esiste più»), imitato da `dieta_ipc`, alla prova del rosso non ha prodotto output — il crate non compilava. Un `pub use` non è una stringa che nomina un simbolo, è un **riferimento**: quel verso ce l'ha già il compilatore, e il test sarebbe stato verde per sempre. La disciplina dei due versi vale per gli elenchi di *stringhe*, e qui l'unico è `MODULI_QUALIFICATI`. **Rosso cinque volte**, fra cui l'elenco provato **togliendo** un elemento e il test del test (`il_camminatore_scende`). Difetto **fuori dalla voce**, dichiarato e non riparato: `src/rules/` è invisibile ai presidi che camminano il sorgente — `common::fieldless_enums()` dice «tutti e soli gli enum di `src/*.rs`» e quella `read_dir` non scende; farla scendere lo farebbe **panicare**, perché `Naming`/`Newline`/`Owner` non derivano `Serialize` e quel lettore confonde «enum `pub` senza payload» con «enum che attraversa l'IPC». È una decisione sul confine JSON, non un refuso. Zone cieche: solo i tipi (non funzioni né costanti), solo `fub-abi`, e un tipo nuovo dentro `arena` o `rules` resta invisibile. Zero firme cambiate, WIT intatto, nessuna dipendenza nuova. | 2026-08-06 |
| [0131](0131-tre-stati-e-la-firma-che-ne-diceva-due.md) | Tre stati, e la firma che ne diceva due | Chiude la [§24.2](../roadmap/24-tre-firme-che-il-freeze-rende-definitive.md), e **due premesse su tre erano false** — seconda voce di fila della stessa seduta a essere P0 per la ragione sbagliata. **«È una firma, quindi scade»: falso.** Al confine WIT non esiste nessun `enabled`: c'è `type option-map = list<option-entry>`, e i tre stati un `option-entry` li porta già tutti — assente, `value: false`, un valore qualunque. `enabled` è un metodo Rust di comodo, e affiancargli `status` è additivo oggi come il giorno dopo il freeze. **«I due `false` sono *il provider non la conosce* e *è spenta in questo `ParseContext`*»: falso**, e sembrava vero perché quelle due frasi sono davvero due stati — solo che stanno in **due mappe diverse**: *cosa so fare* è `FormatCapabilities.syntax`, *cosa devo accendere* è `ParseContext.options`, e chi vuole distinguerle guarda `DocumentFormat`, che ha entrambi i campi apposta. Il terzo stato dentro **una** mappa esiste lo stesso ed è un altro: *nessuno l'ha nominata* contro *qualcuno l'ha nominata per spegnerla*, cioè «non si è deciso» contro «si è deciso di no» — e ha un cliente vero, `overlay`, che sovrappone vault→cartella→nota per chiave proprio perché quella differenza viaggi. **Il verso opposto ha deciso la forma**: la voce chiedeva di guardare prima la strada in cui `enabled` **sparisce**, e i chiamanti dicono che è la sbagliata — parse (wikilink, tag, embed), render, `SyntaxRules::apply`, `Granted::new`, `permission_specs`: **cinque su cinque farebbero la stessa identica cosa** sapendo quale dei due `false` è, e giustamente (un cancello che si aprisse diversamente a seconda di *come* gli è stato detto no sarebbe un cancello peggiore). Toglierlo avrebbe pagato un `match` a tre rami di cui due identici in sei posti dove il booleano è **la risposta giusta**. Decisione: `status(&self, key) -> OptionStatus<'_>` con `Unset`/`Off`/`On(&Value)`, **`enabled` resta ed è `self.status(key).is_on()`** — non una seconda funzione: finchè aveva il proprio `match` la tabella dei casi era in due copie e allargarla in una sola si poteva fare compilando. `On` porta il **parametro**, perché un `Option<bool>` avrebbe distinto i tre stati buttando via allowlist e livelli, cioè avrebbe rifatto il difetto un gradino più su (è la mossa che la [0094](0094-un-tetto-che-si-fa-sentire.md) evitò su `random-bytes`). Il tipo non attraversa il confine e non ha `Serialize`: al confine c'è la mappa. Il chiamante più istruttivo è `Granted::new`, che per l'allowlist di `fub:network` era **già sceso sotto** `enabled` leggendo il `Value` grezzo — quando la distinzione è servita, chi ne aveva bisogno se l'era presa a mano: mancava **il nome**, non il dato. **Il difetto fuori dalla voce, ventiduesimo giro di fila**: `format_of` e `syntax_forms` leggono la **stessa** `OptionMap` e la leggevano in due modi — `enabled` la prima, `iter()` la seconda, che porta anche le spente. Su un provider che scrive `.with(nome, false)` (l'unico modo di dire «la conosco e qui non la faccio») la sintassi **non** compariva fra le capacità e **sì** fra le forme: la superficie di scrittura decora ciò che il parse non legge. Nessun provider del repo lo fa — `FormatCapabilities::of` costruisce solo con `.on()` — ed è esattamente perché poteva restare lì per sempre. Riparato al posto che tutti attraversano: `OptionMap::active()`, e il presidio confronta le due funzioni **l'una con l'altra** invece che contro un terzo elenco scritto a mano. **Rosso tre volte togliendo**, non aggiungendo: tolto `Value::Null` dalla tabella (`On(Null)` invece di `Off`, e `fub:math` che ricompare fra le accese), e rimesso `.iter()` al posto di `.active()` in `syntax_forms`. L'attore è il **test** e non il compilatore, misurato: se `enabled` fosse sparito il compilatore avrebbe preso sei chiamanti che non avevano niente da cambiare — sei falsi positivi e zero difetti — mentre «due funzioni rispondono diverso sulla stessa mappa» è comportamento. Il presidio di ieri ha morso da solo: `OptionStatus` senza `pub use` rende rosso `superficie_della_radice`, e non è stato aggirato. Zone cieche: `in_ns`/`keys`/`iter` continuano a portare le spente (sono le firme con cui si guarda la mappa *com'è scritta*), il pannello dei permessi non ha oggi un cliente per i tre stati, e la copia TypeScript della regola (`permessi.ts`) resta perché la mappa attraversa come JSON — ha il suo presidio. Zero firme WIT cambiate, nessuna fixture rigenerata (verificato, non sperato). | 2026-08-06 |
| [0132](0132-un-rifiuto-non-e-una-frase.md) | Un rifiuto non è una frase, sono i due dati da cui la frase si compone | Chiude la [§24.3](../roadmap/24-tre-firme-che-il-freeze-rende-definitive.md) — **l'ultima voce della roadmap infrastrutturale**, e la sola delle tre di questa seduta che scadeva davvero col freeze. **Tre premesse su tre erano false.** «È l'unico errore che non è testo»: il conto giusto è **quattro** — `Parse`, `Render` e `Serialize` portano una `String` nuda esattamente come `Unsupported`, e per quelle tre è giusto (*«`Display` è per chi legge un log, `Text` è per chi legge uno schermo»*: sono una diagnosi su un documento e finiscono in `Internal`, cioè in un log). Andando oltre, nemmeno il conto sui `PluginError` regge: la maggioranza di quelli che il kernel costruisce è un `Text::Literal` di prosa italiana cablata, ed è **anch'essa una decisione dichiarata**, scritta in `Workspace::localized`. `Unsupported` non era l'unico che non è testo: era l'unico che **non doveva portare prosa affatto**, perché è l'unica delle quattro che non diagnostica un documento — è il disaccordo fra due dati **già dichiarati nel contratto** (`FormatDescriptor::source` contro ciò che è arrivato) e l'unica che il kernel porta a `Unserved`, cioè davanti a chi ha appena provato ad aprire un file. «È la variante che un utente vede più spesso di tutte»: **falso e al contrario** — oggi non la vede **mai** passando dal kernel, perché `DocumentStore::source_from_disk` ([0087](0087-il-testo-che-sta-dentro-gli-allegati.md)) è il punto unico che consulta `FormatDescriptor::source` e legge **nella forma che il provider ha chiesto**; un file non-UTF-8 fallisce prima, in `Vault::read`, come `Io`. Il caso che la voce descriveva esiste, ma è `NoProvider` o `Io`: la conclusione era giusta e la catena no. Questo **non toglie l'urgenza, la sposta**: ciò che si congela a M4 è la *promessa* verso host e provider che non esistono ancora, non il percorso di oggi. **Il verso opposto ha deciso la forma**, per la seconda volta di fila dopo la [0131](0131-tre-stati-e-la-firma-che-ne-diceva-due.md): la seconda forma della voce — e la proposta letterale dell'audit, *«un campo `Text` facoltativo»* — è **impraticabile**, perché l'unico consumatore è `impl From<KernelError> for PluginError`, una `From` senza `&self` (niente registro, niente locale, niente cataloghi) su una via — aprire un documento — che non passa da `Workspace::localize`. Una chiave lì arriverebbe allo schermo **nuda**, cioè peggio della stringa inglese di prima: la forma «più corretta» avrebbe peggiorato ciò che l'utente legge. Decisione: `Unsupported { format: String, got: SourceKind }`, **variante di struct senza `..` da nessuna parte** — la forma di `Inline`/`Block` della [0122](0122-una-sorgente-non-degrada-si-rifiuta.md): `E0063` a chi la costruisce dimenticandosi un campo, `E0027` a chi la legge. Nessun tipo nuovo (`SourceKind` c'era), un metodo nuovo (`DocumentSource::kind()`, perché riscrivere a mano la specie ricevuta vuol dire poterla scrivere sbagliata), e la frase la compone **il kernel** spendendo tutti e due i campi, accanto a un `match` senza `_` che non compila se una specie di sorgente si aggiunge al contratto senza una parola italiana. **Scade davvero**: `format-error` è il tipo d'errore delle funzioni che un plugin di formato **esporta**, ritipare un caso pubblicato non è un'aggiunta in nessuna lettura, e come nella [0102](0102-i-byte-non-stanno-nel-record.md) — a differenza della [0049](0049-una-posizione-dentro-un-documento.md) e della [0101](0101-una-voce-non-e-un-passo.md) — **`0.1.0.wit` si tocca davvero**. La via additiva (un caso in coda, lasciando `unsupported(string)` dov'è) è scartata a verbale: lascerebbe per sempre **due modi di rifiutare una sorgente, di cui uno intraducibile**, e chi scrive un provider sceglierebbe il più corto. **Il difetto fuori dalla voce, ventitreesimo giro di fila, stava dentro il banco che avrebbe dovuto smentirmi**: `un_provider_testuale_rifiuta_i_byte` esiste dalla [0054](0054-il-banco-del-lato-provider.md), cita la regola per intero nel doc-comment (*«risponde `Unsupported` invece di indovinare»*) e ne provava **metà** — `assert!(esito.is_err())`, quale errore non lo guardava. Un provider che rifiutasse con `Parse` sarebbe passato verde per sempre, e la differenza arriva intera allo schermo: `Parse` va a `Internal` (*«segnala un bug»*), `Unsupported` va a `Unserved` (*«installa un plugin»*) — sull'allegato di un utente il primo dice «il tuo file è rotto» di un file che sta benissimo. È la famiglia della 0054 — *un banco che passa a vuoto* — trovata **dentro** un banco della 0054: citare la regola nel commento non è provarla. Attore **doppio**, con la divisione della [0105](0105-una-porta-si-nomina-e-un-presupposto-si-compila.md): il compilatore prende la variante che non vuol dire niente (verificato rompendo: `E0063`, `E0027`, e `match` senza `_`), il test prende ciò che il compilatore non può — il tipo obbliga a *portare* i due dati, non a **spenderli**, e un `format!` che ne dimentichi uno compila benissimo. **Rosso cinque volte**, fra cui `wit_additivity` rimettendo la linea di base al suo stato precedente (*«in posizione 3 c'era `("unsupported", Some("string"))`»*) — l'unico dei tre presidi della seduta 24 che si sia acceso. Nessuna fixture mossa, e **verificato perché**: `FormatError` non è nel mirror TS (non attraversa mai l'IPC, diventa un `PluginError` prima) e non era in `fieldless_enums()` (tutte e quattro le varianti hanno sempre avuto un payload), quindi non poteva né entrarci né uscirne. | 2026-08-06 |
| [0133](0133-chi-ascolta-nomina-fino-a-quando.md) | Chi ascolta nomina fino a quando | Chiude **tre** difetti misurati che erano la stessa frase — 0016 (`onLingua` non torna una disiscrizione), 0024 (il `click` di `showContextMenu` resta appeso se il menu si chiude con Escape), 0083 (`pickIcon` toglie il nodo senza chiamare `chiudi()`) — e la seconda zona cieca lasciata scritta dalla [0125](0125-un-albero-riusato-riceve-una-porta-non-un-handler.md): *«nessun attore vede un `addEventListener` scritto a mano»*. **Il conto era sbagliato e ha deciso la forma**: i siti che registrano su un bersaglio che vive quanto la pagina sono **sei**, non tre, più **quattro** registri di modulo (`onLingua`, `onEvent`, `onAnyEvent`, `store.on`) — dieci posti per una tabella che ne nominava tre. Con tre, riparare tre volte era difendibile; con dieci, no. Decisione: `frontend/src/ui/vita.ts` — una **`Vita`** che possiede un insieme di ascolti, e `ascolta` è un **metodo**, quindi non lo si chiama senza avere in mano ciò che sa anche smettere. È la mossa della `Porta` un piano più in là: là nel riconciliatore girava un rinvio invece di un handler, qui a chi ascolta si passa una vita invece di un bersaglio. **`Vita` NON è marchiata con un `unique symbol`**, ed è la differenza misurata con la 0125: là esisteva un sostituto nudo credibile da rendere inesprimibile (un `ActionHandler`), qui il sostituto è `document.addEventListener`, che non è nostro e non si marchia — *un marchio che non toglie una strada è decorazione*. **Una vita chiusa è inerte**, e non per comodità: è la riparazione del 0024, dove il `setTimeout(…, 0)` registrava *dopo* che Escape aveva già chiuso il menu e il `once: true` non serviva a niente (l'ascoltatore restava fino al primo click qualunque, e chiudeva il menu **successivo**). **0024 e 0083 erano un difetto solo**: tre cose da disfare — nodo, ascoltatore, trappola del fuoco — scritte in tre posti, `showContextMenu` ne dimenticava una e `pickIcon` due; non per distrazione, ma perché l'elenco non stava in nessun posto. **0016 è vero e non morde**: i quattro chiamanti vivono quanto la finestra, e il difetto è del *secondo chiamante* che sarà un pannello — la riparazione è la stessa, cambia solo cosa c'è scritto qui. Scartata a verbale la forma «ogni `onQualcosa` torna una disiscrizione e basta»: TypeScript non ha un *must-use*, quindi la promessa resta da ricordarsi (spostata dal `removeEventListener` al `const smonta =`), e non copre il 0024, dove chi registra e chi dovrebbe smettere sono la stessa funzione un istante dopo. Scartato anche il `Vita` obbligatorio sui quattro registri di modulo: ventotto chiamanti tutti app-lifetime che avrebbero scritto tutti la stessa vita globale, cioè la regola **nei chiamanti**, e contro una decisione già datata in `store.ts` che nomina il §9.4. Attore **doppio**: il compilatore per «non si registra senza una `Vita`», un conto (`check-ascoltatori.mjs`, zone cieche dichiarate: alias, `EventTarget` generici, elementi, banchi) per «nessuno scavalca la porta», provato **rosso sei volte, una per sito**; più quindici casi in tre banchi nuovi, ognuno rosso rompendo ciò che difende. **Due presidi non sono diventati rossi, ed è l'informazione migliore del giro**: *«le opzioni tornano identiche allo smontaggio»* passava anche togliendo `opzioni` dal `removeEventListener` — `happy-dom` stacca un ascoltatore in cattura anche senza la fase, mentre un browser vero no — ed è stato **cancellato** in favore della riga che dice che quel verso ce l'ha la forma di `ascolta` (la mossa della 0125); *«chiudere due volte disfa una volta sola»* non diventava rosso togliendo **nessuna** delle due difese, perché ce n'erano due — una proprietà vera due volte non è presidiata da niente, e la guardia ridondante è stata tolta dal **codice**, non la pretesa dal banco. | 2026-08-06 |
| [0134](0134-una-risposta-porta-la-data-di-chi-l-ha-chiesta.md) | Una risposta porta la data di chi l'ha chiesta | Chiude **tre** difetti misurati che erano la stessa frase — 0031 (`updatePreview` innesta senza token), 0034 (`refreshFromKernel` non ha contatore di generazione), 0087 (il ripiego da `patch` a `renderDeclaredView` non ha token di sequenza). **Il conto ha deciso la forma, e ha deciso anche che le forme sono due**: i siti di `frontend/src/` che aspettano e poi scrivono **senza nessuna guardia** sono **trentanove**, non tre, e la metà del lavoro era già scritta a mano **quattro** volte con tre nomi diversi (`searchSeq`, due `seq`, `generazione`) — la quarta, `doc-search.ts`, la tabella non la nominava. Ma la parola «corse» ne nascondeva **due domande opposte**: trentadue siti vogliono **buttare** (ciò che portavano è un *disegno*, che il giro nuovo rifà) e sette vogliono **aspettare** (ciò che portano è un *effetto*, e buttarlo non è ordinare ma perdere); *riprovare* non lo vuole nessuno. Una forma unica per tutti sarebbe stata elegante e avrebbe **perso dei salvataggi**. Decisione: `frontend/src/ui/corsa.ts` con **due tipi e non due modalità di uno** — `Corsa.ultimo(corpo)`, dove il corpo riceve **`atteso`**, unico modo di ottenere il risultato di un'attesa, che se il giro è scaduto **non torna** e interrompe il corpo prima che scriva; e `Coda.accoda(lavoro)`, che torna l'attesa del **proprio** lavoro e non della coda. Scegliere fra le due è un giudizio, e un giudizio non si esprime passando `true`. È la mossa della [0133](0133-chi-ascolta-nomina-fino-a-quando.md) applicata al tempo invece che alla vita, e come là **niente marchio**: `atteso` si riceve, non si fabbrica. Il padrone è un **oggetto** perché il conto lo imponeva — due delle quattro implementazioni stavano in una chiusura e due in un modulo — e nei tre siti riparati è di tre specie: una `const` di modulo, un campo della `Montata`, una `WeakMap` sull'elemento. **Le quattro implementazioni erano d'accordo sulla forma e no su due cose**, tutt'e due finite nel tipo: il **ramo d'errore** (tre su quattro ricontrollavano il numero anche nel `catch`, la quarta se l'era dimenticato) e l'**annullamento** senza cominciare un giro (una sola su quattro ce l'aveva — ed è, letteralmente, la riparazione del 0031: `clearPreview` fa scadere la corsa della superficie che svuota). **Il difetto stava fuori dal sito nominato**: il 0087 nomina il ripiego, ma la finestra è in `renderDeclaredView` e basta — riparando il posto nominato restavano scoperti il ramo `replace` e ogni ridisegno da evento, che sono i più frequenti. E il 0034 aveva un **guardiano finto**: `ultimaFirma` risponde a «è cambiato qualcosa?», non a «sono ancora io?», e due giri partiti insieme hanno quasi sempre firme *diverse*, quindi il vecchio passava il controllo e vinceva arrivando dopo — **un dedup non è mai un ordinamento**. Attore **triplo**: il compilatore per «`atteso` si riceve», un conto (`check-corse.mjs`) per «nessuno scavalca stando dentro» — un `await` nudo, e un **`} catch` dentro il corpo**, che è il modo involontario e da cui nasce l'idioma *l'errore diventa un valore prima del cancello* (`await atteso(p.catch(…))`) —, e dieci prove in `ui/corsa.test.ts` **senza nessun tempo**: ogni attesa è una promessa che il banco risolve quando decide lui. Provate rosse una per una, sette rotture. **Una zona cieca dichiarata era falsa**: il commento elencava «l'alias» fra i modi di aggirare il conto, e costruendolo il conto è diventato **rosso** — il criterio è «il nome compare nell'espressione attesa», quindi un alias è una violazione e non un buco. Il modo che funziona davvero, trovato costruendolo, è **`.then(…)` al posto di `await`**. La zona cieca che conta resta la prima e sta scritta per intero: il conto guarda *dentro* i giri e non ha niente da dire su **chi non ne apre nessuno**, che sono gli altri trentasei. | 2026-08-06 |
| [0135](0135-una-rinomina-che-atterra-su-una-nota-viva.md) | Una rinomina che atterra su una nota viva non è una rinomina | Chiude la [§25.1](../roadmap/25-sette-scelte-che-il-codice-ha-preso-senza-dirlo.md#251-una-rinomina-che-atterra-su-una-nota-viva) — l'unica P0 della seduta 25, e l'unica voce di questo piano che fosse una **perdita di dati** invece di una firma. `mv A.md B.md` da un terminale, con `B` viva in anagrafe e il suo buffer sporco, tornava `Ok(true)` senza un avviso e cancellava per sempre il testo non salvato di `B`: `migrate_side_data` celebrava il rito della migrazione d'identità sopra un'identità occupata, e i suoi tre canali — icona e pin, spazio per-documento, bozza — scrivevano ciascuno il dato della sorgente sopra quello della destinazione. La premessa caduta è che il **rito sembrasse uniforme**: quattro canali celebrano la stessa collisione e uno solo, `VersionStore::rename`, aveva la politica scritta e argomentata («buttarne una sarebbe perdere versioni senza dirlo») — chi la leggeva concludeva che valesse per tutti. E `docdata.rs` prometteva in un commento l'invariante che rendeva il buco invisibile — «il path di destinazione era **libero** — il kernel rifiuta un rename verso un documento che esiste» — vero per tre chiamanti su quattro e falso per il watcher, che il rename non lo fa: lo riferisce. Decisione: la forma **(a)** della voce, se `to_id` è già in `metas` **non è un rename** — è `remove(from)` + `sync_path(to)`, cioè la degradazione che stava già dieci righe sopra. La guardia sta nell'**unico punto che i tre canali attraversano**, quindi nessuno dei tre se la ricorda; le altre due porte garantivano già la destinazione libera (un `AlreadyExists`, e un accoppiamento per impronta che guarda solo id che ieri non c'erano), e adesso la precondizione è scritta accanto alla funzione invece che sottintesa. Paga chi ha rinominato — la storia della sorgente si spezza, i suoi dati restano orfani fino alla prima raccolta — e non paga niente di ciò che era della vittima. La **(b)**, migrare senza mai schiacciare, resta aperta come casella: vuole tre politiche di collisione e nessuna è urgente finché la guardia impedisce la perdita. Il banco che mancava è uno per tutti e tre i canali, perché la guardia è una: da rosso stampava `BOZZA-B=Some("il testo non salvato di A")` | 2026-08-09 |
| [0136](0136-una-regola-di-identita-di-un-nome-si-dichiara.md) | Una regola di identità di un nome si dichiara | Chiude la [§25.2](../roadmap/25-sette-scelte-che-il-codice-ha-preso-senza-dirlo.md#252-quante-regole-di-identità-di-un-nome-vuole-fub) nella forma **(a)** che la voce stessa raccomandava, e con lei **quattro** difetti misurati — di cui **tre falsi**. La premessa caduta è quella con cui la voce era nata: quarantaquattro funzioni che decidono «quando due nomi sono lo stesso nome» *sembrano* una duplicazione da unificare, e **non lo sono** — quattro verbali dicono che devono essere più di una ([0020](0020-le-regole-in-un-posto-solo.md): «due requisiti che **devono** divergere, e una fixture che li legasse nascerebbe rossa»; [0107](0107-il-caso-di-una-lettera.md): «la domanda non era una: erano tre», che ripudia la riga «unifichiamo» come «il tipo di riga peggiore: dichiara **coperto** ciò che non lo è»; [0058](0058-un-nome-che-nasce.md); [0115](0115-la-verita-e-la-dichiarazione.md)). La duplicazione vera stava nella **dichiarazione mancante**: la 0115 l'aveva già scritta come zona cieca — «il generato, la fixture e il corpus prendono chi **cambia** una regola, non chi ne **aggiunge** una» — e la [0110](0110-la-struttura-non-e-una-preferenza.md) è la prova del danno, una regola divergente nata **tre commit dopo** la 0107. La **(b)**, una porta `fub_abi::rules` esclusiva, **non si fa**: è la quinta stesura di una riga già ripudiata quattro volte, ed è irreversibile perché WIT-adiacente — mentre metà delle regole che ci salirebbero sono *volutamente* diverse e arriverebbero nel contratto con le proprie eccezioni. Decisione: un **conto sulle sorgenti**, `crates/fub-abi/tests/una_regola_di_nome_si_dichiara.rs`, con **quaranta** righe di allowlist che pretendono **famiglia e ragione**. Banco Rust e non l'undicesimo script di CI perché chi aggiunge la regola sta scrivendo Rust e l'attore che gli risponde nello stesso minuto è `cargo test`; **struttura** da `un_lucchetto_solo.rs` (allowlist controllata nei due versi: chi compare e non è dichiarato è rosso, una riga scaduta è rossa anche lei), **cammino** da `una_sola_tabella_di_escape.rs` — ogni `.rs` sotto un `src/`, senza elenco scritto a mano, perché le regole stanno in **sei** crate e ventisei file e un `include_str!` lungo ventisei righe sarebbe la stessa dimenticanza che il conto cerca. La tassonomia **non è inventata, è estratta**: i criteri delle famiglie stanno già sopra `prefix_len_ci` («gli offset sono il prodotto di questa funzione») e sopra `contiene_a_meno_del_caso` («la corsia veloce vale solo dove è dimostrabilmente la stessa risposta»), e un secondo conto verifica che la famiglia **dichiarata** sia quella che il **gesto** nel sorgente mostra, così la colonna non è una decorazione. **0142 vero e riparato**: `to_lowercase()` scritto a mano in due rename diceva sì a `nota.md`/`Nota.md` e **no** a `Café.md` NFC contro NFD, che su APFS sono un file solo — cioè contraddiceva `resolution_key` proprio sul rename che quella regola protegge; adesso è `solo_il_caso`, una funzione sola, e il presidio non ha più niente da dichiarare lì. **0070 falso**: `İ` e `ẞ` sono le risposte *giuste*, il banco che lo tiene esisteva già, e `tags.rs` **cita quel numero** come motivo di non riusare la regola. **0093 falso sulla conseguenza**: `heading_matches` è una **disgiunzione** il cui secondo ramo passa da `resolution_key`, che la NFC la fa — si rompe l'`id=` HTML, non la risoluzione; il banco mancava ed è stato scritto. **0018 punta al posto sbagliato**: la scansione del ramo `Path` è un ripiego documentato su un link già rotto, quella che si paga sempre è nel ramo `Wiki` ed è il difetto **0115**. Prova rossa nei due versi: una quarantunesima regola finta stampa «1 regole di identità di un nome sono nate senza che nessuno le dichiarasse», una famiglia mentita stampa «dichiara CasoAscii ma nel sorgente si legge {Nfc}». Zone cieche dichiarate: il conto **non ripara nessuna divergenza** — **0140**, **0141** e **0115** restano aperti e le loro righe li nominano per numero invece di travestirli da ragione —, `frontend/` resta fuori, e una regola scritta senza uno dei gesti che il conto legge passa (`MemoryHost::data_list`, che usa `starts_with`): la maglia prende il gesto **comodo**, che è l'unico che qualcuno farà avendo fretta. | 2026-08-09 |
| [0138](0138-una-finestra-di-220-caratteri-attorno-al-link.md) | Una finestra di 220 caratteri attorno al link | Chiude la [§25.4](../roadmap/25-sette-scelte-che-il-codice-ha-preso-senza-dirlo.md#254-quanto-contesto-porta-un-backlink) nella forma **(b)** che la voce stessa raccomandava: il contesto di un backlink è una finestra di 220 caratteri attorno al link, e la regola sta in `fub-abi::rules::snippet` col tetto `SNIPPET_CHARS` migrato lì da `fub-features` in un posto solo — il tetto resta fuori dal contratto (0094), in caratteri e non in byte, e il link non si taglia mai. Con lei il difetto **0110**, chiuso come «vera e trascurabile, detta coi numeri»: non tre copie ma **due copie e una move**, e ognuna scende a ≤222 caratteri — 969 KB invece di 53.994.565 byte, l'1,8%. | 2026-08-09 |
| [0139](0139-un-guasto-dell-avvio-si-tira-non-si-spinge.md) | Un guasto dell'avvio si tira, non si spinge | Chiude la [§25.5](../roadmap/25-sette-scelte-che-il-codice-ha-preso-senza-dirlo.md#255-quando-la-cartella-di-configurazione-non-si-può-scrivere) nella forma **(a)** che la voce stessa raccomandava, con la clausola che fissa questo verbale: la porta è un **tiraggio**, non una spinta all'avvio — la cartella di configurazione che non si può scrivere (o non c'è) si dice una volta per sessione su un `Event::Trouble` di severità `Warning`, l'avviso esce da `install_logging`, l'host lo consuma con un `take`, e la shell lo chiede appena il router è in piedi perché una spinta nascerebbe prima del ponte e di ogni ascoltatore e andrebbe persa in silenzio | 2026-08-09 |
| [0140](0140-dove-stanno-i-byte-di-un-kind-di-terzi.md) | Dove stanno i byte di un kind di terzi | Chiude la [§25.7](../roadmap/25-sette-scelte-che-il-codice-ha-preso-senza-dirlo.md#257-dove-stanno-i-byte-di-un-kind-di-terzi) nella forma **(b)** che la voce stessa raccomandava: la chiave del carico di un kind di terzi è `source`, la regola sta in `fub-abi::rules::carichi` accanto alla tabella del core — che per costruzione non può ospitarla (il conto a due versi di `ogni_kind_dichiara_cosa_porta` rifiuta la riga che non nomina una `const`) — e il campione a tre chiavi esce dal renderer, che chiede la risposta al contratto. Il silenzio a runtime resta e si dichiara nella forma della 0052: chi vede il guasto non ha il bus fra le mani. Il banco `un_kind_di_terzi_degradato_mostra_i_byte_della_chiave_convenzionale` è rosso nei due versi (chiave rinominata → `<div>` vuoto; campione riallargato → `TESTO-SBAGLIATO` vince su `source`). | 2026-08-09 |
| [0141](0141-la-prima-fotografia-di-un-vault-esce-dalla-fase-1.md) | La prima fotografia di un vault esce dalla fase 1 | Chiude la [§25.3](../roadmap/25-sette-scelte-che-il-codice-ha-preso-senza-dirlo.md#253-dove-sta-la-prima-fotografia-di-un-vault) nella forma **(a)** che la voce stessa raccomandava: la finestra scoperta resta **zero** e si sposta soltanto *dove* sta la chiamata — la passata esce dalla fase 1 e la chiama il runner, una volta per apertura, prima della prima fetta, perché la 0070 riserva la fase 1 a *quali documenti esistono* e la passata legge il **contenuto**. Il ramo `Event::VaultOpened` e la sua maschera si tolgono; `InCorso` porta una chiusura consumata col `take`, quindi la garanzia una-sola-volta è **il tipo, non un flag**. La forma approvata — un `JobHost` per-capacità, per non tenere l'esclusivo — è morta sul banco: chiudeva un ciclo di lock fra lo store e le scritture normali (`concorrenza.rs` appeso >60 s), e la passata gira sotto l'esclusivo come in fase 1. Due banchi rossi nei due versi (`la_prima_fotografia_precede_la_prima_fetta`, `l_evento_vaultopened_non_fotografa_piu`). Il residuo O(N²) di `inner.docs.clone()` resta **dichiarato e non aperto**: la sua riparazione dipende dal difetto `0113`. | 2026-08-09 |
| [0142](0142-il-vocabolario-del-metodo-esce-dal-glossario.md) | Il vocabolario del metodo esce dal glossario | Il glossario è il vocabolario del prodotto, non del metodo. Le parole del metodo (banco, presidio, ecc.) si spostano in `leggimi-prima.md`. Questo verbale giustifica l'eliminazione di 21 definizioni dal glossario. | 2026-08-11 |
| [0143](0143-i-verbali-si-possono-riscrivere.md) | I verbali si possono riscrivere nella forma | Modifica la convenzione della 0014: il contenuto dei verbali resta immutabile, ma la forma può essere riscritta per renderli più chiari, brevi e leggibili. Nome file e numeri non cambiano mai. | 2026-08-11 |

Una decisione nuova prende il numero successivo — mai uno già usato, nemmeno se
il verbale che lo portava è stato superato — e il verbale ci si sposta
**intero** nel momento in cui la voce di `todo.md` si chiude.

La [0046](0046-l-anagrafe-del-vault.md) chiude **due voci in un verbale solo**,
ed è il caso opposto a quello della 0031/0032: là una voce era troppo grande per
un verbale, qui due voci erano lo stesso lavoro visto da due lati — il piano lo
diceva già («vanno fatte nella stessa passata»), e scriverle in due verbali
avrebbe voluto dire raccontare due volte la stessa scansione. La seduta 14 resta
aperta con le altre due, che sono l'altra coppia.

La [0044](0044-lo-stato-per-documento.md) è il primo verbale che esiste **per
via di un altro**: il §13.2 era una generalizzazione condizionale — «se
l'identità resta il path…» — e la [0043](0043-il-path-e-la-chiave.md) ha reso
vera la condizione. Le due si leggono in fila, come la 0031 e la 0032, ma per la
ragione opposta: là una voce era troppo grande per un verbale solo, qui erano
due voci e la prima ha deciso cosa fosse la seconda.

La [0037](0037-lo-stato-di-vista.md) è il secondo caso di mezza voce, e
diversamente dal primo la seconda metà **non ha ancora una data**: il layout del
§11.2 aspetta il modello di layout, perché oggi l'area principale è un pannello
solo e non c'è nessuna disposizione da salvare. La voce resta aperta, come deve.

La [0031](0031-chi-possiede-i-bundle.md) è la prima che ne ha chiusa **mezza**:
il §9.3 chiedeva quattro cose, e le ultime tre — il runner, la cancellazione,
l'isolamento — andavano decise insieme o la prima si sarebbe riscritta per fare
posto alle altre. La voce è rimasta aperta finché non è arrivato il secondo
verbale, la [0032](0032-il-runner-dei-job.md), che la chiude: le due si leggono
in fila, e ognuna porta in testa il rimando all'altra. Un verbale per *pezzo di
voce* si scrive quando il pezzo è una decisione intera, non quando il lavoro è
lungo: il criterio è sempre quello, un ragionamento che fra sei mesi non si
ricostruisce dal diff.

La [0049](0049-una-posizione-dentro-un-documento.md) e la
[0050](0050-cosa-si-chiede-a-una-ricerca.md) sono la prima **coppia decisa in
una volta sola**, e il taglio non è per numero di voci: la 0049 chiude le due
che chiedevano la stessa primitiva da due firme diverse (dove sta un risultato,
dove punta un riferimento), la 0050 le due che toccavano lo stesso record
(`TextQuery`). Deciderle in quattro verbali avrebbe voluto dire aprire due volte
la stessa firma; deciderle in uno avrebbe messo insieme due ragionamenti che non
si sostengono a vicenda. Il criterio resta quello della 0031/0032: un verbale è
un ragionamento intero, non una quota di lavoro.

La [0051](0051-l-alimentazione-risponde.md) e la
[0052](0052-cio-che-va-storto-e-un-evento.md) sono la seconda **coppia decisa in
una volta sola**, e la spartizione è di nuovo per ragionamento e non per numero
di voci: la 0051 dà un esito a chi non l'aveva, la 0052 gli dà una destinazione
e toglie di mezzo chi lo buttava. Ma c'è un precedente nuovo, ed è la 0052:
chiude **due voci meno una casella**. Il §20.3 si chiude intero; il §20.2 chiude
la forma — la variante, la severità, il soggetto, il primo consumatore — e
lascia dietro l'**adozione**, cioè i ventisette punti che scrivono su `stderr` e
vanno convertiti uno a uno. È il caso opposto alla mezza voce della
[0031](0031-chi-possiede-i-bundle.md): là mancava metà del *ragionamento* e la
voce è rimasta aperta, qui il ragionamento è intero e ciò che resta è lavoro che
non decide niente. Il criterio per distinguerli è sempre lo stesso: una casella
residua è ciò che si può fare **senza aprire un verbale**.

La [0053](0053-il-contratto-ha-una-sorgente.md) chiude due voci in un verbale
solo per la ragione della [0046](0046-l-anagrafe-del-vault.md) — erano lo stesso
lavoro visto da due lati — ma con una differenza che vale la pena: **lo diceva
la seduta, non il verbale**. Il file della
[seduta 16](../roadmap/16-crate-sdk-banchi-di-prova.md) portava scritto in testa
che «la 16.5 non è una voce autonoma: è la gamba TS della domanda che pone la
16.4», e che deciderle separate avrebbe voluto dire decidere due volte la stessa
cosa, la seconda contro la prima. È il primo caso in cui l'accorpamento è
**dichiarato in anticipo** invece di essere scoperto scrivendo: un cappello di
seduta può decidere la forma del verbale che la chiuderà, ed è una cosa che i
cappelli fanno bene perché li si scrive guardando le voci insieme.

La [0054](0054-il-banco-del-lato-provider.md) e la
[0055](0055-il-banco-del-lato-host.md) sono la **terza coppia decisa in una
volta sola**, e portano il caso **opposto** a quello della 0053. Là il cappello
della seduta dichiarava in anticipo che due voci andavano chiuse **insieme**;
qui lo stesso cappello, con la stessa forma — una frase in testa che parla di
entrambe le voci —, dichiarava fra loro un **confine**: *«sono due banchi
diversi… che non possono stare nello stesso crate»*. E un confine fra due cose è
precisamente ciò che le rende due. Da tenere, perché altrimenti il precedente
della 0053 si legge storto: **un cappello va letto per cosa afferma, non per
quante voci nomina** — «sono la stessa domanda vista da due lati» chiede un
verbale, «fra loro c'è un confine» ne chiede due.

E ne inaugurano un altro, che è di metodo e non di forma: la 0054 chiude una
voce **smentendo un presidio che tutti credevano di avere**. Il cappello della
seduta 16 diceva che il kernel dentro l'SDK «violerebbe l'invariante che
`dependency_invariant.rs` presidia»; quel file non nominava `fub-sdk` da nessuna
parte. È la **sesta** specie della famiglia che il §16.7 elenca, e la peggiore:
un conteggio invecchiato fa sopravvalutare una copertura, un *limite*
invecchiato la fa sottovalutare, ma una **garanzia dichiarata che non è mai
esistita** fa entrambe le cose insieme — e nessuno va a controllare una
garanzia, perché il motivo per cui la si scrive è smettere di doverci pensare.

La [0056](0056-un-elenco-che-e-la-sorgente.md) e la
[0057](0057-la-dieta-dell-ipc.md) sono la **quarta coppia decisa in una volta
sola**, e ripetono di proposito la forma che la 0054/0055 aveva prodotto per
caso: il cappello ha guardato le due voci insieme, ha trovato che ponevano lo
stesso difetto in due direzioni opposte — un elenco su cui si *itera* non nota
le aggiunte, uno con cui si *asserisce un'uguaglianza* non può che notarle — e
ha concluso che fra loro c'è un **confine**. Il confine è meccanico: *la
produzione può leggere l'elenco?* Sulle view sì, e l'elenco diventa la sorgente
da cui la cosa esiste; sui comandi Tauri no, perché una macro non itera, e
l'elenco resta una copia da confrontare. Stessa tassonomia, due risposte, due
verbali.

E la 0056 inaugura un caso che non c'era: chiude una voce **meno la sua seconda
metà**, e quella metà non è una casella residua — è una **voce nuova**, la
§16.8. È il rovescio esatto della [0053](0053-il-contratto-ha-una-sorgente.md):
là due voci erano lo stesso ragionamento e sono diventate un verbale, qui una
voce ne teneva due e ne è nata una in più. Il criterio per distinguere questo
caso da quello della [0052](0052-cio-che-va-storto-e-un-evento.md) — la voce
chiusa che lascia una casella — è sempre lo stesso: **una casella residua è ciò
che si può fare senza aprire un verbale**. Presidiare la prosa che conta i
sorgenti chiede di decidere che forma abbia l'annotazione, quindi non lo è.

E c'è una seconda cosa che la [0053](0053-il-contratto-ha-una-sorgente.md)
inaugura: chiude una voce **smentendone la premessa**. Il §16.4 escludeva i tipi
Rust perché «la sorgente autorevole è il WIT, ed è già il repo a trattarlo
così»; controllato contro `wit_conformance.rs`, il repo tratta come autorevole
esattamente Rust — parsa il WIT perché è ciò che controlla. Le decisioni
precedenti hanno corretto **numeri** di una voce (la
[0052](0052-cio-che-va-storto-e-un-evento.md) ne ha corretti quattro); questa
corregge un **fatto sull'architettura** su cui la voce poggiava per intero, e la
conclusione è cambiata di conseguenza: ha ragione il §16.5 sulla direzione, e
torto sullo strumento.

La [0058](0058-un-nome-che-nasce.md) inaugura un terzo modo di lasciare qualcosa
dietro, e va distinto dai due che ci sono. Una **casella residua**
([0052](0052-cio-che-va-storto-e-un-evento.md)) è lavoro che si può fare senza
aprire un verbale, e resta attaccata alla voce chiusa; una **voce nuova**
([0056](0056-un-elenco-che-e-la-sorgente.md)) nasce quando quel pezzo chiede una
decisione sua. Qui invece due righe della voce — i symlink, e i dotfile da
mostrare su richiesta — non erano né l'una né l'altra: erano **della voce
sbagliata**. Un symlink non è una domanda su un *nome*, è «questa voce di
directory partecipa», che è la domanda del §15.6; e sono state spostate là,
dentro la sua lista di caselle, invece di restare un residuo del §15.5.

Il criterio per riconoscere il caso è la stessa domanda che il §16.7 pone a un
elenco: *chi lo legge, lo trova?* Una casella residua vive nel paragrafo di
`todo.md` che le conta, e chi apre la voce che la eredita non la vede. Una riga
consegnata alla voce che la farà sta dove la cercherà chi la farà — e per questo
non entra in nessun totale delle caselle residue: non è rimasta indietro, ha
cambiato indirizzo.

La [0060](0060-il-modello-dice-il-vero-sui-byte.md) è il **terzo** caso di mezza
voce, e porta una ragione che i due precedenti non avevano. Nella
[0031](0031-chi-possiede-i-bundle.md) mancava metà del *ragionamento*; nella
[0037](0037-lo-stato-di-vista.md) mancava il modello su cui la seconda metà
poggia. Qui il ragionamento è intero e la metà che resta non aspetta una
decisione: aspetta **un posto dove girare** — un carico che domini l'overhead e
una macchina che non divida i core, che è ciò che la §8.4 ha già scoperto
misurando ([0026](0026-due-query-insieme.md)). Da tenere perché precisa il
confine con la casella residua della
[0052](0052-cio-che-va-storto-e-un-evento.md): una casella è ciò che si può fare
**senza aprire un verbale**, e qui la seconda metà lo sarebbe se ciò che le
manca si comprasse scrivendo codice. Non si compra, quindi la voce resta aperta.

E il taglio non l'ha scelto il verbale: l'ha scelto **il cappello della seduta**.
Quel cappello giudica le sue voci su *se il costo cresce con l'attesa* — un
criterio scritto per ordinare tre voci fra loro — e applicato **dentro** una l'ha
tagliata in tre: il corpus, il cui costo cresceva; il banco delle prestazioni, che
aspetta una macchina; e una riga che non stava né di qua né di là. È la terza cosa
che un cappello di seduta si scopre capace di fare, dopo l'accorpamento dichiarato
della [0053](0053-il-contratto-ha-una-sorgente.md) e il confine dichiarato della
[0054](0054-il-banco-del-lato-provider.md)/[0055](0055-il-banco-del-lato-host.md).

Le tre parti però si sono viste in due tempi, e per una ragione che vale un
criterio: **un cappello di seduta ordina ciò che vede da fuori.** La 0060 aveva
già scritto che delle cinque caselle del §17.1 «due sono chiuse, due aspettano
la macchina, e la quinta […] non aspetta nessuna delle due cose» — quella quinta
aspettava il corpus, cioè un pezzo della voce stessa, che un criterio scritto
per mettere in fila delle voci non ha modo di vedere. L'ha chiusa la
[0061](0061-un-giro-che-non-passa-dal-modello.md). Da tenere: **un taglio si
dichiara per quante parti ha**, e quel numero è un'affermazione su oggi — si
corregge dov'è scritto, come un conteggio e non come un ragionamento.

La [0061](0061-un-giro-che-non-passa-dal-modello.md) porta poi un caso che i tre
modi di lasciare qualcosa dietro non coprono, perché non è un modo di lasciare
qualcosa dietro: è un modo di **sbagliare la classificazione** di ciò che si
lascia. La 0060 aveva chiamato quella quinta casella «lavoro», che per il
criterio di questa cartella vuol dire *casella residua*: ciò che si fa senza
aprire un verbale. E un verbale è stato aperto. Il criterio non si emenda — **la
previsione era sbagliata**: la riga sembrava lavoro perché il round-trip
sembrava uno, e facendola si è visto che i versi del trasferimento sono due e
non pretendono la stessa cosa, che è una decisione intera e non una quota di
lavoro. Non è quindi una casella residua, né una voce nuova come nella
[0056](0056-un-elenco-che-e-la-sorgente.md), né una riga consegnata altrove come
nella [0058](0058-un-nome-che-nasce.md): la riga è rimasta dov'era e ci è stata
spuntata. Ciò che vale la pena tenere è che classificare un residuo è
**prevedere**, e la previsione si fa guardando la riga — che può mentire. La
conseguenza è mite, ed è la ragione per cui il criterio resta com'è: sbagliarla
costa un verbale in più, cioè costa che una decisione venga scritta invece che
no.

La [0062](0062-il-log-e-il-pavimento-l-evento-e-la-porta.md) inaugura un quarto
modo di lasciare qualcosa dietro, ed è il primo in cui a chiudersi è una
**casella residua di un'altra voce** — non per averla fatta a mano, ma per
averne dato il criterio. La [0052](0052-cio-che-va-storto-e-un-evento.md) aveva
lasciato come casella residua «portare dentro il canale i ventisette punti che
scrivono su `stderr`», e la classificava *casella residua* perché era lavoro
senza decisione. Facendola, si è visto che la domanda nascosta era un'altra: i
ventisette non avevano una destinazione sola, ne avevano **due** — il log per
chi sviluppa, il canale degli eventi per chi legge — e scegliere chi va dove era
la decisione intera della 0062. Fatta quella, la casella è scesa a zero da sé:
il mestiere lo ha svolto una voce che non era nata per lei. Vale la pena
distinguerlo dalla riga consegnata altrove della
[0058](0058-un-nome-che-nasce.md): là una riga cambiava indirizzo perché
apparteneva a un'altra domanda; qui una casella si **chiude** perché la
decisione che la risolve è la stessa di un'altra voce. Le caselle residue di
`todo.md` scendono da nove a otto.

E una cosa di metodo, che vale la pena scrivere qui perché allora
[CONTRIBUTING.md](../CONTRIBUTING.md) diceva che **i verbali sono immutabili**,
senza distinguere fra contenuto e forma — la distinzione arriva dopo, con la
[0143](0143-i-verbali-si-possono-riscrivere.md), e questo paragrafo è il primo
posto in cui il repo se n'è accorto. La 0060 corregge un numero dentro la
[0054](0054-il-banco-del-lato-provider.md), che è già stata toccata una volta
dopo la chiusura — quando il §16.7 ha mostrato che la specie che rivendicava era
la sesta e non la quinta. Le due cose non sono in contrasto, perché ciò che non
si riscrive è il **ragionamento**: cosa si è deciso, cosa si è scartato, perché.
Un **conteggio dei sorgenti** dentro un verbale non è un ragionamento — è
un'affermazione su oggi che invecchia da sola, e in quel caso invecchiava già
nel commit che la scriveva. Si corregge dov'è, e il caso si consegna alla
[§16.8](../roadmap/16-crate-sdk-banchi-di-prova.md), che è la voce che tiene
quell'elenco.

La [0064](0064-il-supporto-sta-sotto.md) lascia una **casella residua** — la
terza specie, quella della [0052](0052-cio-che-va-storto-e-un-evento.md) — con
una precisazione che le altre non avevano: sa **quale voce** le darà il
criterio. Le tre righe di `.fub/` che scrivono con `write_atomic` hanno già la
proprietà che il supporto non promette, quindi portarle sopra il trait adesso
vorrebbe dire toglierla; il criterio lo dà il §15.2. È il rovescio della
[0062](0062-il-log-e-il-pavimento-l-evento-e-la-porta.md), dove una casella si è
**chiusa** perché la decisione di un'altra voce la risolveva senza esserne nata
per lei: qui una casella si **apre** già indirizzata. Il criterio per
distinguerla da una mezza voce resta quello di sempre — non manca un
ragionamento a questa decisione, manca un pezzo di lavoro a un'altra.

E ne inaugura una che non è un modo di lasciare qualcosa dietro: un **buco
dichiarato**. `plugin_data_dir` consegna a un provider nativo una vera cartella
del filesystem, e su un supporto che cifra è il punto in cui la cifratura si
ferma. Non è lavoro rimandato — è un fatto sulla forma dei provider nativi, la
cui risposta è M5 — quindi non è una casella e non entra in nessun totale; ma
sta scritto nel verbale perché chi implementerà quel supporto deve **trovarlo**
prima di scoprirlo. La differenza con una casella è che una casella si spunta,
un buco dichiarato si legge.

La [0065](0065-una-scrittura-o-c-e-o-non-c-e.md) chiude la casella che la
[0064](0064-il-supporto-sta-sotto.md) aveva aperto **già indirizzata**, ed è il
secondo caso in cui una casella residua si chiude dentro un'altra voce dopo
quello della [0062](0062-il-log-e-il-pavimento-l-evento-e-la-porta.md). La
differenza fra i due è tutta nella previsione: la 0052 aveva lasciato i
ventisette punti di `stderr` senza sapere chi li avrebbe presi, e a prenderli è
stata una voce che non era nata per loro; la 0064 aveva scritto **quale voce**,
e quella voce ha fatto esattamente ciò che c'era scritto. Vale la pena tenerlo
perché di indirizzi scritti su una casella ce n'era uno solo, e adesso si sa che
non era un auspicio: le caselle residue di [todo.md](../todo.md) scendono da
dieci a nove, e i posti da sei a cinque.

Ed è la **quarta mezza voce**, dopo la [0031](0031-chi-possiede-i-bundle.md), la
[0037](0037-lo-stato-di-vista.md) e la
[0060](0060-il-modello-dice-il-vero-sui-byte.md), con una ragione che nessuna
delle tre aveva. Nella 0031 mancava metà del ragionamento; nella 0037 il modello
su cui la seconda metà poggia; nella 0060 una macchina su cui girare. Qui la
metà che resta è **un'altra domanda**: la 0065 dice cosa promette una scrittura,
e ciò che resta — il buffer di crash, il journal, i comandi di manutenzione —
non è la scrittura ma il **recovery**, cioè cosa si fa dopo che è andata storta.
Il titolo della voce le nominava entrambe («durabilità *e* recovery») e le
teneva insieme perché sembravano lo stesso argomento; farne una ha mostrato che
il taglio era già scritto nel titolo. Con una sola eccezione, e sta nella prima
metà per una ragione tecnica e non concettuale: la *lost update* è durabilità,
non recovery, e resta aperta perché la primitiva che la chiude —
`std::fs::File::lock` — chiede di alzare l'MSRV, che è una decisione sua.

La [0066](0066-un-aggiornamento-non-e-una-scrittura.md) chiude quell'eccezione,
e porta due cose che vale la pena tenere.

La prima è una **previsione confermata**, ed è il rovescio esatto del caso che
la [0061](0061-un-giro-che-non-passa-dal-modello.md) aveva inaugurato. Là la
0060 aveva classificato «lavoro» una riga che si è rivelata una decisione
intera, e il criterio non si era emendato perché a sbagliare era stata la
previsione. Qui la 0065 aveva classificato **decisione** una riga che poteva
sembrare lavoro — un lock è quattro righe di codice — e aveva ragione: la riga
che è costata non è il lock, è l'**MSRV**. Le due insieme dicono cosa vale
davvero il criterio di questa cartella: *una casella residua è ciò che si può
fare senza aprire un verbale* si applica guardando la riga, e la riga si può
leggere in entrambi i versi. Costa un verbale in più quando si sbaglia per
difetto, e una decisione presa di straforo dentro un verbale su un altro
argomento quando si sbaglia per eccesso — che è il verso caro.

La seconda è che è il primo verbale il cui costo non è codice ma una **promessa
verso l'esterno**. Le decisioni di questa cartella si pagano quasi sempre in
lavoro nostro; questa si paga in `rust-version`, cioè in una riga che
[versionamento.md](../versionamento.md) chiama parte del contratto e che qualcun
altro legge per sapere se può compilare. La forma che ne segue è quella che il
verbale ha: le due strade — alzare l'MSRV, o prendere una dipendenza — si sono
guardate come **due promesse a due platee diverse**, non come due
implementazioni, e ha deciso quale delle due platee può fare qualcosa in
risposta. Chi ricompila oggi aggiorna la toolchain; chi installa una dipendenza
in più se la tiene per sempre.

La [0067](0067-il-registro-di-cio-che-e-successo.md) porta due precedenti, e
nessuno dei due riguarda cosa ha deciso.

Il primo è la terza specie di **premessa sbagliata** che una voce può contenere,
e va distinta dalle due che ci sono. La
[0053](0053-il-contratto-ha-una-sorgente.md) ha chiuso una voce smentendone un
**fatto sull'architettura**; la [0052](0052-cio-che-va-storto-e-un-evento.md) ne
ha corretti dei **numeri**. Qui la riga sbagliata era una **classificazione**:
«append-only in `.fub/data/`», cioè un dato autorevole scritto sotto la radice
di ciò che si butta e si rifà. Non è un fatto verificabile contro i sorgenti
come quello della 0053 — nel momento in cui la riga è stata scritta il file non
esisteva — ed è più insidioso di un numero, perché una classe sbagliata non
diventa falsa col tempo: nasce falsa e ha l'aria di un'istruzione. La regola che
la smentisce è la [0048](0048-una-radice-sola.md), che era già scritta e che
nessuno aveva riletto scrivendo la riga. Da tenere come criterio: **una riga di
`todo.md` che dice *dove* va un dato è una previsione, e si verifica contro la
regola prima di eseguirla** — è il gemello, sull'asse della classe, di ciò che
la §21.10 ha insegnato sull'asse delle affermazioni esterne.

Il secondo è di metodo, e riguarda le frasi che stanno in testa ai moduli.
Quella di `storage.rs` diceva «sette operazioni, e chi ne aggiunge un'ottava sta
chiedendo al supporto di sapere qualcosa sul contenuto», e l'ottava è arrivata.
Non l'ha aggiunta chi non l'aveva letta: l'ha aggiunta il verbale che ci ha
argomentato contro, e la frase è rimasta nel modulo — riscritta come **metro** e
non come divieto — perché è quella che ha costretto a trovare il criterio vero,
che stava poche righe sotto (*ciò che si compone dalle altre ha un default e non
è una capacità in più*). È il rovescio della **garanzia dichiarata che non
esisteva** della [0054](0054-il-banco-del-lato-provider.md): là una frase in
prosa aveva fatto credere a una copertura che nessuno aveva, qui una frase in
prosa ha impedito un'aggiunta finché non c'era la ragione per farla. Le due
insieme dicono cosa vale una frase scritta in testa a un modulo: **non presidia
niente e orienta tutto**, e si tocca scrivendo perché, non cancellandola.

La [0025](0025-la-ricerca-predefinita.md) è l'altra eccezione, ed è dichiarata
come tale: non chiude una voce, ne **apre** nove. Sta qui lo stesso perché il
criterio di questa cartella è il *perché*, non la direzione: chi fra un anno
troverà nel contratto congelato un modo di chiedere una ricerca tollerante ai
refusi deve poter leggere perché quella scelta è finita in una firma WIT invece
che dentro un provider.

La [0068](0068-un-vault-si-apre-per-quel-che-si-legge.md) porta un precedente
sul criterio di questa cartella, ed è il **terzo** modo in cui una voce si può
dividere a metà.

I due che c'erano dividevano per **proprietà**: la
[0031](0031-chi-possiede-i-bundle.md) ha preso una domanda di possesso e
lasciato l'esecuzione alla [0032](0032-il-runner-dei-job.md), che era un confine
fra due argomenti. Qui il taglio è fra un **prerequisito** e ciò che lo
richiede, e la differenza si vede nel fatto che le due metà non sono
scambiabili: la seconda — l'apertura a fasi, col progresso e la cancellazione —
non si sarebbe potuta prendere per prima, perché un'apertura osservabile che si
interrompe al primo documento illeggibile mostra una barra di avanzamento che
arriva al 40% e poi dice che non si apre niente. Da tenere come criterio:
**quando una voce nomina due cose e una regge l'altra, il taglio è già
dichiarato dalla voce**, e non serve un cappello che lo autorizzi.

E una seconda nota, che riguarda i presidi e non il taglio. Dei suoi otto
sabotaggi il più utile non ha **confermato** niente: togliere gli scarti
dall'insieme che `reconcile` dichiara completo ha reso rosso un test scritto per
un'altra ragione, e ha mostrato che l'insieme costruito dai soli documenti
indicizzati avrebbe detto agli indici che una nota illeggibile è sparita. Il
difetto non esisteva prima di questa voce — non poteva, perché prima un
documento non letto non lasciava aprire il vault — ed è nato e morto dentro lo
stesso turno. È l'argomento della [0054](0054-il-banco-del-lato-provider.md)
sull'altro verso: là un presidio dichiarava una copertura che non aveva, qui un
presidio ha coperto qualcosa che chi lo scriveva non stava cercando.

La [0069](0069-cosa-sa-dire-un-abbonamento.md) porta due precedenti, e nessuno
dei due riguarda cosa ha deciso.

Il primo è un accorpamento **dichiarato da un cappello che ha torto**, ed è il
caso che mancava fra i due che ci sono. La [0053](0053-il-contratto-ha-una-sorgente.md)
ha chiuso due voci in un verbale perché il cappello della sua seduta lo diceva in
anticipo; la [0054](0054-il-banco-del-lato-provider.md)/[0055](0055-il-banco-del-lato-host.md)
ne ha chiuse due in due perché lo stesso genere di frase dichiarava fra loro un
**confine**. Qui il cappello afferma che le tre voci della seduta sono «tre
estensioni della stessa maschera», e quell'affermazione **era già stata smentita**
dalla [0063](0063-la-maschera-e-dell-esemplare.md): la §22.3 non è finita in un
campo di `EventMask`, è diventata una funzione su un'altra interfaccia. Eppure
l'accorpamento regge — per una ragione che il cappello non aveva visto: non è il
*record* a legare le voci, è la **regola** che il ritiro della 0063 aveva messo a
verbale (*una dichiarazione che il kernel non valuta mente a chi la scrive*), e
applicarla una volta sola dà due case diverse. Da tenere perché precisa il
criterio della 0054: un cappello va letto per cosa afferma, sì — e ciò che
afferma può essere **falso** senza che la sua conclusione lo sia. Un cappello
sbagliato non si eredita e non si butta: si rifà il ragionamento, e si scrive
quale delle due parti ha retto.

Il secondo è la **terza specie di premessa sbagliata**, e va distinta dalle due
che la [0067](0067-il-registro-di-cio-che-e-successo.md) elenca. La
[0053](0053-il-contratto-ha-una-sorgente.md) ha smentito un *fatto
sull'architettura*, sbagliato nel momento in cui fu scritto; la 0067 una
*classificazione*, nata falsa e con l'aria di un'istruzione. Qui la premessa
della [0013](0013-elenco-delle-capacita.md) — «il kernel è sincrono e non
possiede thread» — era **vera quando è stata scritta**, e l'ha resa falsa la
[0032](0032-il-runner-dei-job.md), cioè un'altra voce, che non sapeva di
toccarla. Non è un errore di nessuno: è la specie di riga che invecchia perché
il repo si muove sotto di lei, ed è la più difficile da vedere, perché
rileggerla contro i sorgenti *del suo tempo* non la smentisce. Il criterio che
ne segue: **una voce che eredita la conclusione di un verbale eredita anche la
sua premessa, e quella è un'affermazione su ieri** — si riverifica contro il
repo di oggi prima di appoggiarcisi. Qui la conclusione ha retto lo stesso, ma
per l'altra regola della stessa 0013, e il verbale dice quale.

La [0070](0070-un-vault-si-apre-in-due-tempi.md) porta un precedente sui
**sabotaggi**, e va nel verso scomodo. Il metodo delle 0066/0067/0068 è sabotare
il codice e guardare quale presidio diventa rosso; la 0068 aveva già mostrato
che un sabotaggio serve anche quando **non** conferma — là ne aveva trovato un
difetto. Qui due dei sei non hanno confermato niente e non hanno trovato niente:
sono rimasti **verdi**, e il verde diceva che le due promesse più centrali della
voce — un'indicizzazione si ferma, chi la ferma riceve comunque un esito — non
erano presidiate affatto. Nessuno se n'era accorto perché i presidi dell'host le
attraversano tutte e due senza asserirle.

Da tenere come criterio: **un sabotaggio verde è un risultato, non un passaggio
a vuoto**, e la sua risposta non è indebolire la frase del verbale finché
diventa vera — è scrivere il presidio che manca. Il che porta con sé la ragione
per cui mancava: entrambe le promesse, su un pool acceso, si osservano solo
indovinando un istante. I due presidi nuovi mettono in scena quel momento —
`avanza_apertura` chiamata a mano con la bandiera già alzata, un pool fermato
**senza thread** — invece di aspettarlo, ed è la stessa regola con cui la
[0032](0032-il-runner-dei-job.md) aveva provato le bandiere su `Flags` e non su
dei thread veri. La regola vale anche al contrario: se un sabotaggio non si può
rendere rosso in modo deterministico, il posto da cui provarlo non è quello.

La [0071](0071-una-feature-si-spegne-dove-si-dichiara.md) porta un precedente
che è il §16.8 visto **dal lato in cui la prosa falsa si crea**, e non da quello
in cui si scopre. `[FeatureUfficiale; 8]` e le sei righe che dicevano «gli otto
bundle», «le nove righe», «nessuna delle nove» erano tutte vere fino al commit
che ha reso quel numero condizionale. Il criterio: **chi rende condizionale un
conteggio è l'unico che sa dove sono le righe che lo ripetono**, e riscriverle è
parte del lavoro, non una pulizia successiva.

E un secondo, sui presidi che diventano rossi per un caso nuovo e **legittimo**:
non si indeboliscono, si circoscrivono. Il conto «zero view non è una suite»
della [0056](0056-un-elenco-che-e-la-sorgente.md) è rimasto identico; è cambiata
la condizione in cui gli si fa la domanda
(`any(backlinks, outline, tags, stats)`). Abbassare la soglia avrebbe spento il
presidio anche nel caso per cui era stato scritto — che è la differenza fra le
due mosse, ed è l'unica che conta.
