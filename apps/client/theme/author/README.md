# Tema theme-1

Fonte generata: `npm run theme:generate`. Verifica: `npm run theme:verify`.

Il contratto richiede **116 ruoli**, espone **194 hook**, **7 stati** (111 assegnazioni su 39 componenti). Hook non assegnati: **0**. Gli elenchi e le coppie di contrasto sono in `contract.json`, generato dalle sorgenti della shell.

Una cartella installabile contiene `manifest.json`, un foglio per luce, `skin.css` e gli asset locali. Il campione `sample/` è un bundle non-serie completo, generato dagli stessi cancelli.
