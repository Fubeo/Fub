<!-- Fub — AndroidManifest snippet (template, MobileOwner P14/F37).
     Fusione a cura di NativeIntegration dentro gen/android/app/src/main/AndroidManifest.xml
     dopo `cargo tauri android init` (NON eseguito qui). Solo aggiunte: nessun
     permesso pericoloso nuovo (niente READ_EXTERNAL_STORAGE: si passa per
     Storage Access Framework + intent, quindi niente permessi da revocare). -->
<manifest>
  <application>
    <activity>
      <!-- Ricevere testo/appunti/URL da altre app: ACTION_SEND testuale. -->
      <intent-filter>
        <action android:name="android.intent.action.SEND" />
        <category android:name="android.intent.category.DEFAULT" />
        <data android:mimeType="text/plain" />
      </intent-filter>
      <!-- Ricevere una o più immagini/file: il bridge li copia in cache (vedi ShareBridge.kt). -->
      <intent-filter>
        <action android:name="android.intent.action.SEND" />
        <action android:name="android.intent.action.SEND_MULTIPLE" />
        <category android:name="android.intent.category.DEFAULT" />
        <data android:mimeType="image/*" />
        <data android:mimeType="video/*" />
        <data android:mimeType="audio/*" />
        <data android:mimeType="application/pdf" />
        <data android:mimeType="text/markdown" />
      </intent-filter>
      <!-- Aprire note markdown dal file manager. -->
      <intent-filter>
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
        <data android:mimeType="text/markdown" />
        <data android:scheme="content" />
        <data android:scheme="file" />
      </intent-filter>
      <!-- Deep link fub:// (open/new/daily/unique/search/capture/mobile-tree):
           TreeGrant classificato in mobile.rs `OpenedUrlKind::TreeGrant` prima
           del parser AutomationOwner; mai scrittura silenziosa. -->
      <intent-filter>
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
        <data android:scheme="fub" />
      </intent-filter>
      <!-- Scorciatoie app (shortcuts.xml, stessi 4 comandi reali). -->
      <meta-data android:name="android.app.shortcuts" android:resource="@xml/shortcuts" />
    </activity>
    <!-- Widget home (FubWidgetProvider + fub_widget_info/layout): 4 tap -> fub://. -->
    <receiver android:name=".FubWidgetProvider" android:exported="false">
      <intent-filter>
        <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
      </intent-filter>
      <meta-data android:name="android.appwidget.provider" android:resource="@xml/fub_widget_info" />
    </receiver>
  </application>
</manifest>
