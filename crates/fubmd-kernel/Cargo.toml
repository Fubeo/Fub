[package]
name = "fubmd-kernel"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Core agnostico di FubMD: vault, grafo dei link, registry dei provider, event bus. NON dipende da markdown, wasm o tauri."

[dependencies]
fubmd-abi = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
camino = { workspace = true }
thiserror = { workspace = true }

# INVARIANTE: niente comrak/pulldown, wasmtime, tauri qui dentro. Il core non
# sa cosa sia il markdown.

[dev-dependencies]
# i test del grafo costruiscono DocumentModel a mano: zero markdown.
