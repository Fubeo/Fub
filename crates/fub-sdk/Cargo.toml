[package]
name = "fub-sdk"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Ergonomic helpers for writing Fub providers (native and, in the future, guest WASM): text scanning toolkit for tags/wikilinks, re-exports the contract."

[dependencies]
fub-abi = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
regex = { workspace = true }
