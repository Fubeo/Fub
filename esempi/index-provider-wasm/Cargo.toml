# Real inbound index fixture using the canonical WIT exports.
[package]
name = "index-provider-wasm"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = "0.60"

[features]
trap-routes = []

[profile.release]
strip = true
opt-level = "s"
